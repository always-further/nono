---
phase: 44.1-oidc-fail-closed-remediation-req-review-fu-01-t-44-01-cr-01
verified: 2026-05-20T22:30:00Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 44.1: OIDC Fail-Closed Remediation Verification Report

**Phase Goal:** Restore the pre-Phase-44 explicit fail-closed contract on `nono trust verify` keyless paths — verify MUST error when neither `--issuer` nor a non-empty `NONO_TRUST_OIDC_ISSUER` is provided — while preserving the env-var fallback as opt-in trust anchor. Remediates T-44-01 (Spoofing-class regression) and CR-01 (CRITICAL code review finding) carried out of Phase 44.

**Verified:** 2026-05-20T22:30:00Z
**Status:** passed
**Re-verification:** No — initial verification.

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                             | Status     | Evidence                                                                                                                                                                                                          |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Keyless verify errors with "keyless bundle requires --issuer / NONO_TRUST_OIDC_ISSUER" when both inputs absent                                                    | VERIFIED   | Test `read_required_oidc_issuer_fails_closed_when_both_unset` PASSES (PRIMARY REGRESSION GATE). Error string asserted via `err.contains("keyless bundle requires --issuer")` + `err.contains("NONO_TRUST_OIDC_ISSUER")` at `crates/nono-cli/src/trust_cmd.rs:2277-2282`. |
| 2   | Env-var fallback remains opt-in; `--issuer` not required when env-var explicitly set; URL-shape validated via `url::Url::parse`                                  | VERIFIED   | Test `read_required_oidc_issuer_returns_env_value_when_user_unset_and_env_set` PASSES. Helper at `trust_cmd.rs:937-960` calls `Url::parse(&env_value)` on line 956. D-44-B3 preserved.                            |
| 3   | Hard-coded `GITHUB_ACTIONS_OIDC_ISSUER` default no longer reaches the verify-callsite trust-anchor decision                                                       | VERIFIED   | `grep '^(pub )?fn configured_oidc_issuer' crates/nono/src/trust/signing.rs` → 0 matches. Library function deleted; both verify callsites now call CLI-side helper.                                                |
| 4   | Regression-codifying test `configured_oidc_issuer_falls_back_to_github_default_when_unset` no longer exists                                                        | VERIFIED   | `grep -rn 'configured_oidc_issuer_falls_back_to_github_default_when_unset' crates/` → 0 matches across entire workspace. Test fully removed; migration trace in `signing.rs:1107-1124` documents the deletion.    |
| 5   | New regression test pins fail-closed shape at verify-callsite boundary                                                                                            | VERIFIED   | `read_required_oidc_issuer_fails_closed_when_both_unset` exists at `trust_cmd.rs:2272-2284`, passes, and asserts both `--issuer` and `NONO_TRUST_OIDC_ISSUER` name appear in the error message.                  |
| 6   | Malformed-URL fail-closed branch retains coverage                                                                                                                 | VERIFIED   | Test `read_required_oidc_issuer_rejects_malformed_env_url` at `trust_cmd.rs:2303-2315` PASSES; preserves the historical `configured_oidc_issuer_rejects_malformed_env_value` semantics with migration comment at line 2300. |
| 7   | CLI `--issuer` doc accurately reflects restored contract — REQUIRED unless `NONO_TRUST_OIDC_ISSUER` is explicitly set                                             | VERIFIED   | `crates/nono-cli/src/cli.rs:3046-3056` documents "REQUIRED for keyless verify, unless NONO_TRUST_OIDC_ISSUER is explicitly set" AND "Either this flag OR a non-empty NONO_TRUST_OIDC_ISSUER env-var MUST be supplied". |
| 8   | Workspace clippy passes `-D warnings -D clippy::unwrap_used` on host (cross-target Linux/macOS marked PARTIAL per `cross-target-verify-checklist.md`)              | VERIFIED   | Host clippy `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used` exit 0; cross-target lanes documented PARTIAL per Windows-host limitation (live CI is decisive signal).                |

**Score:** 8/8 truths VERIFIED

### Required Artifacts

| Artifact                                  | Expected                                                                                                                                                     | Status     | Details                                                                                                                                                                  |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `crates/nono-cli/src/trust_cmd.rs`        | Fail-closed env-var read at both keyless verify callsites; contains canonical error string                                                                  | VERIFIED   | Helper defined at line 937-960; called at line 1036 (multi-subject) and line 1231 (single-file). Error string `"keyless bundle requires --issuer <OIDC_URL> or NONO_TRUST_OIDC_ISSUER"` at line 947-948. `use url::Url;` at line 17. |
| `crates/nono/src/trust/signing.rs`        | `configured_oidc_issuer` function removed; regression test removed; malformed-env coverage migrated                                                          | VERIFIED   | `grep '^(pub )?fn configured_oidc_issuer'` → 0 matches; `grep 'configured_oidc_issuer_falls_back_to_github_default_when_unset'` → 0 matches. Migration trace at lines 136-149 + 1107-1124. `validate_oidc_issuer` + `GITHUB_ACTIONS_OIDC_ISSUER` const retained as documented. |
| `crates/nono-cli/src/cli.rs`              | Updated `--issuer` doc string with REQUIRED-unless-env-var contract; documents `NONO_TRUST_OIDC_ISSUER`                                                       | VERIFIED   | Doc at lines 3046-3056 contains "REQUIRED for keyless verify, unless NONO_TRUST_OIDC_ISSUER" and "Either this flag OR a non-empty NONO_TRUST_OIDC_ISSUER env-var MUST be supplied" and CLAUDE.md Fail-Secure citation. |

### Key Link Verification

| From                                                                                | To                                                              | Via                                                                       | Status | Details                                                                                                                                                                                                                                       |
| ----------------------------------------------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `trust_cmd.rs::verify_multi_subject_file` (line 1036)                                | Operator-supplied trust anchor (`--issuer` OR env-var)         | `read_required_oidc_issuer(user_issuer)?` — fail-closed `ok_or_else(...)?` | WIRED  | Line 1036: `let req_issuer_owned = read_required_oidc_issuer(user_issuer)?;` — no silent default substitution. Helper body at lines 937-960 fail-closes when both inputs absent.                                                              |
| `trust_cmd.rs::verify_single_file` (line 1231)                                      | Operator-supplied trust anchor (`--issuer` OR env-var)         | `read_required_oidc_issuer(user_issuer)?` — fail-closed `ok_or_else(...)?` | WIRED  | Line 1231: `let req_issuer_owned = read_required_oidc_issuer(user_issuer)?;` — second callsite is identical shape to the first.                                                                                                              |
| Verify-path fail-closed unit test                                                   | Canonical fail-closed error string                              | `#[test] fn read_required_oidc_issuer_fails_closed_when_both_unset`        | WIRED  | Test at `trust_cmd.rs:2272-2284` runs the helper with both inputs absent and asserts the error message contains both `--issuer` and `NONO_TRUST_OIDC_ISSUER` literal substrings. Test PASSES.                                                |

### Data-Flow Trace (Level 4)

| Artifact                                            | Data Variable                                  | Source                                                                | Produces Real Data | Status   |
| --------------------------------------------------- | ---------------------------------------------- | --------------------------------------------------------------------- | ------------------ | -------- |
| `trust_cmd.rs::read_required_oidc_issuer` (helper)   | `req_issuer_owned` (passed to `validate_oidc_issuer`) | `std::env::var("NONO_TRUST_OIDC_ISSUER")` after `Url::parse` validation | Yes (live env)     | FLOWING  |
| `trust_cmd.rs::verify_multi_subject_file` callsite  | `req_issuer` (`&str` to `validate_oidc_issuer`) | `read_required_oidc_issuer(user_issuer)?` (line 1036)                  | Yes (helper return)| FLOWING  |
| `trust_cmd.rs::verify_single_file` callsite         | `req_issuer` (`&str` to `validate_oidc_issuer`) | `read_required_oidc_issuer(user_issuer)?` (line 1231)                  | Yes (helper return)| FLOWING  |

### Behavioral Spot-Checks

| Behavior                                                                                          | Command                                                                                                                                       | Result                                                            | Status |
| ------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ------ |
| PRIMARY REGRESSION GATE — fail-closed when both inputs absent                                     | `cargo test -p nono-cli --bin nono trust_cmd::tests::read_required_oidc_issuer_fails_closed_when_both_unset`                                  | 1 passed; 0 failed (exit 0)                                       | PASS   |
| All 5 new fail-closed regression tests                                                            | `cargo test -p nono-cli --bin nono trust_cmd::tests::read_required_oidc_issuer`                                                              | 5 passed; 0 failed (exit 0)                                       | PASS   |
| Trust-signing library tests survive deletion                                                      | `cargo test -p nono --lib trust::signing`                                                                                                    | 31 passed; 0 failed (exit 0)                                      | PASS   |
| Workspace clippy with strict lints                                                                | `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used`                                                                | Exit 0; no warnings, no errors                                    | PASS   |
| Malformed-URL coverage migrated and passes                                                        | `cargo test -p nono-cli --bin nono trust_cmd::tests::read_required_oidc_issuer_rejects_malformed_env_url`                                    | 1 passed; 0 failed (exit 0)                                       | PASS   |

### Requirements Coverage

| Requirement       | Source Plan                                              | Description                                                                                                                              | Status      | Evidence                                                                                                                                                                                                            |
| ----------------- | -------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| REQ-REVIEW-FU-01  | `44.1-01-oidc-fail-closed-remediation-PLAN.md` (frontmatter `requirements: [REQ-REVIEW-FU-01]`) | "16 REVIEW.md warnings resolved via single `chore(v2.6-followup):` plan — Phase 37 (10 warnings, incl. WR-09 OIDC issuer-pin production-verifier wiring)..." | SATISFIED   | `REQUIREMENTS.md:29-31` lists REQ-REVIEW-FU-01 as [x] DONE. Phase 44.1 specifically closes the T-44-01 regression introduced by WR-09 wiring — the fail-closed contract restoration at both verify callsites is the remediation. REQ traceable through PLAN frontmatter → ROADMAP.md Phase 44.1 success criteria → restored fail-closed code at `trust_cmd.rs:937-960` / `1036` / `1231`. |

No orphaned requirements found — `REQUIREMENTS.md` lists only REQ-REVIEW-FU-01 against Phase 44 (and by extension Phase 44.1 follow-up), which matches the plan's frontmatter declaration.

### Anti-Patterns Found

| File                                                | Line          | Pattern                                  | Severity | Impact                                                                                                                       |
| --------------------------------------------------- | ------------- | ---------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `crates/nono-cli/src/trust_cmd.rs`                  | 2254, 2263    | `.expect("...")` in test module          | Info     | Both inside `#[cfg(test)] mod tests` block (line 2186 banner + module). CLAUDE.md exempts test modules; no production impact. |

No blocker or warning anti-patterns found in production code. No `#[allow(dead_code)]` introduced (`git diff 4ee6226c..HEAD -- crates/nono-cli/src/trust_cmd.rs crates/nono/src/trust/signing.rs crates/nono-cli/src/cli.rs | grep '^+#\[allow(dead_code)' | wc -l` → 0). No `TODO`, `FIXME`, `placeholder`, `unimplemented!`, or `todo!()` patterns introduced in production code.

### Commit Discipline

| Hash       | Subject                                                                                       | DCO Signed-off-by                          |
| ---------- | --------------------------------------------------------------------------------------------- | ------------------------------------------ |
| `922d1eb7` | `test(44.1-01): pin verify-path fail-closed contract for keyless OIDC issuer`                 | Oscar Mack <oscar.mack.jr@gmail.com> (OK)  |
| `abe5c09c` | `feat(44.1-01): restore fail-closed contract on keyless OIDC issuer verify path`              | Oscar Mack <oscar.mack.jr@gmail.com> (OK)  |
| `f13b0b3a` | `refactor(44.1-01): remove dead configured_oidc_issuer + regression-codifying test`           | Oscar Mack <oscar.mack.jr@gmail.com> (OK)  |
| `68b09e68` | `docs(44.1-01): clarify --issuer required-or-env-var contract`                                | Oscar Mack <oscar.mack.jr@gmail.com> (OK)  |
| `156cd8ca` | `docs(44.1-01): summary — fail-closed OIDC remediation closes T-44-01 / CR-01`                | Oscar Mack <oscar.mack.jr@gmail.com> (OK)  |

All 5 phase-44.1-01 commits land DCO trailers per CLAUDE.md § Commits + D-44-E7.

### Cross-Target Clippy Disposition

| Target                              | Status   | Detail                                                                                                                                                                                                                                                                            |
| ----------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Windows host (x86_64-pc-windows-msvc) | VERIFIED | `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used` exit 0 during verification run.                                                                                                                                                                  |
| x86_64-unknown-linux-gnu            | PARTIAL  | SKIPPED on Windows dev host due to missing cross-toolchain. Live GH Actions Linux Clippy lane on head SHA is the decisive signal per `.planning/templates/cross-target-verify-checklist.md` § PARTIAL Disposition. REQ marked PARTIAL pending CI confirmation.                  |
| x86_64-apple-darwin                 | PARTIAL  | SKIPPED on Windows dev host due to missing cross-toolchain. Live GH Actions macOS Clippy lane on head SHA is the decisive signal per `.planning/templates/cross-target-verify-checklist.md` § PARTIAL Disposition. REQ marked PARTIAL pending CI confirmation.                  |

**Justification:** The 3 modified files (`crates/nono-cli/src/trust_cmd.rs`, `crates/nono/src/trust/signing.rs`, `crates/nono-cli/src/cli.rs`) contain no `#[cfg(target_os = "linux")]` / `#[cfg(target_os = "macos")]` blocks, are not under `exec_strategy/`, and are not under `bindings/c/src/`. Strictly the CLAUDE.md MUST/NEVER bullet does not apply — but the plan elected to apply the gate broadly as defense-in-depth for the security-critical trust module. PARTIAL disposition is consistent with the standing memory pattern `feedback_clippy_cross_target.md`.

### Human Verification Required

None. Phase 44.1 is a code fix verifiable entirely from grep + test outcomes. No UAT items required per the verification_focus directive.

### Gaps Summary

No gaps found. All 10 verification checks from the verification_focus block pass:

1. PRIMARY REGRESSION GATE `read_required_oidc_issuer_fails_closed_when_both_unset` PASSES (exit 0).
2. Both verify callsites (`verify_multi_subject_file:1036`, `verify_single_file:1231`) call `read_required_oidc_issuer`; 0 executable references to `configured_oidc_issuer` in CLI code (only 2 comment-block citations at lines 955 + 2300, neither in executable position).
3. Library function `configured_oidc_issuer` is GONE — `grep '^(pub )?fn configured_oidc_issuer'` → 0 matches.
4. Regression-codifying test `configured_oidc_issuer_falls_back_to_github_default_when_unset` is GONE — 0 matches across entire workspace.
5. Migrated test `read_required_oidc_issuer_rejects_malformed_env_url` exists and PASSES.
6. CLI doc at `cli.rs:3046-3056` documents "REQUIRED for keyless verify, unless NONO_TRUST_OIDC_ISSUER is explicitly set" + "Either this flag OR a non-empty NONO_TRUST_OIDC_ISSUER env-var MUST be supplied".
7. Workspace clippy `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used` exits 0.
8. No `#[allow(dead_code)]` added by phase commits.
9. All 5 phase-44.1-01 commits carry DCO trailer `Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>`.
10. REQ-REVIEW-FU-01 traceable from PLAN frontmatter to REQUIREMENTS.md (line 29-31, 86).

The cross-target Linux/macOS clippy is PARTIAL (Windows host has no cross-toolchain) — accepted per `cross-target-verify-checklist.md` PARTIAL disposition; live CI on the head SHA is the decisive signal.

### Goal Achievement

The phase goal is FULLY ACHIEVED. The pre-Phase-44 fail-closed contract is restored at both keyless verify callsites; the silent-default-substitution regression (T-44-01) is structurally impossible because the library function that performed the substitution no longer exists. The opt-in env-var fallback (D-44-B3) is preserved with eager `url::Url::parse` URL-shape validation. CLI doc accurately reflects the restored contract. All commit-discipline, lint, and test gates pass.

T-44-01 is now closeable by re-running `/gsd-secure-phase 44` per the plan's `<output>` block — that orchestrator action is outside the verifier scope but is now enabled by this remediation.

---

_Verified: 2026-05-20T22:30:00Z_
_Verifier: Claude (gsd-verifier)_
