---
phase: 50
plan: 05
type: execute
wave: 3
depends_on: [50-03, 50-04]
files_modified:
  - .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-HUMAN-UAT.md
  - docs/cli/development/windows-poc-handoff.mdx
autonomous: false
requirements:
  - SPEC-50-REQ-5
  - SPEC-50-REQ-6
user_setup: []

must_haves:
  truths:
    - "`.planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-HUMAN-UAT.md` exists with exactly ONE scenario per SPEC Req 6 — Windows host behind TLS-inspecting corporate proxy whose enterprise CA is in the Windows root store but not in webpki-roots"
    - "The 50-HUMAN-UAT.md scenario contains the literal phrase 'TLS-inspecting corporate proxy' (matches SPEC Req 6 acceptance line 52)"
    - "The scenario has explicit expected output: step [3/5] exits 0; trusted_root.json file exists in cache; stderr has zero transport errors (SPEC Req 6 acceptance bullet)"
    - "50-HUMAN-UAT.md contains a 'Residual risks' section that explicitly notes (Codex R-50-06) Phase 50 fixes TRANSPARENT TLS interception (corp CA in Windows root store + intercepting proxy) but does NOT fix: explicit proxy discovery (PAC files), proxy authentication (Basic / NTLM / Kerberos), or non-transparent forward proxies that require client-side configuration. POC users running on such networks may still need Phase 49 `--from-file` as a backstop."
    - "50-HUMAN-UAT.md residual-risks section also notes (Codex R-50-10) the 403 -> FileNotFound diagnostic obscurity: a corp proxy returning HTTP 403 for policy-deny reasons is normalized by tough's HTTP transport into a TUF 'target not found' error, which can misdirect debugging. POC users hitting a 'TUF target not found' error should check proxy logs for 403s before assuming Sigstore TUF state is bad."
    - "`docs/cli/development/windows-poc-handoff.mdx` is updated so v0.53.x+ Path A is described as succeeding natively on Windows corp-network hosts (no `--from-file` required); `--from-file` is reframed from 'for corp-network failures' to 'for hosts with no outbound network at all' (SPEC In-Scope last bullet; D-50 discretion bullet 3)"
    - "The doc change does NOT remove or break the existing Phase 49 `--from-file` flag documentation — Phase 49's escape hatch remains in place as a backstop (CONTEXT.md `<specifics>`)"
    - "Cross-target clippy verification: D-50-13 HARD pass (Codex R-50-04). Plan 50-01 Task 0 pre-flighted the rustup targets (Option 3 — added cross-toolchain install as a Wave 0 task). This plan's Task 3 runs `cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` AND `--target x86_64-apple-darwin -- ...` and MUST PASS BOTH on the local dev host. The previous 'Outcome B' deferral path is REMOVED — it contradicted D-50-13. If either lane fails (lint or toolchain), Phase 50 is BLOCKED until resolved; no 'defer to CI' fallback is acceptable per the literal D-50-13 contract."
    - "All grep-verifiable SPEC acceptance criteria from `50-SPEC.md` lines 82-94 are re-confirmed at the end of this plan as a phase-wide gate"
  artifacts:
    - path: ".planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-HUMAN-UAT.md"
      provides: "Single human-UAT scenario for corp-network proof (Req 6) + residual-risk section (R-50-06 proxy discovery; R-50-10 403 diagnostic obscurity)"
      contains: "TLS-inspecting corporate proxy"
      min_lines: 60
    - path: "docs/cli/development/windows-poc-handoff.mdx"
      provides: "Updated trust-root setup section reframing --from-file role and noting v0.53.x+ native corp-network success"
      contains: "v0.53"
  key_links:
    - from: ".planning/phases/50-.../50-HUMAN-UAT.md"
      to: "SPEC Req 6 acceptance text (literal scenario)"
      via: "verbatim copy of acceptance criteria into runnable steps"
      pattern: "TLS-inspecting"
    - from: ".planning/phases/50-.../50-HUMAN-UAT.md Residual risks"
      to: "Phase 49 --from-file documentation"
      via: "explicit POC-user guidance for non-transparent-proxy / 403-misdirection scenarios"
      pattern: "Residual risks"
    - from: "docs/cli/development/windows-poc-handoff.mdx"
      to: "v0.53.x+ native corp-network success note"
      via: "inline patch above the existing Path A / Path B framing"
      pattern: "v0\\.53"
    - from: "Plan 50-05 Task 3 cross-target clippy"
      to: "Plan 50-01 Task 0 rustup target pre-flight"
      via: "pre-installed toolchains make HARD pass mechanically achievable"
      pattern: "rustup target"
---

<objective>
Final-wave verification + documentation: (1) write the single HUMAN-UAT scenario that captures the corp-network proof criterion (SPEC Req 6) WITH explicit residual-risk sections covering proxy discovery/PAC/auth (R-50-06) and 403-> FileNotFound diagnostic obscurity (R-50-10); (2) update `docs/cli/development/windows-poc-handoff.mdx` to reflect that v0.53.x+ Path A succeeds natively on corp-network Windows hosts (reframing `--from-file` from "for corp-network failures" to "for hosts with no outbound network"); (3) run cross-target clippy on `x86_64-unknown-linux-gnu` + `x86_64-apple-darwin` per CLAUDE.md MUST/NEVER + D-50-13 HARD-pass mandate — NO Outcome B deferral (R-50-04 fix: pre-flighted in Plan 50-01 Task 0; HARD pass is now mechanically achievable on the dev host).

Purpose: Closes SPEC Req 6 (HUMAN-UAT artifact exists with the right scenario + residual risks called out) and D-50-13 (cross-target clippy green; no deferral). Performs the developer-discretion doc-update from CONTEXT.md `<decisions>` bullet 3 so future POC users routing through the Windows handoff page see the corrected state.

Output: 50-HUMAN-UAT.md ready for a user run with explicit residual-risk callouts; windows-poc-handoff.mdx updated; cross-target clippy verified LOCALLY (no CI-deferral fallback — Codex R-50-04 resolved by Plan 50-01 Task 0 pre-flight + this plan's removal of the previous Outcome B path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-SPEC.md
@.planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-CONTEXT.md
@.planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-RESEARCH.md
@.planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-PATTERNS.md
@.planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-REVIEWS.md
@.planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-01-SUMMARY.md
@.planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-03-SUMMARY.md
@.planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-04-SUMMARY.md
@.planning/debug/resolved/sigstore-tuf-fetch-transport.md
@.planning/templates/cross-target-verify-checklist.md
@docs/cli/development/windows-poc-handoff.mdx
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 50-HUMAN-UAT.md with corp-network scenario + Residual Risks section (SPEC Req 6 + R-50-06 + R-50-10)</name>
  <files>.planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-SPEC.md (Req 6 lines 50-53 — verbatim scenario text)
    - .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-CONTEXT.md (D-50-09 — HUMAN-UAT is dispositive TLS check)
    - .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-REVIEWS.md (R-50-06 proxy-discovery residual risk; R-50-10 403 -> FileNotFound diagnostic obscurity)
    - .planning/debug/resolved/sigstore-tuf-fetch-transport.md (original failure trace — quoted in the scenario for context)
    - .planning/phases/49-sigstore-trust-root-poc-resilience-from-file-flag-release-as/ (look for any prior 49-HUMAN-UAT.md to mirror format)
  </read_first>
  <action>
    Create `.planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-HUMAN-UAT.md` with the exact content below. The scenario text is derived verbatim from SPEC Req 6 lines 50-53. The Residual Risks section is NEW per Codex R-50-06 + R-50-10.

    IMPORTANT — placeholder substitution: the embedded sample below uses `{FRONTMATTER-OPEN}` and `{FRONTMATTER-CLOSE}` as stand-ins for the literal YAML frontmatter delimiter `---` (three dashes on a line by themselves). When you create the actual `50-HUMAN-UAT.md` file, REPLACE each `{FRONTMATTER-OPEN}` and `{FRONTMATTER-CLOSE}` with `---` (three dashes).

````markdown
{FRONTMATTER-OPEN}
phase: 50
slug: corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t
created: 2026-05-21
status: pending
scenarios: 1
{FRONTMATTER-CLOSE}

# Phase 50 — Human-UAT Scenarios

> **Source:** 50-SPEC.md Req 6 (locked).
> One scenario, dispositive for SPEC Req 6. Per D-50-09, this UAT is the
> ONLY real TLS-stack check for Phase 50 — hermetic unit tests in
> `trust_refresh::tests` cover the chain-walk + cache-contract paths but
> deliberately do NOT exercise a real TLS handshake. Pass here is the
> proof that `ureq + platform-verifier` consults the Windows certificate
> store as advertised.

## Scenario 1: TLS-inspecting corporate proxy refresh succeeds natively

**Requirement:** SPEC Req 6
**Owner:** Original POC user (the one who hit the failure traced in
`.planning/debug/resolved/sigstore-tuf-fetch-transport.md`) OR any
contributor with access to a Windows host behind a TLS-inspecting
enterprise proxy whose interceptor CA is deployed via GPO/MDM into the
Windows root certificate store.

### Pre-conditions

- Windows 10 or Windows 11 host
- Host is connected to a network that uses a TLS-inspecting corporate
  proxy (e.g., Zscaler, Netskope, custom enterprise MITM appliance)
- The proxy's enterprise CA is present in `HKLM\SOFTWARE\Microsoft\SystemCertificates\ROOT`
  (verify in PowerShell with: `Get-ChildItem Cert:\LocalMachine\Root | Where-Object Subject -match '<your-corp-CA-name>'`)
- The Phase 50 nono build is installed (v0.53.x+ user-MSI or built from
  Phase 50 close SHA)
- No `<nono_home>/.nono/trust-root/trusted_root.json` exists yet
  (or it can be deleted before the run to force a fresh refresh)

### Steps

1. Open a PowerShell prompt on the corp-network Windows host.
2. (Optional sanity) Confirm Path B (Phase 49 `--from-file` workaround)
   is no longer the only path that works on this host: skip this and
   go straight to step 3 — if Phase 50 worked, you'll never need
   `--from-file` again on this corp-network.
3. Delete any cached trust root from a prior run:
   ```powershell
   Remove-Item -Force "$env:USERPROFILE\.nono\trust-root\trusted_root.json" -ErrorAction SilentlyContinue
   ```
4. Run the refresh:
   ```powershell
   nono setup --refresh-trust-root
   ```

### Expected output

- Step `[3/5] Refreshing Sigstore trusted root...` (or whatever
  index the step lands at — `[X/N]` formatting) exits 0.
- A new file `$env:USERPROFILE\.nono\trust-root\trusted_root.json`
  exists and is non-empty:
  ```powershell
  Test-Path "$env:USERPROFILE\.nono\trust-root\trusted_root.json"
  # Expected: True
  (Get-Item "$env:USERPROFILE\.nono\trust-root\trusted_root.json").Length
  # Expected: > 0
  ```
- Stderr contains ZERO `error sending request for url` errors (this
  was the corp-network failure signature traced in
  `.planning/debug/resolved/sigstore-tuf-fetch-transport.md`).
- Optionally, the new TUF datastore directory `$env:USERPROFILE\.nono\trust-root\tuf-cache\`
  exists and contains tough's local state (`latest_known_time.json`,
  intermediate root.json files, etc.).

### Failure modes to watch for

- If the run prints `Failed to fetch Sigstore trusted root from https://tuf-repo-cdn.sigstore.dev: ...`,
  capture the full error chain and check whether the trailing `: {e}`
  message contains:
  - `error sending request for url` — corp-network failure NOT
    fixed. Verify the build is post-Phase-50 close SHA. If yes, this
    is a regression — file a debug session.
  - `signature` / `threshold` / `expired` — TUF spec-layer failure
    unrelated to corp-network; consult Phase 49 `--from-file`
    fallback (`docs/cli/development/windows-poc-handoff.mdx`).
  - `not found` / `FileNotFound` — see Residual Risks section below
    re: 403 -> FileNotFound diagnostic obscurity.
- If `[3/5]` exits 0 but no cache file appears: investigate
  `nono_home_dir()` resolution under the running shell's `HOME` /
  `USERPROFILE`.

### Residual risks (Codex R-50-06 + R-50-10)

Phase 50 fixes ONE specific failure mode: **transparent TLS interception
by a corporate proxy whose CA is in the Windows root store**. The
following are explicitly NOT fixed by this phase and may still cause
`nono setup --refresh-trust-root` to fail on certain corp networks:

1. **Explicit proxy discovery (PAC files, WPAD, manual proxy settings).**
   nono / ureq does not consult Internet Explorer / Edge / Windows
   `WinHTTP` proxy settings, PAC files, or WPAD discovery. If the
   corp network REQUIRES traffic to flow through a forward proxy (not
   transparent TLS interception), this phase's fix is insufficient.
   Symptom: connection timeouts to `tuf-repo-cdn.sigstore.dev` even
   though browsers on the same host succeed.
   **Workaround:** Use Phase 49 `--from-file` with a trusted_root.json
   downloaded from the release page or another host that has direct
   internet access.

2. **Proxy authentication (Basic / NTLM / Kerberos).**
   Even if the corp proxy is reachable, it may require client
   authentication that nono does not provide. Browsers handle this
   transparently via SSO; nono does not.
   **Workaround:** Same as (1) — Phase 49 `--from-file`.

3. **403 -> FileNotFound diagnostic obscurity (R-50-10).**
   Tough's HTTP transport (and nono's `UreqTransport`) normalize HTTP
   403 responses into TUF "FileNotFound" errors so the chain walk can
   terminate cleanly when the next `N+1.root.json` doesn't exist on the
   server. CORP PROXIES THAT RETURN 403 FOR POLICY-DENY REASONS will
   appear to nono as if a TUF root file is missing.
   Symptom: error message like
   `Sigstore target not found in TUF repo: trusted_root.json` or
   `read trusted_root target: ...not found...` when in fact the proxy
   denied access to the URL.
   **Diagnostic:** Check the corp proxy's access logs for 403s on
   `tuf-repo-cdn.sigstore.dev` paths at the time of the failure. If
   confirmed, escalate to the proxy admin to allow-list those paths
   OR use Phase 49 `--from-file` as a workaround.

4. **CA missing from Windows root store entirely.**
   If the corp-proxy's interceptor CA is not yet deployed via GPO/MDM,
   nono will (correctly) reject the chain as untrusted. This is
   fail-secure behavior; remediation is to ensure the CA is properly
   deployed before testing this scenario.

### Recording the result

After running, append to `.planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-VERIFICATION.md`:

```markdown
## Scenario 1 — TLS-inspecting corporate proxy refresh

- **Host:** {Windows 10|11} build {YYYY.MM}
- **Corp proxy:** {Zscaler|Netskope|other; CA subject snippet OK}
- **nono build SHA:** {git rev-parse HEAD}
- **Result:** pass | fail
- **Cache file size:** {bytes}
- **Stderr excerpt:** {none / or paste failure trail}
- **Date:** {YYYY-MM-DD}
- **Tester:** {handle}
- **If failed, which Residual Risk category applied?** {1-4 or n/a}
```

### Disposition gate

- `pass` -> Phase 50 SPEC Req 6 satisfied; Phase 50 can close.
- `fail` -> investigate the Residual Risk category first; if none match,
  file a debug session; do NOT close Phase 50.
- `not_run` (no corp-network host available within phase window) ->
  document the deferral in 50-VERIFICATION.md with a clear path to
  re-run when a host becomes available; SPEC Req 6 acceptance gate
  remains open.
````

    NOTE: Do NOT pre-populate the scenario result. The orchestrator (or the POC user) runs the scenario and appends to `50-VERIFICATION.md` after the run.
  </action>
  <verify>
    <automated>grep -cE 'TLS-inspecting corporate proxy' .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-HUMAN-UAT.md ; grep -cE '## Scenario 1' .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-HUMAN-UAT.md ; grep -cE 'Residual risks' .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-HUMAN-UAT.md` exists
    - File contains exactly 1 occurrence of `## Scenario 1` (single scenario per SPEC Req 6)
    - File contains the literal string `TLS-inspecting corporate proxy` (matches SPEC Req 6 line 52 phrasing)
    - File contains `nono setup --refresh-trust-root` exactly once in the Steps section
    - File contains `Stderr contains ZERO` or similar pinning of the SPEC Req 6 acceptance criterion ("zero transport errors")
    - File contains a `### Residual risks` (or similar heading) section listing AT LEAST: (a) PAC/proxy discovery (R-50-06), (b) proxy authentication (R-50-06), (c) 403 -> FileNotFound diagnostic obscurity (R-50-10), (d) CA missing from Windows root store
    - File contains a "Recording the result" section pointing to `50-VERIFICATION.md` for the pass entry
    - The recording template includes a "If failed, which Residual Risk category applied?" field to capture proxy-discovery vs trust-store failures distinctly
    - Frontmatter declares `scenarios: 1` matching the body
  </acceptance_criteria>
  <done>
    `50-HUMAN-UAT.md` exists with one runnable scenario AND an explicit Residual Risks section addressing Codex R-50-06 (proxy discovery / PAC / auth) and R-50-10 (403 -> FileNotFound obscurity); the orchestrator / POC user can execute it on a Windows corp-network host and record the result in `50-VERIFICATION.md` to close SPEC Req 6.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docs/cli/development/windows-poc-handoff.mdx to reflect v0.53.x+ native corp-network success</name>
  <files>docs/cli/development/windows-poc-handoff.mdx</files>
  <read_first>
    - docs/cli/development/windows-poc-handoff.mdx (lines 150-260 — the "Sigstore Trust Root Setup" section; specifically the Path A / Path B framing introduced in Phase 49 + the "Known issue: Sigstore TUF root rotation" subsection)
    - .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-PATTERNS.md (§"docs/cli/development/windows-poc-handoff.mdx" lines 362-398 — reframe direction + style convention + heading levels)
    - .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-CONTEXT.md (D-50 discretion bullet 3 — inline patch OR small rewrite both acceptable)
    - .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-SPEC.md (In-Scope last bullet — doc update is in-scope)
  </read_first>
  <action>
    Locate the existing "Sigstore Trust Root Setup" section in `docs/cli/development/windows-poc-handoff.mdx` (around lines 150-260 per PATTERNS.md analysis; use Grep first to find the exact heading line in case the file has drifted since PATTERNS.md was authored).

    Make a minimum-viable inline patch that:

    1. **Adds a brief v0.53.x+ note** ABOVE the existing Path A / Path B framing block, in the form of a `<Note>` callout (or similar component the file already uses — check the existing file for the convention):

```mdx
<Note>
**As of v0.53.x+:** Path A succeeds natively on Windows hosts behind
TLS-inspecting corporate proxies whose enterprise CA is in the Windows
root certificate store. The HTTP client used for the TUF chain walk
(`nono setup --refresh-trust-root`) now consults the Windows root store
via the `ureq + platform-verifier` transport. The historic
corp-network failure (`error sending request for url`) traced in
`.planning/debug/resolved/sigstore-tuf-fetch-transport.md` is structurally
resolved. See Phase 50 for details.

**Caveats** (not fixed by Phase 50): explicit proxy discovery (PAC files,
WPAD), proxy authentication (Basic / NTLM / Kerberos), and corp proxies
that return HTTP 403 for policy-deny reasons (which nono will report as
"TUF target not found"). For those scenarios, Path B `--from-file`
remains the recommended workaround.
</Note>
```

    2. **Reframes the existing Path B / `--from-file` callout** so it describes the flag's role as "for hosts with no outbound network at all (true air-gapped / offline POC scenarios) AND for residual corp-network failures not covered by Phase 50's transparent-TLS-interception fix" rather than "for corp-network failures generally". A minimum edit is to change a single line or two — replace any phrasing like "Path B is also the recovery path when Path A fails with a stale-embedded-anchor error" with something like:

```mdx
Path B remains the path for hosts with no outbound network at all
(true air-gapped / offline POC scenarios) AND for corp-network
failures NOT covered by Phase 50's transparent-TLS-interception fix
(see Caveats in the v0.53.x+ note above: PAC discovery, proxy auth,
403-policy-deny). It is also the residual recovery path if Path A
fails for any reason other than corp-network TLS interception (e.g.,
upstream Sigstore TUF root rotation; see "Known issue: Sigstore TUF
root rotation" below).
```

    3. **Touch the "Known issue: Sigstore TUF root rotation" subsection** ONLY to clarify the scope: corp-network TLS interception is NO LONGER a cause of Path A failure in v0.53.x+ (that historic failure mode is resolved). The remaining Path A failure mode is upstream stale-anchor due to Sigstore root rotation, which Phase 49's `--from-file` covers.

    Do NOT:
    - Delete the existing Phase 49 Path A / Path B framing (Phase 49's escape hatch remains in place per CONTEXT.md `<specifics>`)
    - Remove the "Known issue: Sigstore TUF root rotation" subsection (still relevant for stale-anchor failures)
    - Change MDX heading levels (preserve existing H2 / H3 / H4 structure)

    Use the existing file's component conventions (`<Note>`, `<Warning>`, fenced code blocks with `powershell` language tag, etc.) — DO NOT introduce new MDX components the file doesn't already import.

    After the edit, verify:

```bash
grep -n 'v0\.53' docs/cli/development/windows-poc-handoff.mdx
# Expected: at least 1 line referencing v0.53.x+
grep -n 'TLS-inspecting' docs/cli/development/windows-poc-handoff.mdx
# Expected: at least 1 line
grep -n 'from-file' docs/cli/development/windows-poc-handoff.mdx
# Expected: still present (Phase 49 docs preserved)
grep -n 'Caveats' docs/cli/development/windows-poc-handoff.mdx
# Expected: at least 1 line (R-50-06 / R-50-10 caveats called out in user-facing docs too)
```
  </action>
  <verify>
    <automated>grep -cE 'v0\.53' docs/cli/development/windows-poc-handoff.mdx ; grep -cE 'from-file' docs/cli/development/windows-poc-handoff.mdx ; grep -cE 'TLS-inspecting|root certificate store' docs/cli/development/windows-poc-handoff.mdx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'v0\.53' docs/cli/development/windows-poc-handoff.mdx` returns at least 1 (new v0.53.x+ note added)
    - `grep -cE 'from-file' docs/cli/development/windows-poc-handoff.mdx` returns at least 1 (Phase 49 Path B docs preserved — NOT deleted)
    - `grep -cE 'TLS-inspecting|root certificate store' docs/cli/development/windows-poc-handoff.mdx` returns at least 1 (corp-network corrected wording present)
    - `grep -cE 'air-gapped|outbound network|offline POC' docs/cli/development/windows-poc-handoff.mdx` returns at least 1 (Path B reframed)
    - `grep -cE '[Cc]aveats|PAC|proxy auth' docs/cli/development/windows-poc-handoff.mdx` returns at least 1 (R-50-06 caveats called out)
    - The "Sigstore Trust Root Setup" section's structural heading levels (H2/H3/H4) are unchanged from the pre-edit file (verify by `grep -nE '^##+ ' docs/cli/development/windows-poc-handoff.mdx | head -20` before and after; line offsets may shift but heading levels are preserved)
    - The "Known issue: Sigstore TUF root rotation" subsection still exists (`grep -nE 'Known issue.*[Ss]igstore.*[Rr]otation' docs/cli/development/windows-poc-handoff.mdx` returns at least 1)
  </acceptance_criteria>
  <done>
    `docs/cli/development/windows-poc-handoff.mdx` reflects v0.53.x+ native corp-network success with explicit Caveats note covering R-50-06 (PAC / proxy auth) and R-50-10 (403 obscurity); `--from-file` is reframed as the offline-only / residual fallback path; Phase 49's docs are preserved; heading structure intact.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cross-target clippy verification — HARD pass, no Outcome B deferral (D-50-13 + Codex R-50-04)</name>
  <files>(no files modified; produces verification log appended to 50-05-SUMMARY.md)</files>
  <read_first>
    - CLAUDE.md (§ Coding Standards "Cross-target clippy verification" bullet)
    - .planning/templates/cross-target-verify-checklist.md (full file — protocol)
    - .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-CONTEXT.md (D-50-13 — HARD pass, not partial)
    - .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-REVIEWS.md (R-50-04 — Outcome B contradicts D-50-13; Plan 50-01 Task 0 pre-flights the targets to make HARD pass mechanically achievable)
    - .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-01-SUMMARY.md (confirm Plan 50-01 Task 0 outcome — were both cross-toolchains installed successfully?)
    - .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-SPEC.md (Acceptance Criteria line 93 — cross-target clippy on three triples)
  </read_first>
  <action>
    **Codex R-50-04 closure path (Option 3 chosen):** Plan 50-01 Task 0 ran `rustup target add x86_64-unknown-linux-gnu` and `rustup target add x86_64-apple-darwin` as a Wave 0 pre-flight, AND ran `cargo check --workspace --target <triple>` against both to confirm the cross-toolchains are functional on this dev host. If Plan 50-01 Task 0 reported success, this task's HARD pass is mechanically achievable. If Plan 50-01 Task 0 reported a toolchain failure, Phase 50 should already be flagged as BLOCKED (per the Plan 50-01 Task 0 acceptance criteria) — DO NOT proceed with this task until that block is resolved.

    Confirm Plan 50-01 Task 0 outcome FIRST:

```bash
# Sanity: confirm the toolchains are still installed (in case the developer cleaned them up between Plan 01 and Plan 05).
rustup target list --installed 2>/dev/null | grep -cE 'x86_64-unknown-linux-gnu'
# Expected: 1
rustup target list --installed 2>/dev/null | grep -cE 'x86_64-apple-darwin'
# Expected: 1
```

    If either returns 0: HALT this task. The cross-toolchain was pre-flighted in Plan 50-01 Task 0 but is now missing — surface this as a regression. Re-run `rustup target add` for the missing triple before continuing.

    Now run the two HARD cross-target clippy lanes per D-50-13:

```bash
cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used 2>&1 | tee /tmp/clippy-linux.log
LINUX_EXIT=$?
echo "linux exit: $LINUX_EXIT"

cargo clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used 2>&1 | tee /tmp/clippy-macos.log
MACOS_EXIT=$?
echo "macos exit: $MACOS_EXIT"
```

    **D-50-13 HARD-pass disposition (no Outcome B):**

    - **PASS:** Both exits are 0. Phase 50 cross-target verification is COMPLETE locally. Record both logs in 50-05-SUMMARY.md. CI on PR merge re-validates as a redundant safety check.

    - **FAIL (LINT failures):** One or both lanes exit non-zero due to clippy lint findings. DO NOT close this task. Fix the lint findings in the relevant source files (likely `crates/nono-cli/src/trust_refresh.rs` or `crates/nono-cli/src/setup.rs` — cfg-gated Unix code paths), commit the fix, and re-run clippy until both lanes exit 0. Common failure modes:
      - Platform-specific dep feature differences (e.g., `keyring` is `optional = true` and Linux-only gated)
      - Unused imports under a non-host target's cfg (add `#[allow(unused_imports)]` only if the import is genuinely used under another cfg branch)

    - **FAIL (TOOLCHAIN failures):** One or both lanes exit non-zero due to missing system libraries (rare — should have been caught by Plan 50-01 Task 0 `cargo check`, but possible for clippy-specific build steps). DO NOT defer to CI. This is a HARD blocker per D-50-13. Surface the missing system component in 50-05-SUMMARY.md and HALT phase close. The developer must resolve before Phase 50 can ship.

    **What is REMOVED from the previous plan version (per Codex R-50-04):** the previous "Outcome B — deferred to CI / append to cross-target-verify-checklist.md" path. That contradicted D-50-13. The escape hatch documented in `cross-target-verify-checklist.md` continues to exist for OTHER phases, but Phase 50 explicitly opts OUT — D-50-13's HARD-pass language was deliberate per CONTEXT.md.

    Record both clippy outputs in `50-05-SUMMARY.md` with the SHA they ran against.
  </action>
  <verify>
    <automated>rustup target list --installed 2>/dev/null | grep -cE '(x86_64-unknown-linux-gnu|x86_64-apple-darwin)'</automated>
  </verify>
  <acceptance_criteria>
    - `rustup target list --installed | grep -c x86_64-unknown-linux-gnu` returns 1 (toolchain installed — should be from Plan 50-01 Task 0)
    - `rustup target list --installed | grep -c x86_64-apple-darwin` returns 1 (toolchain installed — should be from Plan 50-01 Task 0)
    - `cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` EXITS 0 (HARD pass — D-50-13 + R-50-04)
    - `cargo clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used` EXITS 0 (HARD pass — D-50-13 + R-50-04)
    - 50-05-SUMMARY.md records both clippy logs + SHA they ran against
    - There is NO "Outcome B" fallback section in 50-05-SUMMARY.md (R-50-04 fix: that path is removed; if cross-target lanes can't pass locally, Phase 50 is blocked, not deferred)
  </acceptance_criteria>
  <done>
    D-50-13 cross-target clippy is verified LOCALLY on the dev host. Both `x86_64-unknown-linux-gnu` and `x86_64-apple-darwin` lint lanes exit 0 under `-D warnings -D clippy::unwrap_used`. The previous Outcome B deferral path (which contradicted D-50-13 per Codex R-50-04) is removed; HARD means HARD. Plan 50-01 Task 0's pre-flight made this mechanically achievable.
  </done>
</task>

<task type="auto">
  <name>Task 4: Phase-wide SPEC acceptance gate re-verification (all 11 SPEC acceptance bullets)</name>
  <files>(read-only — produces verification block in 50-05-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-SPEC.md (lines 82-94 — the 11 acceptance criteria checkboxes)
    - All prior plan SUMMARYs (50-01 through 50-04) — confirm each REQ was closed
  </read_first>
  <action>
    Run each grep / cargo command from the SPEC.md Acceptance Criteria block (lines 82-94) and record the result for the 50-05-SUMMARY.md.

    Each item below maps to a SPEC acceptance bullet — execute and record the pass/fail status. NOTE: the Req 1 grep is now SCOPED to setup.rs only per R-50-02 / Plan 50-03 fix.

```bash
# Acceptance 1 (R-50-02 fixed): grep restricted to setup.rs — expected 0
grep -nE 'TrustedRoot::production\(\)' crates/nono-cli/src/setup.rs | wc -l

# Acceptance 2: New chain-walk function exists and invoked exactly once
grep -rcn 'crate::trust_refresh::refresh_production_trusted_root' crates/nono-cli/src/setup.rs

# Acceptance 3: HTTP transport uses ureq::Agent with platform-verifier; no reqwest::Client::builder
grep -rcn 'RootCerts::PlatformVerifier' crates/nono-cli/src/trust_refresh.rs
grep -rcn 'reqwest::Client::builder' crates/nono-cli/src/trust_refresh.rs    # expected 0

# Acceptance 4: All TUF signature verification routes through tough — no hand-rolled verify_role
grep -rcn 'verify_role' crates/nono-cli/src/    # expected 0

# Acceptance 5 (R-50-03 strengthened): byte-identical vs CAPTURED baseline + from_file round-trip
cargo test -p nono-cli trust_refresh::tests::cache_bytes_match_baseline 2>&1 | grep -E 'test result|passed|failed'
cargo test -p nono-cli trust_refresh::tests::cache_file_loadable_by_load_production_trusted_root 2>&1 | grep -E 'test result|passed|failed'

# Acceptance 6 (R-50-07 added): >=6 tests pass on host triple incl. public-wrapper test
cargo test -p nono-cli trust_refresh::tests 2>&1 | grep -E 'test result'

# Acceptance 7: TrustedRoot::from_file succeeds on the new cache file (covered by Test 5 above)
# Already verified above; no separate cargo test needed.

# Acceptance 8: 50-HUMAN-UAT.md contains the corp-network scenario (Task 1 of this plan)
grep -c 'TLS-inspecting corporate proxy' .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-HUMAN-UAT.md

# Acceptance 8.5 (NEW per R-50-06 + R-50-10): residual risks documented in HUMAN-UAT
grep -cE 'Residual risks|PAC|proxy auth|403' .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-HUMAN-UAT.md

# Acceptance 9: Live UAT pass entry in 50-VERIFICATION.md — DEFERRED until orchestrator/POC user runs the scenario
ls .planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-VERIFICATION.md 2>/dev/null && echo "VERIFICATION.md exists" || echo "VERIFICATION.md PENDING (orchestrator gate)"

# Acceptance 10: Zero file diff under crates/**/*.rs on Linux/macOS-only paths
git diff --stat HEAD~10 -- 'crates/**/*_linux.rs' 'crates/**/*_macos.rs' 2>&1 | head -5
# Expected: empty (Phase 50 touches only trust_refresh.rs, setup.rs, Cargo.toml; no platform-specific files)

# Acceptance 11 (R-50-04 HARD pass): cross-target clippy on BOTH Unix triples — both must exit 0 locally
# (record outcome from Task 3 here)

# Acceptance 12 (from SPEC): docs update — covered by Task 2 of this plan
grep -c 'v0\.53' docs/cli/development/windows-poc-handoff.mdx
```

    Compile the results into a verification table in `50-05-SUMMARY.md`:

```markdown
## SPEC.md Acceptance Criteria — Phase 50 Close Gate

| # | Criterion | Command Result | Status |
|---|-----------|----------------|--------|
| 1 | grep -nE 'TrustedRoot::production\(\)' crates/nono-cli/src/setup.rs == 0 (R-50-02 fixed scope) | {count} | OK / FAIL |
| 2 | New function invoked exactly once in setup.rs | {count} | OK / FAIL |
| 3 | ureq + platform-verifier present; no reqwest in trust_refresh.rs | {counts} | OK / FAIL |
| 4 | No hand-rolled verify_role | {count} | OK / FAIL |
| 5 | Byte-identical snapshot test (R-50-03 strengthened — vs captured baseline) passes | {test result} | OK / FAIL |
| 6 | >= 6 hermetic tests pass on host triple (R-50-03 + R-50-07 additions) | {test result} | OK / FAIL |
| 7 | TrustedRoot::from_file round-trip test (R-50-03 additional) passes | {test result} | OK / FAIL |
| 8 | HUMAN-UAT scenario file exists with right text | {count} | OK / FAIL |
| 8.5 | HUMAN-UAT residual-risks section (R-50-06 + R-50-10) | {count} | OK / FAIL |
| 9 | One UAT pass entry in VERIFICATION.md | DEFERRED — orchestrator gate | PENDING |
| 10 | Linux/macOS-only files have zero diff | {git diff stat} | OK / FAIL |
| 11 | Cross-target clippy HARD pass (R-50-04 — no Outcome B) | {Task 3 result} | OK / FAIL |
| 12 | windows-poc-handoff.mdx updated | {count} | OK / FAIL |
```

    If any row is FAIL, DO NOT close Phase 50 — file the failing item back to the appropriate plan for remediation. Row 9 may be PENDING (deferred to orchestrator post-close gate); all other rows must be OK for the phase to close — including Row 11 which is HARD per D-50-13 + R-50-04.
  </action>
  <verify>
    <automated>grep -c 'TrustedRoot::production()' crates/nono-cli/src/setup.rs 2>/dev/null ; grep -c 'crate::trust_refresh::refresh_production_trusted_root' crates/nono-cli/src/setup.rs</automated>
  </verify>
  <acceptance_criteria>
    - All 12 SPEC.md acceptance criteria are verified and recorded in 50-05-SUMMARY.md as either OK, PENDING (acceptable for row 9 only), or FAIL
    - Zero FAIL rows
    - Row 1 (TrustedRoot::production grep, setup.rs scope) shows count = 0
    - Row 2 (new function call site) shows count = 1
    - Row 3 (platform-verifier present; no reqwest::Client::builder in trust_refresh.rs) shows correct counts
    - Row 4 (no hand-rolled verify_role) shows count = 0
    - Row 6 (>=6 tests pass) shows `test result: ok. N passed` with N >= 6 (R-50-03 + R-50-07 expansion)
    - Row 8 (HUMAN-UAT contains "TLS-inspecting corporate proxy") shows count >= 1
    - Row 8.5 (HUMAN-UAT residual risks) shows count >= 1
    - Row 11 (cross-target clippy HARD pass) is OK — Task 3 closed it with both Unix triples passing locally (R-50-04 fix: no PENDING/Outcome B fallback allowed)
    - Row 12 (docs mention v0.53.x+) shows count >= 1
  </acceptance_criteria>
  <done>
    The full SPEC acceptance criteria table is verified and recorded; Phase 50 either passes the close gate (zero FAIL) or has remediation work routed back to the appropriate plan. Only Row 9 (live UAT) may remain PENDING — all 11 other rows must be OK including Row 11 (cross-target clippy HARD pass).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HUMAN-UAT scenario -> POC user runs on real corp-network host | Out-of-band trust; the user's pass/fail report is the security property |
| HUMAN-UAT residual-risks section -> POC users | Explicit documentation of what Phase 50 does + does NOT fix prevents false confidence |
| docs MDX update -> future POC users | Doc drift could mislead users into using `--from-file` for cases that v0.53.x+ now handles natively (informational only; not security) |
| Cross-target clippy local pass -> phase close | D-50-13 + R-50-04: HARD pass on both Unix triples is non-negotiable; no deferral path exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-05-01 | Information Disclosure | HUMAN-UAT records identifiable corp-network details | mitigate | The recording template asks for "CA subject snippet" (truncated), not full enterprise CA fingerprint or proxy hostname. User judgment is the gate. |
| T-50-05-02 | Tampering | Doc update accidentally deletes Phase 49 `--from-file` docs | mitigate | Task 2 acceptance grep enforces `from-file` token still present (>= 1 match); deletion would fail the gate. |
| T-50-05-03 | Information Disclosure / Repudiation | POC user mis-attributes residual-risk failure to Phase 50 regression (R-50-06) | mitigate | HUMAN-UAT Residual Risks section explicitly lists PAC / proxy-auth / 403 / missing-CA as non-Phase-50 failure modes; recording template captures "which Residual Risk category applied" so triage is fast. |
| T-50-05-04 | Spoofing / Repudiation | 403 misdirected as TUF state corruption (R-50-10) | mitigate | HUMAN-UAT Residual Risks #3 explicitly documents the 403 -> FileNotFound normalization and advises checking proxy logs first. |
| T-50-05-05 | Tampering | Cross-target clippy silently skipped via Outcome B (R-50-04) | mitigate | This plan REMOVES the Outcome B fallback path. Task 3 acceptance criteria require both Unix triples to exit 0 LOCALLY on the dev host. Plan 50-01 Task 0 pre-flighted the toolchains; if they're missing here, Phase 50 is blocked (correct semantics of HARD per D-50-13). |
| T-50-05-06 | Spoofing | UAT pass entry forged without real run | accept | The phase ships on user trust; the POC user is also the original failure reporter so motivated to test honestly. |
</threat_model>

<verification>
- `50-HUMAN-UAT.md` exists with the corp-network scenario AND a Residual Risks section covering R-50-06 (PAC / proxy auth) + R-50-10 (403 obscurity)
- `docs/cli/development/windows-poc-handoff.mdx` updated; v0.53.x+ note present with Caveats subsection; `--from-file` reframed; Phase 49 docs preserved
- Cross-target clippy HARD pass: both `x86_64-unknown-linux-gnu` and `x86_64-apple-darwin` lint lanes exit 0 LOCALLY (R-50-04 fix: Outcome B deferral REMOVED)
- All 12 SPEC.md acceptance criteria (plus the 8.5 residual-risks row) verified OK or PENDING (Row 9 only) in 50-05-SUMMARY.md
- HUMAN-UAT scenario is queued for the POC user run (SPEC Req 6 close gate)
</verification>

<success_criteria>
- [ ] `50-HUMAN-UAT.md` created with exactly 1 scenario, containing the literal "TLS-inspecting corporate proxy" phrase
- [ ] `50-HUMAN-UAT.md` contains a Residual Risks section explicitly covering PAC / proxy auth (R-50-06) and 403 -> FileNotFound diagnostic obscurity (R-50-10)
- [ ] `docs/cli/development/windows-poc-handoff.mdx` updated with v0.53.x+ note (including Caveats subsection) + `--from-file` reframe; Phase 49 docs preserved
- [ ] Cross-target clippy HARD pass: both Unix triples exit 0 LOCALLY (R-50-04 fix — no Outcome B deferral path)
- [ ] SPEC acceptance gate table populated in 50-05-SUMMARY.md with all 12 rows OK or PENDING (PENDING allowed only on row 9); zero FAIL
- [ ] Row 11 specifically is OK (HARD pass) — not PENDING — per D-50-13 + R-50-04
- [ ] HUMAN-UAT scenario queued for POC user run (orchestrator gate for SPEC Req 6 close)
- [ ] If clippy fixes were needed during Task 3, they were committed before this plan closed
</success_criteria>

<output>
After completion, create `.planning/phases/50-corp-network-tuf-refresh-via-os-root-store-replace-or-wrap-t/50-05-SUMMARY.md` documenting:
- Wave 3 final-pass landed: HUMAN-UAT + docs + cross-target verify + SPEC gate
- Codex review findings closed: R-50-04 (HARD pass cross-target — Outcome B REMOVED), R-50-06 (proxy-discovery residual risk in UAT + docs), R-50-10 (403 obscurity residual risk in UAT + docs)
- Cross-target clippy outcome: BOTH lanes (linux-gnu + apple-darwin) exit 0 locally, with full logs captured
- 12-row SPEC acceptance gate table (no FAIL; PENDING only on row 9)
- Open follow-ups for orchestrator:
  - Row 9: POC user must run HUMAN-UAT scenario on Windows corp-network host and append to 50-VERIFICATION.md
- A "Phase 50 Ready to Close" statement contingent on row 9 resolving
</output>
</content>
