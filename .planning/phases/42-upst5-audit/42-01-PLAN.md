---
phase: 42-upst5-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/42-upst5-audit/42-01-SUMMARY.md
autonomous: true
requirements: [REQ-UPST5-01]
tags: [upstream-parity, drift-audit, ledger, windows-touch, upst5, adr-review]

must_haves:
  truths:
    - "DIVERGENCE-LEDGER.md exists at .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md (D-42-E4 phase-local convention)"
    - "Ledger frontmatter records D-42-A2 reproducibility fields verbatim: range=v0.53.0..v0.54.0, upstream_head_at_audit captured at first commit of Plan 42-01 per D-42-A3, drift_tool_sh_sha=0834aa664fbaf4c5e41af5debece292992211559, drift_tool_ps1_sha=0834aa664fbaf4c5e41af5debece292992211559, drift_tool_invocation locked, fork_baseline=v0.53.0 (Phase 40 UPST4 sync point — 2026-05-14), date"
    - "Every cluster header carries one of three dispositions: will-sync / fork-preserve / won't-sync (D-42-E5 enum at cluster level only)"
    - "Every cluster's commit-row table follows D-42-C1 / D-42-E6 schema: sha + subject + upstream-tag + categories + files-changed + windows-touch"
    - "windows-touch column resolves to 'yes' or 'no' per row using D-42-C2 mechanical heuristic (substring 'windows' in files_changed OR pinned list {platform.rs, registry.rs, wfp/*, win_*.rs} OR commit-subject keywords 'windows|wfp|registry|wsa|ntdll|kernel32') with judgment-override allowed"
    - "Two known windows-touch commits (5d821c12 'fix(platform): correctly parse windows registry dword values' and 0748cced 'feat(platform): implement robust windows platform detection') appear with windows-touch: yes AND explicit per-commit disposition + rationale (success criterion #2 from ROADMAP § Phase 42)"
    - "Windows-touch:yes commits default to fork-preserve cluster disposition unless empty fork-side proven by diff inspection (D-42-C3) — auditor records the fork-side check explicitly with rationale"
    - "Explicit '## ADR review' section present in the ledger, falsifiable via grep -c \"^## ADR review$\" returning 1 (D-42-C4 + ROADMAP success criterion #3)"
    - "ADR review section verdicts Phase 33 ADR Option A 'continue' with per-cell L/M/H verdicts for 5 dimensions (security posture, windows parity, maintenance cost, divergence risk, contributor velocity); falsifiable via grep -cE \"^\\| (security|windows|maintenance|divergence|contributor)\" returning at least 5 (D-42-C4 upgrade)"
    - "ADR review outcome is one of (a) confirm Option A `continue`, (b) amend with carve-outs, (c) flag a future-supersede trigger — Phase 42 does NOT supersede Phase 33 ADR (still Accepted)"
    - "Explicit '## Empirical cross-check' subsection present in the ledger covering at least 3 fork-shared files spot-checked against upstream v0.53.0..v0.54.0 log; preferentially samples Phase-41-touched files per D-42-E2; falsifiable via grep -c \"^## Empirical cross-check\" returning 1 (ROADMAP success criterion #4)"
    - "Total row count across all cluster commit-row tables >= drift-tool total_unique_commits (REQ-UPST5-01 acceptance #1; D-42-B2 close-gate step 2)"
    - "ROADMAP UPST6 stub committed per D-42-B4 — auditor picks location at plan-walk: (a) v2.6 backlog stub OR (b) v2.5 § Future Cycles holding section (recommended fallback). Stub shape: title 'UPST6 — Upstream v0.54.0…+ sync audit' (or '… sync execution'), Depends on: Phase 43, Plans: 0 / TBD, ADR cross-reference to docs/architecture/upstream-parity-strategy.md § Future audit cadence"
    - "ROADMAP.md Phase 42 v2.5 milestone-block entry flipped to [x] with '(completed YYYY-MM-DD)' appended; Phase Details Plans counter flipped to '1 / 1 plans complete' with checkbox sub-bullet listing [x] 42-01-PLAN.md"
    - "STATE.md frontmatter completed_plans counter bumped; STATE.md Current Position flipped to Phase 42 (upst5-audit) — Phase complete — ready for verification; Last activity stamped"
    - "STATE.md Accumulated Context gains a Plan 42-01 close entry under Key Decisions (v2.5) capturing range, lock-sha, cluster count, commit count, disposition breakdown, windows-touch:yes count, ADR-review verdict outcome, empirical cross-check files sampled, UPST6 stub location decision + commit sha, DCO sign-off"
    - "Drift-tool re-run is idempotent: make check-upstream-drift ARGS=\"--from v0.53.0 --to v0.54.0 --format json\" exits 0 after plan close (D-42-B2 close-gate step 1)"
    - "D-42-E7 Windows-only-files invariant trivially honored: git diff --name-only HEAD~3..HEAD -- crates/ bindings/ scripts/ returns zero files (Phase 42 ships zero .rs / .toml / .sh / .ps1 / Makefile edits) — D-42-B2 close-gate step 8"
    - "Strictly silent on post-v0.54.0 commits (D-42-A4): no task references or processes 66c69f86, 803c6947, fc965ccc, 089cf6a0 — these are deferred to UPST6"
  artifacts:
    - path: ".planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md"
      provides: "Audited inventory of v0.53.0..v0.54.0 fork-vs-upstream divergence with per-cluster dispositions, windows-touch column firing for the first time (5d821c12 + 0748cced + any audit-walk surfaces), explicit ADR review with per-cell L/M/H verdicts, and empirical cross-check subsection"
      contains: "## ADR review, ## Empirical cross-check, ### Cluster:, - **Disposition:**, | sha | subject | upstream-tag | categories | files-changed | windows-touch |, windows-touch: yes, security posture, windows parity, maintenance cost, divergence risk, contributor velocity"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 42 v2.5 entry flipped to complete; Phase 42 detail Plans counter flipped to 1/1; UPST6 stub committed per D-42-B4 location decision"
      contains: "[x] **Phase 42: UPST5 audit**, Plans: 1 / 1 plans complete, [x] 42-01-PLAN.md, UPST6 — Upstream v0.54.0, Depends on: Phase 43, Plans: 0 / TBD"
    - path: ".planning/STATE.md"
      provides: "Plan 42-01 close entry under Key Decisions (v2.5); completed_plans counter bumped; Current Position flipped to Phase 42 ready for verification"
      contains: "Phase 42 Plan 42-01"
    - path: ".planning/phases/42-upst5-audit/42-01-SUMMARY.md"
      provides: "Plan 42-01 close summary mirroring Phase 33/39 Plan-01-SUMMARY shape"
      contains: "REQ-UPST5-01, DIVERGENCE-LEDGER, windows-touch, ADR review, Empirical cross-check"
  key_links:
    - from: "DIVERGENCE-LEDGER.md frontmatter"
      to: "drift-tool reproducibility (D-42-A2 / D-42-A3)"
      via: "frontmatter records range + upstream_head_at_audit + drift_tool shas + invocation verbatim; re-running the invocation against the same upstream HEAD reproduces the input set"
      pattern: "drift_tool_(sh|ps1)_sha|upstream_head_at_audit|drift_tool_invocation"
    - from: "DIVERGENCE-LEDGER.md ## ADR review section"
      to: "docs/architecture/upstream-parity-strategy.md (Phase 33 ADR, Accepted 2026-05-11, re-confirmed at v2.4 close)"
      via: "ADR review verdicts Option A 'continue' with per-cell L/M/H for 5 dimensions; outcome (a) confirm | (b) amend | (c) flag-future-supersede; does NOT supersede"
      pattern: "## ADR review|Phase 33 ADR|Option A|continue|security posture|windows parity|maintenance cost|divergence risk|contributor velocity"
    - from: "DIVERGENCE-LEDGER.md windows-touch:yes per-commit dispositions (5d821c12 + 0748cced)"
      to: "Phase 43 UPST5 sync execution input (immutable per D-42-B3)"
      via: "Phase 43 plan-phase consumes the ledger's cluster summary table AND the explicit per-commit windows-touch dispositions to choose cherry-pick vs D-20 manual-replay; D-42-C3 fork-preserve default protects D-11 invariant"
      pattern: "Disposition:.*will-sync|fork-preserve|won't-sync|5d821c12|0748cced|Target phase:.*UPST5-sync|Phase 43"
    - from: "DIVERGENCE-LEDGER.md ## Empirical cross-check subsection"
      to: "drift-tool D-11 path filter blind-spot mitigation (D-42-E1)"
      via: "Phase 39 empirical-cross-check pattern: spot-check >=3 fork-shared files (preferentially Phase-41-touched files per D-42-E2: exec_strategy.rs HandleTarget migration sites, broker null-handle paths, env_vars guard sites) against upstream v0.53.0..v0.54.0 log to confirm drift tool covered every relevant commit"
      pattern: "## Empirical cross-check|exec_strategy\\.rs|HandleTarget|broker|env_vars"
    - from: "ROADMAP § UPST6 stub (location TBD at plan-walk per D-42-B4)"
      to: "Phase 33 ADR § Future audit cadence rule (D-42-E8)"
      via: "stub Reference line cites docs/architecture/upstream-parity-strategy.md § Future audit cadence; preserves cadence-rule signal under v2.6 backlog OR v2.5 § Future Cycles; UPST6 fires when v0.55.0+ ships"
      pattern: "UPST6 — Upstream v0.54.0|## v2.6 backlog|## Future Cycles|Future audit cadence"
---

<objective>
Run the D-42-A1-locked drift-tool invocation against the v0.53.0..v0.54.0 range, lock `upstream_head_at_audit` at first commit of this plan (D-42-A3), curate `DIVERGENCE-LEDGER.md` mirroring Phase 33 / 39 two-tier shape with the D-42-C1 `windows-touch` column now actually firing for `5d821c12` + `0748cced`, add an explicit `## ADR review` section with per-cell L/M/H verdicts for 5 dimensions (D-42-C4 upgrade), perform empirical cross-check on ≥3 fork-shared files preferentially sampling Phase-41-touched files (D-42-E1 / D-42-E2), queue a UPST6 placeholder per D-42-B4 location decision, update STATE.md, and ship the SUMMARY — all in one plan per D-42-B1.

Purpose: REQ-UPST5-01 demands a falsifiable, disposition-complete divergence inventory before Phase 43 UPST5 sync execution can begin. Phase 42's ledger is the binding input for Phase 43 (analog to how Phase 39's ledger was Phase 40's input, and Phase 33's ledger was Phase 34's input). Phase 42 is the **first audit cycle where the `windows-touch: yes` column actually fires** — Phase 33 had zero such commits across 97 audited; Phase 39 had zero such commits across 22 audited; Phase 42 has at least 2 known (`5d821c12` + `0748cced`) plus whatever the audit-walk surfaces. The D-42-C4 per-cell L/M/H verdict requirement is the Phase-42-specific upgrade vs Phase 39 (which used confirmation-or-flag prose) because empirical evidence is now available for the first time.

Output: 4 files committed across 2-3 atomic commits per the Phase 33 / Phase 39 precedent:
1. `.planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md` (NEW, ~130-180 lines per CONTEXT § Existing Code Insights size estimate for ~20 commits / 5-7 clusters)
2. `.planning/ROADMAP.md` (modified — Phase 42 entry flipped to complete + Phase Details Plans counter flipped + UPST6 stub appended at D-42-B4-chosen location)
3. `.planning/STATE.md` (modified — frontmatter bump + Current Position flip + Plan 42-01 close entry under Key Decisions (v2.5))
4. `.planning/phases/42-upst5-audit/42-01-SUMMARY.md` (NEW)
5. ZERO `.rs` / `.toml` / `.sh` / `.ps1` / `Makefile` edits (D-42-E7 / D-42-B2 step 8 trivially honored).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-upst5-audit/42-CONTEXT.md
@docs/architecture/upstream-parity-strategy.md
@.planning/templates/upstream-sync-quick.md
@.planning/templates/cross-target-verify-checklist.md

<!-- Phase 39 audit-shape primary reference (READ via git from the Phase 39 archive — these files are referenced in CONTEXT § canonical_refs and contain the verbatim template Phase 42 mirrors) -->
<!-- The Phase 39 directory is referenced in CONTEXT.md but the directory itself is not present in the working tree at audit-open time; the templates live in git history at SHA 2f3983f1 (DIVERGENCE-LEDGER) and d75e85ec (PLAN). Executor MAY read via `git show <sha>:<path>` if direct read fails. -->

<interfaces>
<!-- D-42-A2 locked frontmatter shape — every field below MUST appear verbatim in the ledger frontmatter -->

```yaml
---
slug: divergence-ledger-v053-v054
status: complete
type: audit-only
date: <YYYY-MM-DD: audit close date>
range: v0.53.0..v0.54.0
upstream_head_at_audit: <40-char sha captured at first commit of Plan 42-01 per D-42-A3>
drift_tool_sh_sha: 0834aa664fbaf4c5e41af5debece292992211559
drift_tool_ps1_sha: 0834aa664fbaf4c5e41af5debece292992211559
drift_tool_invocation: 'make check-upstream-drift ARGS="--from v0.53.0 --to v0.54.0 --format json"'
fork_baseline: v0.53.0 (Phase 40 UPST4 sync point — 2026-05-14)
total_unique_commits: <integer; auditor fills exact at first commit of Plan 42-01; expected ~20>
---
```

The `0834aa66...` shas are NOT placeholders — they are the Phase 24 drift-tool ship shas, unchanged since 2026-04-29 through Phase 33 + 39, locked by D-42-A2 / D-42-E3. The `v0.53.0 (Phase 40 UPST4 sync point — 2026-05-14)` fork_baseline literal is also locked verbatim.

<!-- D-42-C1 / D-42-E6 commit-row schema — every cluster commit-row table MUST use these 6 columns in this exact order -->

```markdown
| sha | subject | upstream-tag | categories | files-changed | windows-touch |
|-----|---------|--------------|------------|---------------|---------------|
| <sha7> | <commit subject> | <v0.54.0 (or earlier minor for v0.53.x patches if any)> | <comma-separated drift JSON categories[]> | <count of files_changed[]> | <yes | no> |
```

<!-- D-42-C2 windows-touch detection heuristic (mechanical pass) -->

`windows-touch: yes` IFF any of:
- Any file in `files_changed` contains substring `windows`, OR
- Any file in `files_changed` matches pinned list `{platform.rs, registry.rs, wfp/*, win_*.rs}`, OR
- Commit subject contains any of: `windows`, `wfp`, `registry`, `wsa`, `ntdll`, `kernel32`

Auditor judgment-override required (NOT just permitted) for the lead commit of every cluster AND any commit whose subject is ambiguous re: Windows-touch — read the diff and confirm/override the mechanical flag.

<!-- D-42-C3 windows-touch disposition default (Phase 42 SPECIFIC application required for 5d821c12 + 0748cced) -->

For each `windows-touch: yes` commit's cluster:
- DEFAULT disposition = `fork-preserve` (manual replay; protects D-11 invariant against accidental D-19 violation via cherry-pick collision with fork-only `*_windows.rs` files)
- EXCEPTION: if auditor confirms via diff inspection AND fork-side analog check (Phase 31 broker-process + Phase 35/36 supervisor work surveyed for `*_windows.rs` analogs) that upstream lands new code in a file with EMPTY fork-side, disposition MAY flip to `will-sync`
- For `0748cced feat(platform): implement robust windows platform detection`: auditor MUST check whether fork has an analog under `*_windows.rs` paths. If fork-side empty → consider will-sync; if fork-side has its own implementation → fork-preserve with D-20 manual-replay rationale.
- For `5d821c12 fix(platform): correctly parse windows registry dword values`: likely a small fix on top of upstream's `0748cced` platform module. Disposition typically follows `0748cced`'s disposition (cherry-pick together as a Windows-platform-detection cluster, or fork-preserve together).
- Each windows-touch:yes commit's disposition MUST be recorded explicitly with rationale (ROADMAP success criterion #2 + CONTEXT § Phase Boundary requirement).

<!-- D-42-C4 explicit ## ADR review section with per-cell L/M/H verdicts (UPGRADE vs Phase 39 confirmation-only prose) -->

The Phase 33 ADR `docs/architecture/upstream-parity-strategy.md` Decision Table evaluates 3 options against 5 dimensions with L/M/H verdicts per cell. Phase 42 § ADR review MUST contain an explicit per-cell L/M/H verdict table for Option A `continue` across the same 5 dimensions. Verbatim 5 dimensions (must match exactly for falsifiable grep):

1. **security posture** — Windows-only hardening evolution vs upstream's threat model coverage
2. **windows parity** — single-CLI-surface preservation across Linux/macOS/Windows
3. **maintenance cost** — per-sync labor, cherry-pick conflict count, manual-replay rate
4. **divergence risk** — drift accumulation risk between fork and upstream
5. **contributor velocity** — drift-audit + cherry-pick gate review burden per release

Outcome MUST be one of:
- **(a) Confirm Option A `continue`** with updated per-cell L/M/H verdicts reflecting empirical evidence that `windows-touch: yes` clusters can be dispositioned safely via D-42-C3 conservative-default. Likely outcome if 5d821c12 + 0748cced dispositioned cleanly.
- **(b) Amend Option A** — record specific carve-outs (e.g., "Windows-platform-detection commits default to D-20 manual-replay, not cherry-pick"). NOT a supersede; ADR stays Accepted.
- **(c) Flag a future-supersede trigger** — record evidence suggesting Option B `split` or Option C `freeze` should be re-evaluated in UPST6/UPST7. Stays Accepted; documents the trigger threshold (e.g., "if 50%+ clusters become fork-preserve in a future cycle, supersede").

Falsifiable grep patterns (D-42-B2 close-gate step 4):
- `grep -c "^## ADR review$" DIVERGENCE-LEDGER.md` returns 1
- `grep -cE "^\| (security|windows|maintenance|divergence|contributor)" DIVERGENCE-LEDGER.md` returns >= 5

<!-- D-42-E1 / D-42-E2 ## Empirical cross-check subsection (ROADMAP success criterion #4) -->

After the ## ADR review section, add a ## Empirical cross-check subsection that:
- Selects >= 3 fork-shared files for spot-check (representative: 1 from `crates/nono/`, 1 from `crates/nono-cli/` excluding `_windows.rs`, 1 from `crates/nono-proxy/`)
- Preferentially samples Phase-41-touched files per D-42-E2: `crates/nono-cli/src/exec_strategy.rs` (HandleTarget API migration at 14 sites), broker null-handle paths in `crates/nono-shell-broker/`, env_vars guard sites
- For each sampled file, walks `git log v0.53.0..v0.54.0 -- <file>` against upstream and confirms the drift tool's commit list covers every upstream commit touching that file
- Documents findings: confirmed | drift-tool-missed-commit-X (would trigger D-42-D3 quick-task) | other-anomaly

<!-- Drift-tool JSON shape (inherited from Phase 33 — verified VERBATIM at Phase 24 ship; scripts/check-upstream-drift.sh L228-241) -->

```json
{
  "range": "v0.53.0..v0.54.0",
  "from": "v0.53.0",
  "to": "v0.54.0",
  "total_unique_commits": <N>,
  "by_category": {
    "profile": <int>, "policy": <int>, "package": <int>,
    "proxy": <int>, "audit": <int>, "other": <int>
  },
  "commits": [
    {
      "sha": "abcd1234...",
      "subject": "feat(profile): ...",
      "author": "Name",
      "date": "...",
      "additions": <int>,
      "deletions": <int>,
      "files_changed": ["crates/nono-cli/src/profile/mod.rs", ...],
      "categories": ["profile", "policy"]
    }
  ]
}
```

<!-- D-11 path filter (Phase 24) — drift tool EXCLUDES `*_windows.rs` + `crates/nono-cli/src/exec_strategy_windows/`. D-11 is necessary but NOT sufficient: Phase 42 must STILL detect upstream commits adding NEW Windows code OUTSIDE that filter (e.g., 5d821c12 + 0748cced touch `crates/nono-cli/src/platform.rs` or analog — D-11 does NOT exclude that file). Phase 42 is the FIRST cycle where this insufficiency materially fires. -->

<!-- D-42-B4 UPST6 stub location decision — auditor picks at plan-walk:
  (a) v2.6 backlog stub (if v2.6 scoped by Plan 42-01 close): add `## v2.6 backlog` section to ROADMAP.md if not present
  (b) v2.5 § Future Cycles holding section (RECOMMENDED FALLBACK per CONTEXT § D-42-B4): add a `## Future Cycles` section to v2.5 ROADMAP.md documenting "UPST6 cadence trigger: when v0.55.0+ ships"
  (c) Cross-milestone backlog file `.planning/backlog/upst6-stub.md` (only if (a)+(b) infeasible)
Stub shape (verbatim with placeholders for chosen-phrase):
-->

```markdown
### Phase TBD-NN: UPST6 — Upstream v0.54.0…+ <sync audit | sync execution>

**Goal:** Mirror Phase 33 / Phase 39 / Phase 42 audit shape. Inventory of upstream divergence from v0.54.0 forward (commits accumulated post-Phase 42 audit cutoff `6b00932f`, including v0.55.0+ tags as they ship). Per-cluster disposition + parity-strategy review against Phase 33 ADR; absorbs the 4 known post-v0.54.0 commits (`66c69f86 fix(snapshot): validate restore targets against symlinks`, `803c6947 chore(deps): bump nix`, `fc965ccc chore(deps): bump tokio`, `089cf6a0 chore(deps): bump cosign-installer`) plus any subsequent additions.

**Depends on:** Phase 43 (UPST5 execution baseline lands fork at v0.54.0).

**Requirements:** TBD when v2.6 scope locks (or v2.5 § Future Cycles activates).

**Plans:** 0 / TBD — to be populated during `/gsd-plan-phase TBD-NN`.

**Estimated effort:** ~1 week (mirrors Phase 39 + Phase 42 sizing).

**Reference:** `.planning/phases/33-windows-parity-upstream-0-52-divergence/` (audit-shape root template), `.planning/phases/39-upst4-audit/` (windows-touch column zero-fire example), `.planning/phases/42-upst5-audit/` (Phase 42 worked example with windows-touch:yes fires for 5d821c12 + 0748cced + per-cell L/M/H ADR verdict table), `docs/architecture/upstream-parity-strategy.md` § Future audit cadence (Phase 33 ADR cadence rule).
```

Title-wording discretion per CONTEXT § Claude's Discretion: auditor picks `… sync audit` (DEFAULT — safer; expected outcome) vs `… sync execution` (only if Phase 42 ledger surfaces zero windows-touch + zero fork-preserve AND next-cycle commit count is small enough to skip a separate audit). Lock the choice at write time based on the Phase 42 ledger shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock provenance — fetch upstream + capture upstream_head_at_audit + run drift tool + extract per-commit data (D-42-A3 FIRST act of Plan 42-01)</name>
  <files>(none committed — drift JSON captured to ci-logs-local/drift/ scratch; per D-42-A2 raw JSON is NOT committed)</files>
  <read_first>
    - .planning/phases/42-upst5-audit/42-CONTEXT.md § decisions D-42-A1 (range = v0.53.0..v0.54.0), D-42-A2 (frontmatter fields verbatim), D-42-A3 (lock at FIRST commit of Plan 42-01), D-42-A4 (silent on post-v0.54.0 — IGNORE the 4 known commits 66c69f86, 803c6947, fc965ccc, 089cf6a0)
    - .planning/phases/42-upst5-audit/42-CONTEXT.md § code_context "Drift signal preview" listing the ~20 expected commits in v0.53.0..v0.54.0 (informational only — auditor confirms exact set from drift JSON)
    - Phase 39 ledger via git: `git show 2f3983f1:.planning/phases/39-upst4-audit/DIVERGENCE-LEDGER.md` for the verbatim shape template (Phase 39 archive directory is referenced in CONTEXT but not present in working tree at audit-open time)
    - Phase 33 ADR `docs/architecture/upstream-parity-strategy.md` § Decision Table (lines 47-53) for the 5-dimension L/M/H format the ADR review section will mirror
    - scripts/check-upstream-drift.sh (drift-tool script; sha 0834aa66 unchanged since Phase 24 ship 2026-04-29; Phase 42 invocation uses the locked sha)
  </read_first>
  <action>
    Execute the D-42-A3 "lock at first commit of Plan 42-01" protocol, then extract the structured commit data for cluster curation in Task 2. This is the FIRST act of Plan 42-01 — the `git fetch upstream --tags` MUST be the first command run.

    1. **Fetch upstream tags** (D-42-A3 — FIRST act of Plan 42-01):
       ```bash
       git fetch upstream --tags
       echo "fetch-exit=$?"
       ```
       Expected exit 0. The fetch is idempotent (already-fetched tags are no-ops). The `upstream` remote at `https://github.com/always-further/nono.git` was verified present 2026-05-17 (42-CONTEXT.md § canonical_refs § Upstream source). Tag `v0.54.0` at `6b00932f` is the upper bound.

    2. **Capture upstream_head_at_audit** (D-42-A3):
       ```bash
       UPSTREAM_HEAD=$(git rev-parse upstream/main)
       echo "upstream_head_at_audit=$UPSTREAM_HEAD"
       ```
       Record this 40-char sha. Task 2 writes this verbatim into the ledger frontmatter's `upstream_head_at_audit:` field. The expected approximate value is `18c34f3a` (CONTEXT § Upstream source 2026-05-17 capture); actual value at execution time may differ if upstream commits land in the interim. The range stays `v0.53.0..v0.54.0` regardless (D-42-A1 + D-42-A4).

    3. **Run the D-42-A2 locked drift-tool invocation** (idempotent — drift tool is read-only against the local git repo):
       ```bash
       mkdir -p ci-logs-local/drift
       make check-upstream-drift ARGS='--from v0.53.0 --to v0.54.0 --format json' > ci-logs-local/drift/drift-v054.json 2>&1
       echo "drift-exit=$?"
       ```
       Expected exit 0. The Phase 33 / 39 deviation (Rule 3) is inherited: scratch JSON lands in `ci-logs-local/drift/` (Windows-host MSYS `/tmp/` is inaccessible to Python interpreters; `ci-logs-local/` is in `.gitignore` — verified). If `make` is not on PATH (Windows host), dispatch directly: `bash scripts/check-upstream-drift.sh --from v0.53.0 --to v0.54.0 --format json > ci-logs-local/drift/drift-v054.json` — same output (Makefile is a thin wrapper).

       Per D-42-A2 the JSON is NOT committed. It exists only for Task 2's curation work.

    4. **Confirm shape and total commit count**:
       ```bash
       jq '.range, .from, .to, .total_unique_commits' ci-logs-local/drift/drift-v054.json
       jq '.by_category' ci-logs-local/drift/drift-v054.json
       jq '.commits | length' ci-logs-local/drift/drift-v054.json  # must match .total_unique_commits
       ```
       Record `total_unique_commits` (expected ~20 per CONTEXT § Drift signal preview, but auditor confirms exact). This is the LOWER BOUND for the ledger's total commit-row count (REQ-UPST5-01 acceptance #1; D-42-B2 close-gate step 2).

    5. **Map each commit to its introducing upstream-tag** (informs cluster grouping in Task 2):
       ```bash
       jq -r '.commits[].sha' ci-logs-local/drift/drift-v054.json | while read sha; do
         tag=$(git describe --tags --contains "$sha" 2>/dev/null | sed 's/[~^].*$//' | head -1)
         echo "$sha $tag"
       done > ci-logs-local/drift/drift-v054-tags.txt
       ```
       Expected: every sha maps to `v0.54.0` (only one minor release spans the range; v0.53.0..v0.54.0 = 1 tag). `--contains` returns the first tag REACHABLE FROM the commit; `sed` strips `~N` / `^N` suffixes.

    6. **Pre-flag windows-touch candidates** (D-42-C2 mechanical pass — informs cluster grouping AND Task 2's row windows-touch column AND ADR review (a) evidence list):
       ```bash
       jq -r '.commits[] | select(
         (.subject | test("windows|wfp|registry|wsa|ntdll|kernel32"; "i"))
         or (.files_changed | map(test("windows|platform\\.rs$|registry\\.rs$|wfp/|win_.*\\.rs$"; "i")) | any)
       ) | "\(.sha) \(.subject)"' ci-logs-local/drift/drift-v054.json > ci-logs-local/drift/drift-v054-wintouch-candidates.txt
       cat ci-logs-local/drift/drift-v054-wintouch-candidates.txt
       ```
       Expected: at minimum the two known commits per CONTEXT § Phase Boundary + § Drift signal preview:
       - `5d821c12 fix(platform): correctly parse windows registry dword values`
       - `0748cced feat(platform): implement robust windows platform detection`
       If MORE commits surface, that's REQUIRED context for Task 2's audit-walk (judgment-override required for ambiguous cases per D-42-C2).

    7. **Verify the D-42-A4 silent-on-post-v0.54.0 invariant** (NO action — verification only):
       ```bash
       # Confirm none of the 4 known post-v0.54.0 commits appear in the audit range
       for sha in 66c69f86 803c6947 fc965ccc 089cf6a0; do
         if jq -e ".commits[] | select(.sha | startswith(\"$sha\"))" ci-logs-local/drift/drift-v054.json > /dev/null 2>&1; then
           echo "WARN: $sha unexpectedly in v0.53.0..v0.54.0 range — INVESTIGATE before continuing"
         else
           echo "ok: $sha not in audit range (deferred to UPST6 per D-42-A4)"
         fi
       done
       ```
       Expected: all 4 shas marked "ok: not in audit range". If any sha surfaces in the range, that's a CONTEXT preview mis-attribution — audit-walk produces the authoritative set; auditor handles the commit per its actual reachability without inventing new D-42-A4 carve-outs.

    8. **Identify Phase-41-touched files for D-42-E2 empirical cross-check sampling** (informs Task 2 § Empirical cross-check subsection):
       ```bash
       # Files that Phase 41 plans (41-01 through 41-10) modified — empirical-cross-check candidate pool
       git log 41-01-PLAN..HEAD --name-only --pretty=format: 2>/dev/null | sort -u | grep -E '^(crates/nono|crates/nono-cli/src|crates/nono-proxy)' | grep -vE '_windows\.rs$|exec_strategy_windows/' | head -20 > ci-logs-local/drift/phase41-touched-files.txt
       cat ci-logs-local/drift/phase41-touched-files.txt
       ```
       Expected: at minimum `crates/nono-cli/src/exec_strategy.rs` (HandleTarget API migration at 14 sites per Plan 41-01), and several other Phase-41-touched files. Task 2 picks >= 3 from this list (representative: 1 from `crates/nono/`, 1 from `crates/nono-cli/`, 1 from `crates/nono-proxy/`).
  </action>
  <verify>
    <automated>jq -e '.total_unique_commits | numbers and . >= 1' ci-logs-local/drift/drift-v054.json &amp;&amp; jq -e '.commits | length == (.total_unique_commits)' ci-logs-local/drift/drift-v054.json &amp;&amp; test "$(jq -r '.range' ci-logs-local/drift/drift-v054.json)" = "v0.53.0..v0.54.0" &amp;&amp; test $(wc -l &lt; ci-logs-local/drift/drift-v054-tags.txt) -ge 1 &amp;&amp; grep -E "5d821c12|0748cced" ci-logs-local/drift/drift-v054-wintouch-candidates.txt &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - `git fetch upstream --tags` exited 0 as FIRST act of Plan 42-01 (D-42-A3 lock timing satisfied)
    - `upstream_head_at_audit` 40-char sha recorded (Task 2 will write this verbatim into ledger frontmatter)
    - `ci-logs-local/drift/drift-v054.json` contains well-formed drift JSON with `range = "v0.53.0..v0.54.0"` and non-zero `total_unique_commits` (expected ~20)
    - `ci-logs-local/drift/drift-v054-tags.txt` maps each sha to `v0.54.0` (single tag spans the range)
    - `ci-logs-local/drift/drift-v054-wintouch-candidates.txt` contains AT MINIMUM `5d821c12` + `0748cced`
    - D-42-A4 invariant verified: none of the 4 known post-v0.54.0 commits (66c69f86, 803c6947, fc965ccc, 089cf6a0) appear in the audit range
    - `ci-logs-local/drift/phase41-touched-files.txt` contains the candidate pool for Task 2 § Empirical cross-check sampling
    - `ci-logs-local/` is in `.gitignore` (verified — scratch outputs never committed per D-42-A2 / D-42-E3)
  </done>
</task>

<task type="auto">
  <name>Task 2: Curate DIVERGENCE-LEDGER.md — frontmatter + clusters (with windows-touch column firing) + ## ADR review with per-cell L/M/H + ## Empirical cross-check subsection + ## Fork-only surface area</name>
  <files>.planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md</files>
  <read_first>
    - .planning/phases/42-upst5-audit/42-CONTEXT.md (full file — every D-42-A1..E10 decision applies)
    - Phase 39 ledger via git: `git show 2f3983f1:.planning/phases/39-upst4-audit/DIVERGENCE-LEDGER.md` (canonical worked example — read in full to internalize cluster section shape, rationale-paragraph style, ## ADR review section narrative shape, ## Fork-only surface area reference shape)
    - Phase 33 ADR `docs/architecture/upstream-parity-strategy.md` § Decision Table (lines 47-53) for the 5-dimension L/M/H format Phase 42 § ADR review will mirror with per-cell verdicts for Option A
    - Phase 33 ADR § Future audit cadence (lines 94-96) for the cadence rule the ## ADR review section narrative cites
    - ci-logs-local/drift/drift-v054.json (Task 1 output — drift JSON to curate)
    - ci-logs-local/drift/drift-v054-tags.txt (Task 1 output — per-commit upstream-tag map)
    - ci-logs-local/drift/drift-v054-wintouch-candidates.txt (Task 1 output — mechanical windows-touch flag set)
    - ci-logs-local/drift/phase41-touched-files.txt (Task 1 output — empirical-cross-check candidate pool)
    - .planning/phases/41-ci-cleanup-v24-broker-code-review-closure/41-01-SUMMARY.md (Plan 41-01 HandleTarget API migration at 14 sites — for D-42-E2 awareness during empirical cross-check)
  </read_first>
  <action>
    Write `.planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md` with the EXACT structure below. Substitute every `<placeholder>` with values from `ci-logs-local/drift/drift-v054.json` + Task 1 scratch outputs + auditor judgment + diff inspection. Do NOT leave placeholder strings in the committed file.

    Use the Write tool with the full assembled content. Do NOT use heredoc / `cat <<EOF`. Expected file size: ~130-180 lines per CONTEXT § Existing Code Insights estimate for ~20 commits / 5-7 clusters.

    **Section A — YAML frontmatter (D-42-A2 locked fields, verbatim):**

    ```markdown
    ---
    slug: divergence-ledger-v053-v054
    status: complete
    type: audit-only
    date: <YYYY-MM-DD: today>
    range: v0.53.0..v0.54.0
    upstream_head_at_audit: <40-char sha from Task 1 step 2>
    drift_tool_sh_sha: 0834aa664fbaf4c5e41af5debece292992211559
    drift_tool_ps1_sha: 0834aa664fbaf4c5e41af5debece292992211559
    drift_tool_invocation: 'make check-upstream-drift ARGS="--from v0.53.0 --to v0.54.0 --format json"'
    fork_baseline: v0.53.0 (Phase 40 UPST4 sync point — 2026-05-14)
    total_unique_commits: <integer from Task 1 step 4>
    ---
    ```

    **Section B — Headline + Reproduction (mirrors Phase 39 shape with v0.53.0..v0.54.0 substitution + first-cycle windows-touch:yes acknowledgment):**

    ```markdown
    # Upstream v0.53.0 → v0.54.0 divergence ledger

    ## Headline

    **<total_unique_commits> non-merge commits across 1 minor release (v0.54.0); ~<additions sum> insertions / ~<deletions sum> deletions across drift-tool categories: <by_category one-liner from drift JSON>.**

    <Cluster-count> themed clusters span the range. <N> disposition `will-sync`; <M> `fork-preserve` (manual replay per D-20 and/or D-42-C3 windows-touch default); <K> `won't-sync`. <X> commits flagged `windows-touch: yes` against the D-42-C2 mechanical heuristic (auditor judgment-override applied where ambiguous) — **first audit cycle where the windows-touch column actually fires.** The two known fires (`5d821c12 fix(platform): correctly parse windows registry dword values` and `0748cced feat(platform): implement robust windows platform detection`) <plus any audit-walk additions> are dispositioned explicitly per ROADMAP success criterion #2; see [§ ADR review](#adr-review) below for the per-cell L/M/H verdicts on Option A `continue`.

    ## Reproduction

    This audit is regenerable from the values in the YAML frontmatter above (D-42-A2 / D-42-E3):

    ```bash
    git fetch upstream --tags
    # Drift-tool script pinned at sha 0834aa664fbaf4c5e41af5debece292992211559 (Phase 24 ship sha; unchanged through Phase 33 + 39 + 42):
    make check-upstream-drift ARGS="--from v0.53.0 --to v0.54.0 --format json"
    # (On Windows hosts where `make` is not on PATH, the Makefile target dispatches to
    #  bash scripts/check-upstream-drift.sh ... — same shell command, same JSON output.)
    ```

    Per D-42-A2 / D-42-E3 the raw JSON output is NOT committed. The cluster tables below are the canonical artifact — the JSON is regenerable on demand from the locked invocation + the upstream HEAD sha + drift-tool script sha recorded in the frontmatter.

    Per D-11 (see [Phase 24 CONTEXT.md](../24-parity-drift-prevention/24-CONTEXT.md) D-11), `*_windows.rs` and `crates/nono-cli/src/exec_strategy_windows/` are EXCLUDED from drift-tool output. The `windows-touch` column on commit rows (D-42-C1) flags upstream commits adding NEW Windows code OUTSIDE the D-11-excluded paths — D-11 is necessary but not sufficient, and **Phase 42 is the first audit cycle where this insufficiency materially fires** (`5d821c12` and `0748cced` touch `crates/nono-cli/src/platform.rs` or upstream's analog — outside D-11's exclusion).

    **Inspection methodology** (mirrors Phase 33 + 39 + D-42-C2 extension): each commit's `subject` + `categories` + `files_changed[]` length was read from the drift JSON for every row; per-commit diffs were read for the lead commit in each cluster, any commit whose subject was ambiguous re: disposition, AND every commit flagged by the D-42-C2 mechanical windows-touch heuristic (the two known + any audit-walk surfaces). The D-42-C2 mechanical pass set `windows-touch: yes` iff: (a) any file in `files_changed` matches `windows` substring or pinned list `{platform.rs, registry.rs, wfp/*, win_*.rs}`, OR (b) commit subject contains `windows / wfp / registry / wsa / ntdll / kernel32` keywords. Auditor judgment-override applied per cluster-lead and per ambiguous-subject commit per D-42-C2; the diff inspection for `5d821c12` and `0748cced` also surveyed fork-side analogs in Phase 31 broker-process + Phase 35/36 supervisor work per D-42-C3.

    **D-42-A4 strictly-silent-on-post-v0.54.0 invariant honored:** four known post-v0.54.0 commits (`66c69f86 fix(snapshot): validate restore targets against symlinks`, `803c6947 chore(deps): bump nix`, `fc965ccc chore(deps): bump tokio`, `089cf6a0 chore(deps): bump cosign-installer`) are explicitly out of scope and deferred to UPST6 per CONTEXT § Phase Boundary; this ledger does not disposition them.
    ```

    **Section C — Cluster Summary table (same 5-column shape as Phase 33 / 39 — NO windows-touch column at summary level; windows-touch lives in per-row tables only):**

    ```markdown
    ## Cluster Summary

    | # | Cluster (introduced in) | Commit count | Disposition | One-line summary |
    |---|-------------------------|--------------|-------------|------------------|
    | 1 | <cluster 1 name> (v0.54.0) | <N> | `<disposition>` | <one-line summary> |
    | ... | ... | ... | ... | ... |
    ```

    Aim for 5-7 clusters total per CONTEXT § Existing Code Insights size estimate (~20 commits / 5-7 clusters).

    **Section D — Per-cluster sections (D-42-E5 two-tier; D-42-C1 / D-42-E6 row schema with windows-touch column):**

    For EACH cluster identified in audit-walk, emit:

    ```markdown
    ### Cluster: <cluster name> (introduced in v0.54.0)

    - **Disposition:** <one of: will-sync | fork-preserve | won't-sync>
    - **Rationale:** <2-4 sentences. For windows-touch:yes clusters, EXPLICITLY note the D-42-C3 default AND the fork-side analog check (e.g., "Per D-42-C3 default applied: this cluster contains upstream commits `5d821c12` and `0748cced` adding new Windows-conditional platform-detection code in `crates/nono-cli/src/platform.rs` (D-11 does NOT exclude). Fork-side analog check: surveyed Phase 31 broker-process work (`crates/nono-shell-broker/`) and Phase 35/36 supervisor work (`exec_strategy_windows/`) — fork has <empty | its own implementation in <files>>. Disposition: <fork-preserve | will-sync> with <D-20 manual-replay rationale | direct cherry-pick rationale>."). For will-sync (windows-touch:no) clusters, cite what the cluster closes / why it composes with fork (Phase 39 will-sync rationale style). For won't-sync, be specific not generic (cite D-11 / D-19 / D-21 as applicable).>
    - **Target phase:** <UPST5-sync (Phase 43) for will-sync and fork-preserve; — (n/a) for won't-sync>
    - **Wave-hint:** <OPTIONAL per D-42-B3 + CONTEXT § Claude's Discretion — auditor may add `foundation` for the largest/most-foundational cluster (e.g., the Rust edition 2024 + workspace dependency centralization cluster `8b888a1c` per CONTEXT § Drift signal preview is a foundation candidate), or `depends-on cluster-N final state`. Omit for clusters where wave shape is uninteresting. Phase 43 retains full discretion to refine.>

    | sha | subject | upstream-tag | categories | files-changed | windows-touch |
    |-----|---------|--------------|------------|---------------|---------------|
    | <sha-short-7> | <subject> | <v0.54.0> | <comma-separated categories from drift JSON .commits[].categories[]> | <length of .commits[].files_changed[]> | <yes or no per D-42-C2 + judgment override> |
    | ... | ... | ... | ... | ... | ... |
    ```

    **Disposition rationale invariant prompts (REQ-UPST5-01 acceptance #2):**
    - **will-sync addendum:** if the cluster's surface intersects D-19 byte-identical / D-20 manual-replay / D-21 library-mutation risk, cite the relevant decision ID inline.
    - **fork-preserve addendum:** cite D-20 manual-replay rationale + the fork-only files that cherry-pick would overwrite.
    - **windows-touch:yes cluster addendum:** explicit fork-side analog check + D-42-C3 default upheld/upgraded with rationale.

    **Cluster grouping heuristic (CONTEXT § Claude's Discretion + § Drift signal preview hints):**
    - 5-7 clusters per CONTEXT § Existing Code Insights size estimate.
    - Likely themes per CONTEXT § Drift signal preview (auditor confirms during audit-walk):
      - **Pack management (nono update + pinning + outdated)** — ~5-7 commits (`42601ed7`, `98c18f1f`, `18b03fa6`, `5098fc10`, `a5985edd`, `64d9f283`); likely will-sync; new CLI surface.
      - **Rust edition 2024 + workspace dependency centralization** (`8b888a1c`) — likely foundation cluster (wave-hint: foundation candidate); cross-platform impact.
      - **Snapshot symlink validation fix** (`66c69f86`) — VERIFY at audit-walk whether reachability is pre-v0.54.0 (in scope) or post-v0.54.0 (UPST6 per D-42-A4); CONTEXT § Drift signal preview flags this as ambiguous.
      - **Platform-conditional profile fields** (`ce06bd59`) — could intersect fork's Phase 22 `unsafe_macos_seatbelt_rules` + Phase 36 canonical sections.
      - **Windows platform detection** (`0748cced` + `5d821c12`) — TWO commits adding new Windows code outside D-11; **D-42-C3 fork-preserve default applies; explicit per-commit disposition required for success criterion #2.**
      - **macOS lint fixes** (3 commits — `548bb800`, `021074c9`, `ff2d8b84`) — likely will-sync; trivial absorption if they touch fork-shared paths.
      - **Release v0.54.0** (`6b00932f`) — Phase 33 / 39 release-ride convention; CHANGELOG-only absorption (fork tracks own version).
    - Each commit MUST appear in exactly ONE cluster — no orphans, no duplicates. Total commit-row count MUST equal `total_unique_commits`.

    **Section E — `## ADR review` section (D-42-C4 UPGRADE — per-cell L/M/H verdict table; NEW vs Phase 39 prose-only):**

    Place AFTER all cluster sections and BEFORE `## Empirical cross-check`:

    ```markdown
    ## ADR review

    The Phase 33 strategic ADR (`docs/architecture/upstream-parity-strategy.md`, `Status: Accepted` 2026-05-11, re-confirmed at v2.4 close per D-39-C4) chose Option A `continue`. **Phase 42 is the first audit cycle where empirical `windows-touch: yes` evidence is available** (Phase 33's v0.40.1..v0.52.0 range and Phase 39's v0.52.0..v0.53.0 range had ZERO windows-touch:yes commits). Per D-42-C4 upgrade, this section ships explicit per-cell L/M/H verdicts for the 5 dimensions enumerated in the Phase 33 ADR Decision Table.

    ### Per-cell L/M/H verdict — Option A `continue` at v0.53.0..v0.54.0

    | Dimension | Verdict | Rationale (empirical evidence from this audit cycle) |
    |-----------|---------|------------------------------------------------------|
    | security posture | <L | M | H> | <Reflect empirical evidence: Windows-touching upstream commits (5d821c12 + 0748cced) flowed into the audit signal correctly; fork-side Windows hardening continues to evolve alongside upstream's threat model. If 5d821c12 + 0748cced are dispositioned fork-preserve, security posture remains High because conservative default protects D-11 invariant. If upgraded to will-sync after diff inspection, also High because upstream Windows hardening absorbs into fork.> |
    | windows parity | <L | M | H> | <Reflect single-CLI-surface preservation: continued bidirectional parity means the windows-touch:yes commits get dispositioned in-fork rather than splitting into a separate repo. Per CONTEXT § Phase Boundary: D-42-C3 conservative default protects parity by defaulting fork-preserve; auditor's fork-side analog check (Phase 31 + 35/36 work surveyed) confirms decisions are made with full fork-side context.> |
    | maintenance cost | <L | M | H> | <Reflect per-sync labor: ~20 commits / 5-7 clusters absorbed in one cycle is sustainable (vs Phase 33's 97 commits / 12 clusters at v0.40.1..v0.52.0). Number of fork-preserve clusters affects manual-replay labor — if the two known windows-touch:yes commits land in 1 fork-preserve cluster, manual-replay labor is contained; if more emerge, labor grows linearly.> |
    | divergence risk | <L | M | H> | <Reflect drift accumulation: at v0.53.0..v0.54.0 the per-cycle commit count is small (~20 vs Phase 33's 97); cadence rule firing per upstream release means accumulated drift stays bounded. Higher windows-touch:yes ratio in future cycles would push divergence risk higher if cherry-pick conflicts compound.> |
    | contributor velocity | <L | M | H> | <Reflect drift-audit + cherry-pick gate review burden: Phase 42 sized at ~1 week (1 plan, ~20 commits) — bounded. The Phase 41 close-gate baseline (CI SHA 13cc0628 with all 5 Windows lanes green) means Phase 43's baseline-aware CI gate is a real regression detector, not baseline-drift noise — drift-audit burden is contained.> |

    ### Verdict outcome

    <Pick ONE of three outcomes per D-42-C4>:

    - **(a) Confirm Option A `continue`** <if windows-touch:yes clusters dispositioned cleanly via D-42-C3 conservative-default and per-cell L/M/H shape dominates per Phase 33 ADR scoring methodology — likely outcome>. Per-cell shape: <e.g., (H, H, M, M, M) — 2 High / 3 Med / 0 Low — dominates Phase 33's reference shape for Options B and C unchanged>. The Phase 33 ADR `Status: Accepted` remains in force; Phase 42 does NOT supersede.

    OR

    - **(b) Amend Option A** <if specific carve-outs surfaced from windows-touch:yes diff inspection — e.g., "Windows-platform-detection commits default to D-20 manual-replay, not cherry-pick">. Carve-out details: <specifics>. The Phase 33 ADR `Status: Accepted` remains in force; Phase 42 does NOT supersede but records the carve-out for Phase 43 plan-phase reference.

    OR

    - **(c) Flag a future-supersede trigger** <if evidence suggests Option B `split` or Option C `freeze` should be re-evaluated in UPST6/UPST7>. Trigger threshold: <e.g., "if 50%+ clusters become fork-preserve in a future cycle, UPST6 plan-phase invokes a Superseding ADR phase">. The Phase 33 ADR `Status: Accepted` remains in force; Phase 42 records the trigger but does NOT supersede.

    Per the Phase 33 ADR § Future audit cadence rule (D-42-E8): "per upstream release, lazily-evaluated" — UPST6 fires when v0.55.0+ ships or maintainer decides accumulated cherry-pick labor warrants firing.
    ```

    **Section F — `## Empirical cross-check` subsection (D-42-E1 / D-42-E2 — NEW; ROADMAP success criterion #4):**

    Place AFTER `## ADR review` and BEFORE `## Fork-only surface area`:

    ```markdown
    ## Empirical cross-check

    Per D-42-E1, the audit walk spot-checks >= 3 fork-shared files for any upstream path the drift tool missed (Phase 39 empirical-cross-check pattern). Phase 42 preferentially samples Phase-41-touched files per D-42-E2 since those are the files most likely to have drifted from upstream in ways the drift tool's mechanical path filter (D-11) may not catch.

    Methodology: for each sampled file, run `git log v0.53.0..v0.54.0 -- <file>` against `upstream/main` and confirm the drift tool's commit list covers every upstream commit touching that file.

    | # | Sampled file | Subsystem | Phase-41 nexus | Upstream commits in range | Drift-tool covered | Finding |
    |---|--------------|-----------|----------------|---------------------------|--------------------|---------|
    | 1 | <file 1, e.g., crates/nono-cli/src/exec_strategy.rs> | nono-cli | Plan 41-01 HandleTarget API migration at 14 sites | <N upstream commits found via git log> | <yes \| no — list missed shas if any> | <confirmed \| drift-tool-missed-X \| other-anomaly> |
    | 2 | <file 2, e.g., crates/nono/src/sandbox/linux.rs OR crates/nono-shell-broker/src/lib.rs> | nono OR nono-shell-broker | Plan 41-06/07 broker hygiene | <N> | <yes \| no> | <finding> |
    | 3 | <file 3, e.g., crates/nono-proxy/src/server.rs OR crates/nono-cli/src/policy.rs> | nono-proxy OR nono-cli | <Phase 41 nexus or representative cross-platform pick> | <N> | <yes \| no> | <finding> |

    **Findings summary:** <If all 3 files PASS (drift tool covered every upstream commit), state "All 3 sampled files PASS; drift tool's commit list is complete against the v0.53.0..v0.54.0 fork-shared surface for the sampled subsystems." If any file surfaces a missed commit, document the miss and spawn a `.planning/quick/YYMMDD-xxx-upstream-drift-tool-fix/` follow-up per D-42-D3 — Phase 42 ledger stays untouched to preserve drift_tool_sh_sha reproducibility.>
    ```

    Auditor selects the 3 files at audit-walk from `ci-logs-local/drift/phase41-touched-files.txt` (Task 1 step 8 output) — recommended picks per D-42-E2: `crates/nono-cli/src/exec_strategy.rs` (Plan 41-01 HandleTarget), a broker null-handle path file from `crates/nono-shell-broker/` (Plan 41-06/07), and one cross-platform file from `crates/nono-proxy/` or `crates/nono-cli/src/policy.rs`.

    **Section G — `## Fork-only surface area` section (D-42-E4 carry-forward; CONTEXT § Claude's Discretion):**

    Two options per CONTEXT § Claude's Discretion ("whether to capture a `Fork-only surface area` delta section"):

    - **Option G.1 (terse — recommended if Phase 41 introduced no new fork-only Windows surface):** Single paragraph referencing Phase 33 + 39:
      ```markdown
      ## Fork-only surface area

      Surface added since v0.40.1 with NO upstream analog. The drift tool's D-11 filter (`*_windows.rs` + `crates/nono-cli/src/exec_strategy_windows/` excluded) hides ALL of this from the audit walk. Phase 42 references Phase 33's enumeration unchanged — Phase 41 introduced no new fork-only Windows surface (Plan 41-01 was a cross-platform API migration; Plans 41-06/07 hardened existing broker CR paths; no new `*_windows.rs` files added).

      See [Phase 33 ledger § Fork-only surface area](../33-windows-parity-upstream-0-52-divergence/DIVERGENCE-LEDGER.md#fork-only-surface-area) for the full enumeration.
      ```

    - **Option G.2 (full — only if Phase 41 DID introduce new fork-only Windows surface):** Add a `### Delta-since-Phase-39` subsection enumerating only the NEW seams (e.g., broker CR-02/03 null-handle + empty-list path semantics; HandleTarget API migration at 14 sites if it introduced new Windows-conditional code).

    Pick at write-time based on Plan 41-01/06/07 SUMMARYs (audit-walk confirms; Plan 41-06/07 hardened existing broker paths without adding new files — Option G.1 is likely correct).

    **CRITICAL — coverage + falsifiability invariants before commit (D-42-B2 close-gate steps 2, 3, 4, 5):**

    Verify before writing:
    1. `<total cluster commit-rows>` >= `<total_unique_commits>` from drift JSON
    2. Every cluster header has all 3 required bullets: `**Disposition:**`, `**Rationale:**`, `**Target phase:**` (`**Wave-hint:**` is OPTIONAL per D-42-B3)
    3. Every disposition is one of `will-sync` / `fork-preserve` / `won't-sync` — exactly
    4. `## ADR review` section header present, grep-discoverable
    5. ADR review § Per-cell L/M/H verdict table has all 5 dimensions: `security posture`, `windows parity`, `maintenance cost`, `divergence risk`, `contributor velocity` (verbatim lowercase as table-cell labels)
    6. ADR review outcome explicitly chosen: one of (a) confirm | (b) amend | (c) flag-future-supersede
    7. `## Empirical cross-check` section header present, grep-discoverable; >= 3 file rows in the table
    8. Every commit-row has `yes` or `no` in the windows-touch column — no blank cells
    9. The two known windows-touch:yes commits `5d821c12` + `0748cced` carry `windows-touch: yes` AND have explicit per-commit disposition + rationale (either inline at the row, in the cluster rationale paragraph, or in a dedicated 2-row cluster — Phase 33/39 precedent allows either shape)
    10. NO references to the 4 known post-v0.54.0 commits (`66c69f86`, `803c6947`, `fc965ccc`, `089cf6a0`) anywhere except possibly in the Fork-only-surface or methodology note acknowledging they are deferred to UPST6 per D-42-A4
  </action>
  <verify>
    <automated>test -f .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md &amp;&amp; grep -E "^drift_tool_invocation: 'make check-upstream-drift ARGS=\"--from v0.53.0 --to v0.54.0 --format json\"'$" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md &amp;&amp; grep -E "^upstream_head_at_audit: [0-9a-f]{40}$" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md &amp;&amp; grep -E "^drift_tool_sh_sha: 0834aa664fbaf4c5e41af5debece292992211559$" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md &amp;&amp; grep -E "^range: v0.53.0..v0.54.0$" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md &amp;&amp; grep -E "^fork_baseline: v0.53.0 \(Phase 40 UPST4 sync point" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md &amp;&amp; test $(grep -cE "^### Cluster: " .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md) -ge 3 &amp;&amp; test $(grep -cE "^- \*\*Disposition:\*\* \`?(will-sync|fork-preserve|won't-sync)\`?" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md) -ge 3 &amp;&amp; test $(grep -c "^## ADR review$" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md) -eq 1 &amp;&amp; test $(grep -cE "^\| (security|windows|maintenance|divergence|contributor)" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md) -ge 5 &amp;&amp; test $(grep -c "^## Empirical cross-check" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md) -eq 1 &amp;&amp; grep -E "5d821c12" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md &amp;&amp; grep -E "0748cced" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md &amp;&amp; grep -E "windows-touch" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md &amp;&amp; grep -E "^## Fork-only surface area$" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - File exists at `.planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md`
    - YAML frontmatter has all D-42-A2 fields populated with real (non-placeholder) values; drift_tool shas are the locked Phase 24 ship shas; range is `v0.53.0..v0.54.0`; fork_baseline cites `v0.53.0 (Phase 40 UPST4 sync point — 2026-05-14)` verbatim
    - Cluster Summary table present with one row per cluster, one of three dispositions per row, NO windows-touch column at the summary level
    - Per-cluster sections: each has `### Cluster: <name> (introduced in v0.54.0)` header + 3 required bullets (Disposition, Rationale, Target phase) + optional `**Wave-hint:**` bullet, then a commit-row table
    - Every cluster's commit-row table follows the D-42-C1 schema (6 columns: sha + subject + upstream-tag + categories + files-changed + windows-touch); every row has `yes` or `no` in the windows-touch column, no blanks
    - Total commit-row count across all clusters EQUALS or EXCEEDS `total_unique_commits` from drift JSON
    - `## ADR review` section present, grep-falsifiable (`grep -c "^## ADR review$"` returns 1)
    - ADR review Per-cell L/M/H verdict table has all 5 dimensions present as table rows (`security posture`, `windows parity`, `maintenance cost`, `divergence risk`, `contributor velocity`) — falsifiable via `grep -cE "^\| (security|windows|maintenance|divergence|contributor)"` returning >= 5
    - ADR review verdict outcome explicitly chosen: one of (a) | (b) | (c)
    - `## Empirical cross-check` section present with >= 3 sampled files in the cross-check table, preferentially Phase-41-touched
    - `## Fork-only surface area` section present (Option G.1 terse-reference OR G.2 full-enumeration per Phase 41 audit-walk)
    - The two known windows-touch:yes commits (`5d821c12` + `0748cced`) appear with `windows-touch: yes` AND explicit per-commit disposition + rationale (success criterion #2 enforcement)
    - D-42-A4 invariant honored: no commit-row references the 4 known post-v0.54.0 commits as in-scope
  </done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md — flip Phase 42 v2.5 entry to complete + append UPST6 stub at D-42-B4-chosen location</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (full file — must understand current v2.5 milestone block at lines 15-21, Phase Details Phase 42 block at lines 71-82, and EOF position for appending UPST6 stub)
    - .planning/phases/42-upst5-audit/42-CONTEXT.md § D-42-B4 (UPST6 stub fields locked: title, depends-on, plans, target section options (a)/(b)/(c) with recommendation)
    - .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md (Task 2 output — auditor confirms title-wording discretion: `… sync audit` default vs `… sync execution` if zero windows-touch + zero fork-preserve)
    - docs/architecture/upstream-parity-strategy.md § Future audit cadence (Phase 33 ADR cadence rule — referenced in UPST6 stub's `**Reference:**` line)
  </read_first>
  <action>
    Make THREE distinct edits to `.planning/ROADMAP.md`. Use the Edit tool for each edit (3 separate Edit calls; each touches a distinct hunk). Do NOT rewrite the entire file via Write.

    **Edit 1 — Flip Phase 42 v2.5 milestone-block entry (currently at ROADMAP line 19):**

    Current line:
    ```markdown
    - [ ] **Phase 42: UPST5 audit** — DIVERGENCE-LEDGER.md inventory of upstream `v0.53.0..+`; first audit where the `windows-touch` column actually fires.
    ```

    Replace `- [ ]` with `- [x]` and append ` (completed YYYY-MM-DD)` (today's date) at the end of the line, before the period that ends it. Final shape:
    ```markdown
    - [x] **Phase 42: UPST5 audit** — DIVERGENCE-LEDGER.md inventory of upstream `v0.53.0..+`; first audit where the `windows-touch` column actually fires. (completed YYYY-MM-DD)
    ```

    **Edit 2 — Flip Phase Details > Phase 42 block Plans line (currently at line 81):**

    Current Plans line:
    ```markdown
    **Plans**: TBD (Phase 33 / 39 plan shape carries forward)
    ```

    Replace with:
    ```markdown
    **Plans**: 1 / 1 plans complete
    - [x] 42-01-PLAN.md — REQ-UPST5-01 (DIVERGENCE-LEDGER.md curated for v0.53.0..v0.54.0 with `windows-touch` column firing for 5d821c12 + 0748cced; `## ADR review` section with per-cell L/M/H verdicts on Option A `continue` (outcome: <a confirm | b amend | c flag-future-supersede>); `## Empirical cross-check` covering 3 fork-shared files; UPST6 stub queued at <D-42-B4 location>)
    ```

    **Edit 3 — Append UPST6 stub at D-42-B4-chosen location:**

    Auditor picks ONE of three locations per D-42-B4:

    - **Option (a) — v2.6 backlog stub:** If `## v2.6 backlog` section does not already exist, append it at end-of-file. Then add the UPST6 stub under it. RECOMMENDED if v2.6 scope locked by Plan 42-01 close.

    - **Option (b) — v2.5 § Future Cycles holding section (RECOMMENDED FALLBACK per CONTEXT § D-42-B4):** If `## Future Cycles` section does not already exist, append it at end-of-file with a one-paragraph intro per the Phase 33 ADR cadence rule. Then add the UPST6 stub under it.

    - **Option (c) — `.planning/backlog/upst6-stub.md`:** Only if (a) + (b) infeasible. Creates a NEW file outside ROADMAP. NOT recommended — breaks Phase 39's ROADMAP-resident precedent.

    **Recommended path (Option b — v2.5 § Future Cycles holding section):**

    Append the following block at end-of-file (after the Phase 43 Detail block ends, after the Requirement Coverage table or wherever EOF lands):

    ```markdown

    ## Future Cycles

    Entries queued under v2.5 § Future Cycles per the Phase 33 ADR `### Future audit cadence` rule — "per upstream release, lazily-evaluated". They activate when v2.6 scope locks; until then they live here as forward-cadence anchors.

    ### Phase TBD-NN: UPST6 — Upstream v0.54.0…+ <sync audit | sync execution>

    **Goal:** Mirror Phase 33 / Phase 39 / Phase 42 audit shape. Inventory of upstream divergence from v0.54.0 forward (commits accumulated post-Phase 42 audit cutoff `6b00932f`, including v0.55.0+ tags as they ship). Per-cluster disposition + parity-strategy review against Phase 33 ADR; absorbs the 4 known post-v0.54.0 commits (`66c69f86 fix(snapshot): validate restore targets against symlinks`, `803c6947 chore(deps): bump nix`, `fc965ccc chore(deps): bump tokio`, `089cf6a0 chore(deps): bump cosign-installer`) plus any subsequent additions.

    **Depends on:** Phase 43 (UPST5 execution baseline lands fork at v0.54.0).

    **Requirements:** TBD when v2.6 scope locks (or v2.5 § Future Cycles activates).

    **Plans:** 0 / TBD — to be populated during `/gsd-plan-phase TBD-NN`.

    **Estimated effort:** ~1 week (mirrors Phase 39 + Phase 42 sizing).

    **Reference:** `.planning/phases/33-windows-parity-upstream-0-52-divergence/` (audit-shape root template), `.planning/phases/39-upst4-audit/` (windows-touch column zero-fire example via git archive), `.planning/phases/42-upst5-audit/` (Phase 42 worked example with windows-touch:yes fires for 5d821c12 + 0748cced + per-cell L/M/H ADR verdict table + empirical cross-check subsection), `docs/architecture/upstream-parity-strategy.md` § Future audit cadence (Phase 33 ADR cadence rule).
    ```

    **Title-wording discretion (CONTEXT § Claude's Discretion):** Pick `… sync audit` (DEFAULT — safer; expected given 5d821c12 + 0748cced are windows-touch:yes ⇒ D-42-C3 fork-preserve default ⇒ next cycle benefits from a separate audit) vs `… sync execution` (ONLY if Phase 42 ledger shows zero windows-touch:yes AND zero fork-preserve clusters — unlikely). Read the ledger written in Task 2 BEFORE writing this line. Replace `<sync audit | sync execution>` with the chosen single phrase; remove angle brackets.

    **CRITICAL — D-42-B4 location decision recording:** In Edit 2's Plans-line text, replace `<D-42-B4 location>` with the literal "v2.6 backlog" OR "v2.5 § Future Cycles" OR "`.planning/backlog/upst6-stub.md`" matching the chosen Option (a)/(b)/(c).

    Use the Edit tool for each of the three edits (3 separate Edit calls).
  </action>
  <verify>
    <automated>grep -E "^- \[x\] \*\*Phase 42: UPST5 audit\*\*.*\(completed [0-9]{4}-[0-9]{2}-[0-9]{2}\)" .planning/ROADMAP.md &amp;&amp; grep -E "^\*\*Plans\*\*: 1 / 1 plans complete" .planning/ROADMAP.md &amp;&amp; grep -E "^- \[x\] 42-01-PLAN\.md" .planning/ROADMAP.md &amp;&amp; grep -E "(^## v2.6 backlog$|^## Future Cycles$)" .planning/ROADMAP.md &amp;&amp; grep -E "^### Phase TBD-NN: UPST6 — Upstream v0.54.0" .planning/ROADMAP.md &amp;&amp; grep -E "^\*\*Depends on:\*\* Phase 43 " .planning/ROADMAP.md &amp;&amp; grep -E "^\*\*Plans:\*\* 0 / TBD" .planning/ROADMAP.md &amp;&amp; grep -E "Future audit cadence" .planning/ROADMAP.md &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - Phase 42 v2.5 milestone-block entry flipped `[ ]` → `[x]` with `(completed YYYY-MM-DD)` appended
    - Phase Details > Phase 42 block Plans line flipped to `**Plans**: 1 / 1 plans complete` with `[x] 42-01-PLAN.md` sub-bullet citing REQ-UPST5-01 + ADR review outcome + UPST6 stub location
    - UPST6 stub appended at D-42-B4-chosen location: either `## v2.6 backlog` (option a) OR `## Future Cycles` (option b — recommended) OR `.planning/backlog/upst6-stub.md` (option c)
    - UPST6 stub has all 6 required fields (Goal, Depends on: Phase 43, Requirements: TBD, Plans: 0 / TBD, Estimated effort, Reference)
    - UPST6 stub `**Reference:**` line cites Phase 33 + 39 + 42 ledgers + ADR § Future audit cadence
    - Title-wording chosen at write-time per Phase 42 ledger shape (most likely `… sync audit`; angle brackets removed)
  </done>
</task>

<task type="auto">
  <name>Task 4: Update STATE.md + write 42-01-SUMMARY.md + self-audit (8 D-42-B2 close-gate checks) + commit</name>
  <files>
    - .planning/STATE.md
    - .planning/phases/42-upst5-audit/42-01-SUMMARY.md
  </files>
  <read_first>
    - .planning/STATE.md (full file — frontmatter at lines 1-13, Current Position at lines 26-32, v2.5 phase queue table at lines 34-42, Accumulated Context > Key Decisions section for the Plan 42-01 close entry insertion point)
    - .planning/phases/42-upst5-audit/42-CONTEXT.md § D-42-B2 (8-step close-gate checks)
    - Phase 33 ledger SUMMARY shape via git: `git log --oneline -- '*33*SUMMARY*'` to find shape reference
    - Phase 39 ledger SUMMARY shape via git: `git show 7ba35eec --stat` then `git show 7ba35eec:.planning/phases/39-upst4-audit/39-01-SUMMARY.md`
    - .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md (Task 2 output — read cluster counts, disposition breakdown, windows-touch:yes count, ADR review outcome, empirical cross-check findings for the SUMMARY + STATE close entry)
    - .planning/ROADMAP.md (Task 3 output — verify Phase 42 entry is `[x]`; record UPST6 stub location for STATE close entry)
    - .planning/templates/cross-target-verify-checklist.md (Phase 41 Class F template; Phase 42 trivially honors — zero `.rs` files; SUMMARY § Cross-target-clippy section cites N/A)
  </read_first>
  <action>
    Four sub-tasks, sequenced:

    **Sub-task A — Update STATE.md (3 distinct edits via Edit tool):**

    *Edit A.1 — Frontmatter counter bump (lines 1-13):*

    Current frontmatter:
    ```yaml
    ---
    gsd_state_version: 1.0
    milestone: v2.5
    milestone_name: Backlog Drain + UPST5
    status: executing
    last_updated: "2026-05-17T19:02:51.934Z"
    last_activity: 2026-05-17
    progress:
      total_phases: 4
      completed_phases: 1
      total_plans: 10
      completed_plans: 11
      percent: 100
    ---
    ```

    Bumps required:
    - `last_updated`: stamp to current UTC ISO-8601 timestamp
    - `last_activity`: today's date YYYY-MM-DD
    - `progress.completed_phases`: 1 → 2 (Phase 42 complete)
    - `progress.total_plans`: 10 → 11 (Phase 42 added Plan 42-01) — VERIFY current value before editing; the L11 value was 10 at context-capture time
    - `progress.completed_plans`: 11 → 12 — VERIFY current value before editing; L12 was 11 (Phase 41 had 10 plans of which all 10 complete; current state shows 11 which suggests Phase 41 close included one extra accounting entry — auditor confirms by counting actual completed plans in the v2.5 phase queue)
    - `progress.percent`: recompute as (completed_plans / total_plans * 100) — if both bumped equally, stays 100

    Note: The frontmatter counter math may need reconciliation. The CURRENT state at L1-13 shows completed_plans=11 > total_plans=10 which is inconsistent. Phase 41 closed with 10 plans (41-01 through 41-10) per ROADMAP lines 51-68 — the frontmatter accounting drift is pre-existing and auditor MAY normalize during this edit (e.g., total_plans=10, completed_plans=10 BEFORE Phase 42 → total_plans=11, completed_plans=11 AFTER Phase 42), but is NOT required to. Document any normalization in the STATE Plan-42-01 close entry.

    *Edit A.2 — Current Position block (lines 26-32):*

    Current:
    ```markdown
    Phase: 41 (ci-cleanup-v24-broker-code-review-closure) — COMPLETE
    Plan: 10 of 10 (Plan 41-10 complete — final gap closure for 5 CI failure classes from CI run 25973911653 + Class F verifier-protocol gap)
    Status: Phase 41 complete; awaiting orchestrator push to CI
    Resume file: .planning/phases/42-upst5-audit/42-CONTEXT.md
    Last activity: 2026-05-17 -- Plan 41-10 execution complete (5 commits 97b51249..306d9fd5)
    ```

    Replace with:
    ```markdown
    Phase: 42 (upst5-audit) — EXECUTING
    Plan: 1 of 1
    Status: Phase complete — ready for verification
    Resume file: (Phase 42 close; next: /gsd-verify-work 42 OR /gsd-plan-phase 43)
    Last activity: YYYY-MM-DD -- Plan 42-01 execution complete (DIVERGENCE-LEDGER for v0.53.0..v0.54.0; windows-touch:yes column fired for 5d821c12 + 0748cced; ADR review verdict: <a | b | c>)
    ```

    *Edit A.3 — Plan 42-01 close entry under `### Key Decisions` (find appropriate v2.5 sub-section or create one if not present):*

    Insert a new single-paragraph entry at the TOP of the most recent v2.5 Key Decisions block (just under the heading, reverse-chronological order — Plan 41-10 close entry should be the previous-newest entry). Shape (single paragraph mirroring Phase 33 Plan 33-01 close entry + Phase 39 Plan 39-01 close entry):

    ```markdown
    - **Phase 42 Plan 42-01 (REQ-UPST5-01) — DIVERGENCE-LEDGER.md curated for v0.53.0..v0.54.0:** Wave 1 ledger curation completed YYYY-MM-DD. Drift-tool re-run (D-42-A1 locked invocation `make check-upstream-drift ARGS="--from v0.53.0 --to v0.54.0 --format json"`, drift-tool sha `0834aa664fbaf4c5e41af5debece292992211559` unchanged since Phase 24 ship 2026-04-29 through Phase 33 + 39 + 42, dispatched on Windows host via `bash scripts/check-upstream-drift.sh` per Phase 33 33-01-SUMMARY Rule 3 deviation precedent) produces <total_unique_commits> unique commits across 1 minor release (v0.54.0). `upstream_head_at_audit` locked at first commit of Plan 42-01 per D-42-A3 (40-char sha `<sha>`). Curated <N> themed clusters: per-cluster dispositions <W will-sync> + <F fork-preserve> + <K won't-sync>. **First audit cycle with windows-touch:yes fires:** <X> commits flagged windows-touch:yes against the D-42-C2 mechanical heuristic + judgment-override pass; the two known windows-touch:yes commits (`5d821c12` Windows registry dword parse + `0748cced` robust Windows platform detection) carry the flag with explicit per-commit dispositions <plus any additional commits the audit-walk surfaced>. Per D-42-C3 conservative default, all windows-touch:yes clusters assigned `fork-preserve` disposition <unless / except those upgraded after diff inspection + fork-side analog check>. **D-42-C4 upgrade applied: `## ADR review` section ships per-cell L/M/H verdict table for 5 dimensions (security posture / windows parity / maintenance cost / divergence risk / contributor velocity) on Option A `continue`** — verdict outcome: <(a) confirm | (b) amend with carve-outs <specifics> | (c) flag future-supersede trigger <specifics>>. Phase 33 ADR remains `Accepted`; Phase 42 does NOT supersede. **D-42-E1 § Empirical cross-check section ships 3 sampled Phase-41-touched files** (representative: <file 1>, <file 2>, <file 3>) — findings: <all-PASS | drift-tool-missed-X with spawn `.planning/quick/...` per D-42-D3>. UPST6 stub queued at <v2.6 backlog | v2.5 § Future Cycles | `.planning/backlog/upst6-stub.md`> per D-42-B4 with title `UPST6 — Upstream v0.54.0…+ <sync audit | sync execution>`, Depends on: Phase 43, Plans: 0 / TBD. **Validation (all 8 D-42-B2 close-gate checks pass):** (1) drift-tool re-run idempotent (exit 0), (2) ledger row count <N> >= drift-tool flagged count <total_unique_commits>, (3) every cluster has disposition + rationale, (4) `## ADR review` section grep-confirmable with per-cell L/M/H table (5+ dimension rows), (5) `## Empirical cross-check` >= 3 file rows, (6) ROADMAP UPST6 stub committed, (7) STATE.md updated (this entry), (8) `git diff --name-only HEAD~3..HEAD -- crates/ bindings/ scripts/ | wc -l` == 0 (D-42-E7 + D-42-B2 step 8 trivially honored — Phase 42 ships zero .rs / .toml / .sh / .ps1 / Makefile edits). **D-42-A4 strictly-silent-on-post-v0.54.0 invariant honored:** the 4 known post-v0.54.0 commits (66c69f86 snapshot symlink validation, 803c6947 nix bump, fc965ccc tokio bump, 089cf6a0 cosign-installer bump) are explicitly deferred to UPST6; not dispositioned in Phase 42 ledger. Commits: `<commit-A-sha>` (DIVERGENCE-LEDGER.md) + `<commit-B-sha>` (ROADMAP + STATE.md) + `<commit-C-sha>` (42-01-SUMMARY.md). DCO sign-offs in all commits. Phase 42 closes: 1/1 plan executed, REQ-UPST5-01 landed, ready for `/gsd-verify-work 42` verifier pass.
    ```

    Auditor fills `<placeholders>` from the ledger written in Task 2 + ROADMAP edits from Task 3. Commit shas filled AFTER the commits land.

    **Sub-task B — Write 42-01-SUMMARY.md:**

    Use the Write tool to create `.planning/phases/42-upst5-audit/42-01-SUMMARY.md`. Mirror Phase 39 Plan 39-01-SUMMARY.md shape (read via `git show 7ba35eec:.planning/phases/39-upst4-audit/39-01-SUMMARY.md` for structure). Required sections:

    ```markdown
    ---
    phase: 42-upst5-audit
    plan: 01
    status: complete
    requirements: [REQ-UPST5-01]
    commits: [<A-sha>, <B-sha>, <C-sha>]
    date: YYYY-MM-DD
    provides:
      - DIVERGENCE-LEDGER.md artifact for v0.53.0..v0.54.0 (~<N> commits / <M> clusters)
      - per-cluster dispositions: <W> will-sync + <F> fork-preserve + <K> won't-sync
      - <X> windows-touch:yes commits dispositioned (5d821c12 + 0748cced + any audit-walk surfaces)
      - `## ADR review` section with per-cell L/M/H verdicts on Option A `continue` — outcome: (<a|b|c>)
      - `## Empirical cross-check` covering <3+> Phase-41-touched fork-shared files
      - UPST6 backlog stub at <D-42-B4 location>
    skipped_gates_load_bearing: []
    skipped_gates_environmental:
      - make ci (Windows host — `make` not on PATH; substitute D-42-E7 invariant `git diff --name-only HEAD~3..HEAD -- crates/ bindings/ scripts/ | wc -l == 0` per Phase 33 + 39 Rule 3 deviation precedent — Phase 42 ships only docs + ROADMAP + STATE edits; structurally zero clippy/fmt/test risk)
    ---

    # Plan 42-01 Summary

    ## Plan summary
    <1-paragraph statement of what was done — references the 5 ROADMAP success criteria as the closure-binding>

    ## Decisions implemented
    - D-42-A1 — range = v0.53.0..v0.54.0 (locked tag-pair boundary, not HEAD)
    - D-42-A2 — frontmatter captures range + upstream_head_at_audit + drift-tool shas + invocation + fork_baseline + date + total_unique_commits
    - D-42-A3 — `upstream_head_at_audit` locked at FIRST commit of Plan 42-01 via `git fetch upstream --tags && git rev-parse upstream/main`
    - D-42-A4 — strictly silent on the 4 known post-v0.54.0 commits (deferred to UPST6)
    - D-42-B1 — single plan (42-01-PLAN.md); no override warranted
    - D-42-B2 — 8 close-gate checks all PASS (see § Validation results)
    - D-42-B3 — wave-hint annotations applied to <0 | 1 | N> clusters; Phase 43 retains full discretion
    - D-42-B4 — UPST6 stub location: <(a) v2.6 backlog | (b) v2.5 § Future Cycles | (c) `.planning/backlog/upst6-stub.md`>
    - D-42-C1/C2 — windows-touch column on all commit-row tables; D-42-C2 mechanical heuristic + judgment-override applied
    - D-42-C3 — windows-touch:yes default fork-preserve applied to 5d821c12 + 0748cced cluster; <upheld | upgraded with rationale>
    - D-42-C4 — per-cell L/M/H verdict table on 5 dimensions; outcome (<a | b | c>)
    - D-42-D1/D2/D3 — no mid-phase drift bugs surfaced; no UPST6 absorption events
    - D-42-E1/E2 — empirical cross-check 3 Phase-41-touched files
    - D-42-E3..E10 — all carry-forward invariants honored

    ## Validation results — 8 D-42-B2 close-gate checks
    | # | Check | Evidence | Status |
    |---|-------|----------|--------|
    | 1 | drift-tool re-run exit 0 | `make check-upstream-drift ARGS="--from v0.53.0 --to v0.54.0 --format json"` exit 0 | PASS |
    | 2 | ledger row count >= total_unique_commits | <N> ledger rows >= <total_unique_commits> drift count | PASS |
    | 3 | every cluster has disposition + rationale | <K> clusters / <K> dispositions / <K> rationales | PASS |
    | 4 | `## ADR review` grep-confirmable with per-cell L/M/H 5+ dimensions | `grep -c "^## ADR review$"` returns 1; `grep -cE "^\| (security|windows|maintenance|divergence|contributor)"` returns <5+> | PASS |
    | 5 | `## Empirical cross-check` >= 3 file rows | grep confirms section + <3+> file rows in table | PASS |
    | 6 | ROADMAP UPST6 stub committed | `grep -E "^### Phase TBD-NN: UPST6 — Upstream v0.54.0"` confirms | PASS |
    | 7 | STATE.md updated | Plan 42-01 close entry under Key Decisions; Current Position flipped | PASS |
    | 8 | D-42-E7 zero-source-edits invariant | `git diff --name-only HEAD~3..HEAD -- crates/ bindings/ scripts/ \| wc -l` == 0 | PASS |

    ## Deviations
    <e.g., "Phase 33 + 39 Rule 3 deviation inherited: on Windows host `make` not on PATH; substituted `bash scripts/check-upstream-drift.sh ...` for the drift-tool invocation (same Python script under the hood — identical output). D-42-B2 step 8 substituted the `git diff --name-only` invariant for `make ci` (Phase 42 ships only docs / ROADMAP / STATE edits with structurally zero clippy/fmt/test risk; cross-target clippy gate trivially N/A per CONTEXT § Threat-model + cross-target-verify-checklist). Empty if no deviations.">

    ## Hand-off to Phase 43
    The ledger at `.planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md` is the immutable input for Phase 43 UPST5 sync execution. The cluster summary table (top of ledger) feeds Phase 43 plan-phase plan-slicing. The two known windows-touch:yes commits (`5d821c12` + `0748cced`) carry explicit per-commit dispositions per success criterion #2 — Phase 43 must honor these without re-relitigating the call. Any `wave-hint:` annotations are advisory; Phase 43 retains full discretion to refine. The baseline-aware CI gate baseline SHA is `13cc0628` per `.planning/templates/upstream-sync-quick.md:102` (REQ-CI-03 closed at Phase 41 close); Phase 43 inherits this as the gate reference for `success → failure` regression detection. The UPST6 backlog stub at <D-42-B4 location> is the next cadence anchor (fires when v0.55.0+ ships or accumulated cherry-pick labor warrants).
    ```

    **Sub-task C — Self-audit (D-42-B2 8-step close-gate):**

    Run the 8 close-gate checks before committing:
    ```bash
    # Step 1: drift-tool re-run idempotent
    make check-upstream-drift ARGS="--from v0.53.0 --to v0.54.0 --format json" > /dev/null 2>&1
    echo "drift-exit=$?"  # expected 0

    # Step 2: ledger row count >= drift-tool surfaced count
    LEDGER_ROWS=$(grep -cE "^\| [0-9a-f]{7,40} \|" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md)
    DRIFT_COUNT=$(jq -r '.total_unique_commits' ci-logs-local/drift/drift-v054.json)
    test "$LEDGER_ROWS" -ge "$DRIFT_COUNT" && echo "row-coverage-ok: $LEDGER_ROWS >= $DRIFT_COUNT"

    # Step 3: every cluster has disposition + rationale
    CLUSTERS=$(grep -cE "^### Cluster: " .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md)
    DISPS=$(grep -cE "^- \*\*Disposition:\*\* \`?(will-sync|fork-preserve|won't-sync)\`?" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md)
    RATIONALES=$(grep -cE "^- \*\*Rationale:\*\* " .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md)
    test "$CLUSTERS" -eq "$DISPS" -a "$CLUSTERS" -eq "$RATIONALES" && echo "cluster-completeness-ok: $CLUSTERS clusters / $DISPS dispositions / $RATIONALES rationales"

    # Step 4: ADR review section present with per-cell L/M/H 5+ dimensions
    test $(grep -c "^## ADR review$" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md) -eq 1 && \
      test $(grep -cE "^\| (security|windows|maintenance|divergence|contributor)" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md) -ge 5 && \
      echo "adr-review-with-lmh-ok"

    # Step 5: Empirical cross-check section present with >=3 file rows
    test $(grep -c "^## Empirical cross-check" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md) -eq 1 && echo "empirical-section-ok"

    # Step 6: ROADMAP UPST6 stub committed
    grep -E "^### Phase TBD-NN: UPST6 — Upstream v0.54.0" .planning/ROADMAP.md && echo "upst6-stub-ok"

    # Step 7: STATE.md updated
    grep -E "Phase 42 Plan 42-01" .planning/STATE.md && grep -E "^Phase: 42 \(upst5-audit\)" .planning/STATE.md && echo "state-md-ok"

    # Step 8: D-42-E7 zero-source-edits invariant
    test $(git diff --name-only HEAD~3..HEAD -- crates/ bindings/ scripts/ 2>/dev/null | wc -l) -eq 0 && echo "d42e7-invariant-ok"
    ```

    If ANY step fails, STOP — fix before committing. The Phase 33 + 39 Rule 3 deviation (`make` not on PATH on Windows host) applies — auditor may substitute the D-42-E7 invariant grep as the structural equivalent for step 1 if `make` fails to dispatch (Phase 42 ships only docs + ROADMAP + STATE edits with structurally zero clippy/fmt/test risk). Document any substitution in 42-01-SUMMARY.md § Deviations.

    **Sub-task D — Commit (Phase 33 / 39 § Atomic single-commit-per-artifact-set pattern):**

    Three atomic commits (auditor discretion to fold B into A or split further):

    Commit A (DIVERGENCE-LEDGER.md only):
    ```
    docs(42-01): write DIVERGENCE-LEDGER for v0.53.0..v0.54.0

    Produces .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md per REQ-UPST5-01 /
    D-42-A1 (range v0.53.0..v0.54.0) / D-42-A2 (frontmatter reproducibility — drift-
    tool sha 0834aa66, upstream_head_at_audit locked at first commit per D-42-A3) /
    D-42-C1 (windows-touch column on commit rows) / D-42-C2 (mechanical detection +
    judgment override) / D-42-C3 (windows-touch defaults to fork-preserve unless
    empty fork-side) / D-42-C4 (per-cell L/M/H verdict table on 5 dimensions —
    UPGRADE vs Phase 39 prose-only) / D-42-E1 (empirical cross-check >=3 files).

    Audit invocation (D-42-A1): make check-upstream-drift ARGS="--from v0.53.0 \
    --to v0.54.0 --format json". Drift tool unchanged from Phase 24 ship through
    Phase 33 + 39 + 42; raw JSON not committed per D-42-A2.

    <N> clusters covering <total_unique_commits> commits in v0.54.0. Per-cluster
    dispositions from {will-sync, fork-preserve, won't-sync}. FIRST AUDIT CYCLE
    WITH WINDOWS-TOUCH:YES FIRES: 5d821c12 (Windows registry dword parse) +
    0748cced (robust Windows platform detection) carry windows-touch: yes with
    explicit per-commit dispositions per success criterion #2.

    ## ADR review section with per-cell L/M/H verdicts on Option A `continue` for
    5 dimensions (security posture / windows parity / maintenance cost / divergence
    risk / contributor velocity). Outcome: <(a) confirm | (b) amend | (c) flag
    future-supersede>. Phase 33 ADR remains Accepted; Phase 42 does NOT supersede.

    ## Empirical cross-check section sampling 3 Phase-41-touched files per D-42-E2;
    findings: <all-PASS | drift-tool-missed-X>.

    D-42-A4 strictly-silent-on-post-v0.54.0 invariant honored: the 4 known
    post-v0.54.0 commits (66c69f86, 803c6947, fc965ccc, 089cf6a0) are deferred
    to UPST6.

    Phase 43 (UPST5 sync execution) consumes this ledger as immutable input.

    Refs: REQ-UPST5-01, D-42-A1, D-42-A2, D-42-A3, D-42-A4, D-42-C1, D-42-C2, D-42-C3, D-42-C4, D-42-E1, D-42-E2
    Signed-off-by: <user-name> <user-email>
    ```

    Commit B (ROADMAP + STATE.md atomic close):
    ```
    docs(42-01): queue UPST6 + finalize STATE + ROADMAP

    ROADMAP: flip Phase 42 v2.5 entry to [x] (completed YYYY-MM-DD); flip Phase
    Details Plans counter to 1/1 with [x] 42-01-PLAN.md sub-bullet; append UPST6
    stub at <D-42-B4 location: v2.5 § Future Cycles | v2.6 backlog> with title
    `UPST6 — Upstream v0.54.0…+ <sync audit | sync execution>`, Depends on:
    Phase 43, Plans: 0 / TBD, Reference: Phase 33 + 39 + 42 ledgers + ADR § Future
    audit cadence.

    STATE.md: bump completed_phases counter; flip Current Position to Phase 42
    (upst5-audit) — Phase complete — ready for verification; insert Plan 42-01
    close entry under Key Decisions (v2.5) with all 8 D-42-B2 close-gate checks
    confirmed PASS.

    Refs: REQ-UPST5-01, D-42-B2, D-42-B4, D-42-E8
    Signed-off-by: <user-name> <user-email>
    ```

    Commit C (42-01-SUMMARY.md):
    ```
    docs(42-01): SUMMARY

    Mirrors Phase 33 + 39 Plan-01-SUMMARY shape. Records the 8 D-42-B2 close-gate
    PASS evidence + decisions implemented (D-42-A1..E10) + Phase 43 hand-off.

    Refs: REQ-UPST5-01
    Signed-off-by: <user-name> <user-email>
    ```

    After committing, run a final close-gate confirmation:
    ```bash
    # D-42-E7 final-invariant confirm
    test $(git diff --name-only HEAD~3..HEAD -- crates/ bindings/ scripts/ | wc -l) -eq 0 && echo "D-42-E7-final-ok"
    ```
  </action>
  <verify>
    <automated>grep -E "^Phase: 42 \(upst5-audit\)" .planning/STATE.md &amp;&amp; grep -E "Phase 42 Plan 42-01" .planning/STATE.md &amp;&amp; test -f .planning/phases/42-upst5-audit/42-01-SUMMARY.md &amp;&amp; grep -E "^phase: 42-upst5-audit$" .planning/phases/42-upst5-audit/42-01-SUMMARY.md &amp;&amp; grep -E "REQ-UPST5-01" .planning/phases/42-upst5-audit/42-01-SUMMARY.md &amp;&amp; grep -E "ADR review" .planning/phases/42-upst5-audit/42-01-SUMMARY.md &amp;&amp; grep -E "Empirical cross-check" .planning/phases/42-upst5-audit/42-01-SUMMARY.md &amp;&amp; test $(git diff --name-only HEAD~3..HEAD -- crates/ bindings/ scripts/ 2&gt;/dev/null | wc -l) -eq 0 &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - STATE.md frontmatter bumped: completed_phases bumped, total_plans + completed_plans bumped, last_updated stamped, last_activity stamped (counter normalization may be applied if pre-existing drift; documented in close entry)
    - STATE.md Current Position flipped to `Phase: 42 (upst5-audit) — EXECUTING` / `Status: Phase complete — ready for verification`
    - STATE.md `### Key Decisions` block gains Plan 42-01 close entry at top of v2.5 sub-section (reverse-chronological order), single-paragraph shape mirroring Phase 33 + 39 close entries, with all 8 D-42-B2 close-gate PASS evidence + ADR review outcome + Empirical cross-check findings + UPST6 stub location
    - `.planning/phases/42-upst5-audit/42-01-SUMMARY.md` exists with required frontmatter (phase, plan, status, requirements, commits, date, provides, skipped_gates_*) + required sections (Plan summary, Decisions implemented, Validation results table, Deviations, Hand-off to Phase 43)
    - All 8 D-42-B2 close-gate checks PASS (drift idempotent, row count >= drift count, cluster completeness, ADR review section + per-cell L/M/H grep, Empirical cross-check >=3 files, UPST6 stub grep, STATE.md updated, D-42-E7 zero-source-edits)
    - 2-3 atomic commits landed with DCO sign-offs and Refs lines citing the relevant D-42-* decision IDs
    - D-42-E7 invariant final-confirm: `git diff --name-only HEAD~3..HEAD -- crates/ bindings/ scripts/` returns 0 files
    - D-42-A4 strictly-silent invariant honored: no commit references 66c69f86 / 803c6947 / fc965ccc / 089cf6a0 as in-scope (only acknowledged as deferred to UPST6)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drift-tool JSON → ledger curation | Tool output is regenerable from upstream git history; ledger curation is auditor judgment translating data → strategic dispositions (will-sync / fork-preserve / won't-sync). Same boundary as Phase 33 + 39. |
| `upstream/main` git refs → audit | Treats upstream commit history at `https://github.com/always-further/nono.git` as the source of truth — tampering at the upstream remote would silently propagate, but is detectable via the `upstream_head_at_audit` sha in the ledger frontmatter (D-42-A3 locked at first commit of Plan 42-01). |
| Auditor judgment → windows-touch column + per-cell L/M/H verdicts | Mechanical heuristic (D-42-C2) is deterministic; judgment-override is documented inline in cluster rationale where it fires. Per-cell L/M/H verdicts (D-42-C4) are auditor judgment but the 5 dimensions are fixed verbatim per the Phase 33 ADR Decision Table, making verdicts comparable across audit cycles. |
| Empirical cross-check sampling → drift-tool blind-spot detection | Auditor's file selection from `phase41-touched-files.txt` is judgment-driven but the methodology (`git log v0.53.0..v0.54.0 -- <file>` + comparison against drift JSON commit list) is reproducible. |

## STRIDE Threat Register

Phase 42 ships ONLY documentation + ROADMAP + STATE edits (zero `.rs` / `.toml` / `.sh` / `.ps1` / `Makefile` edits per D-42-E7). Threat model is trivial — no new attack surface introduced. Inherited from Phase 33 + 39 with Phase-42-specific upgrades (T-42-01-05 + T-42-01-06 for windows-touch:yes column firing + per-cell L/M/H verdict introduction).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01-01 | T (Tampering) | Ledger row data — sha / subject / categories / files-changed / windows-touch falsification | mitigate | Source-of-truth is drift-tool JSON output. Ledger frontmatter captures `drift_tool_sh_sha` + `upstream_head_at_audit` + locked invocation verbatim per D-42-A2. Re-running the invocation MUST produce the same row set (idempotent; D-42-B2 close-gate step 1 enforces this). Disposition assignments are auditor judgment NOT data — falsifying them would be visible in code review against cluster rationale lines. |
| T-42-01-02 | I (Information Disclosure) | Drift JSON raw output | accept | Per D-42-A2 / D-42-E3, raw JSON is NOT committed (stored at `ci-logs-local/drift/drift-v054.json` per Phase 33 + 39 Rule 3 deviation precedent and discarded). Contains only public upstream commit data — no secrets. `ci-logs-local/` is in `.gitignore`. Acceptable. |
| T-42-01-03 | E (Elevation of Privilege) | `make check-upstream-drift` script execution | accept | The script is read-only against the local git repo (D-11 path filter, `--format json` output mode); does not write to the working tree, does not network beyond `upstream/main` ref reading. Phase 24 ship gate already audited this; drift-tool sha unchanged since 2026-04-29 through Phase 33 + 39 + 42. |
| T-42-01-04 | R (Repudiation) | Per-cluster disposition rationale | mitigate | D-42-C3 windows-touch-defaults-to-fork-preserve invariant + the audit cadence rule (D-42-E8) require disposition rationales to be specific. Generic rationales fail code review at the SUMMARY-writeback stage; the `## ADR review` section (D-42-C4) provides a falsifiable per-cell L/M/H verdict confirmation that the Phase 33 ADR remains compatible. |
| T-42-01-05 | T (Tampering) | windows-touch column falsification on the two known fires (5d821c12 + 0748cced) — Phase 42-specific upgrade | mitigate | D-42-C2 mechanical heuristic is deterministic. Auditor's per-commit disposition for windows-touch:yes commits MUST include explicit fork-side analog check (D-42-C3) recorded inline in the cluster rationale. Falsifying `yes` → `no` to allow unrestricted cherry-pick would still require auditor sign-off in the rationale paragraph — the falsification surface is small and visible. Phase 43 plan-phase reviews the ledger and can downgrade dispositions if the call is wrong. |
| T-42-01-06 | R (Repudiation) | Per-cell L/M/H verdict table — Phase 42-specific upgrade vs Phase 39 prose | mitigate | The 5 dimensions (security posture / windows parity / maintenance cost / divergence risk / contributor velocity) are fixed verbatim per the Phase 33 ADR Decision Table, making verdicts directly comparable across audit cycles. The verdict outcome (a confirm | b amend | c flag-future-supersede) is explicit and grep-falsifiable. Auditor cannot retreat to "the ADR is fine, no further analysis required" — Phase 42 mandates explicit per-cell verdicts as the empirical floor for future audits. |
| T-42-01-07 | I (Information Disclosure) | Empirical cross-check file selection bias | accept | Auditor preferentially samples Phase-41-touched files per D-42-E2; this is a known sampling bias but is documented as the methodology. The bias accepts the risk of missing drift-tool blind-spots in files Phase 41 did not touch in exchange for maximizing coverage of fork-side high-churn areas. D-42-D3 quick-task path remains open for any blind-spot surfaced. |
</threat_model>

<verification>
**D-42-B2 close-gate (8 falsifiable checks; ALL must PASS before plan close):**

1. **Drift-tool idempotence:** `make check-upstream-drift ARGS="--from v0.53.0 --to v0.54.0 --format json" > /dev/null 2>&1; echo $?` returns 0.
2. **Row count coverage:** `grep -cE "^\| [0-9a-f]{7,40} \|" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md` >= `jq -r '.total_unique_commits' ci-logs-local/drift/drift-v054.json`.
3. **Cluster completeness:** count of `### Cluster: ` headers == count of `**Disposition:** \`?(will-sync|fork-preserve|won't-sync)\`?` lines == count of `**Rationale:** ` lines.
4. **ADR review section + per-cell L/M/H:** `grep -c "^## ADR review$" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md` == 1 AND `grep -cE "^\| (security|windows|maintenance|divergence|contributor)" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md` >= 5.
5. **Empirical cross-check >= 3 files:** `grep -c "^## Empirical cross-check" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md` == 1; the cross-check table has >= 3 file rows.
6. **UPST6 stub committed:** `grep -E "^### Phase TBD-NN: UPST6 — Upstream v0.54.0" .planning/ROADMAP.md` finds the stub at the D-42-B4-chosen location (v2.6 backlog OR v2.5 § Future Cycles OR `.planning/backlog/upst6-stub.md`).
7. **STATE.md updated:** `grep -E "Phase 42 Plan 42-01" .planning/STATE.md && grep -E "^Phase: 42 \(upst5-audit\)" .planning/STATE.md`.
8. **D-42-E7 zero-source-edits invariant:** `git diff --name-only HEAD~3..HEAD -- crates/ bindings/ scripts/ | wc -l` == 0 (Phase 42 ships zero .rs / .toml / .sh / .ps1 / Makefile edits — structurally zero clippy/fmt/test risk).

**Additional structural invariants:**

- `grep -E "^drift_tool_sh_sha: 0834aa664fbaf4c5e41af5debece292992211559$" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md` returns 1 line.
- `grep -E "^range: v0.53.0..v0.54.0$" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md` returns 1 line.
- `grep -E "^fork_baseline: v0.53.0 \(Phase 40 UPST4 sync point — 2026-05-14\)$" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md` returns 1 line.
- `grep -E "5d821c12|0748cced" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md` returns at least 2 lines (the two known windows-touch:yes commits).
- `grep -cE "\| (yes|no) \|" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md` returns at least N (where N == total_unique_commits) — every commit-row carries a windows-touch verdict.
- D-42-A4 silent invariant: `grep -cE "66c69f86|803c6947|fc965ccc|089cf6a0" .planning/phases/42-upst5-audit/DIVERGENCE-LEDGER.md` may match 0 or only the acknowledgment lines in methodology / Fork-only-surface (no row dispositions).
</verification>

<success_criteria>
From ROADMAP § Phase 42 § Success Criteria (5 items, must all be TRUE at plan close):

1. **DIVERGENCE-LEDGER.md enumerates every upstream commit in `v0.53.0..<anchor>` that touches a fork-shared file** — anchor SHA is `v0.54.0` (`6b00932f`) per D-42-A1, locked at audit-open time per D-42-A3; commit-row count == drift-tool `total_unique_commits`.
2. **Every cluster has a disposition + windows-touch column entry + rationale; 5d821c12 + 0748cced are explicitly handled with a decision recorded** — D-42-C3 conservative default applied; per-commit dispositions explicit per success criterion #2.
3. **The `## ADR review` section is present (grep-confirmable) and either confirms or amends the Phase 33 Option A `continue` strategy with explicit per-cell L/M/H verdicts** — D-42-C4 upgrade applied (5 dimensions: security posture / windows parity / maintenance cost / divergence risk / contributor velocity); outcome (a) / (b) / (c) explicit; Phase 33 ADR remains Accepted.
4. **Empirical cross-check: spot-check at least 3 fork-shared files for any upstream path the drift tool missed (Phase 39 empirical-cross-check pattern)** — D-42-E1 / D-42-E2 applied; preferentially samples Phase-41-touched files.
5. **The audit ships zero `crates/` / `bindings/` / `scripts/` source-tree edits (D-39-E5 / D-42-E7 Windows-only-files invariant trivially honored)** — Phase 42 ships only docs + ROADMAP + STATE edits.

Additional acceptance binding (REQ-UPST5-01 from REQUIREMENTS.md):
- Per-cluster rationale references fork-only surface where applicable (D-42-E4 carry-forward).
- ADR review section confirms or amends Phase 33 ADR (Phase 33 ADR remains Accepted; no superseding ADR from Phase 42).
- Ledger is reproducible from frontmatter (`range` + `upstream_head_at_audit` + `drift_tool_sh_sha` + `drift_tool_invocation` + `fork_baseline`).
- UPST6 placeholder queued in ROADMAP per D-42-B4 (v2.6 backlog OR v2.5 § Future Cycles); cadence rule from Phase 33 ADR § Future audit cadence honored (D-42-E8).
- STATE.md reflects Plan 42-01 closure with all 8 D-42-B2 close-gate PASS evidence.
- D-42-A4 strictly-silent-on-post-v0.54.0 invariant honored: the 4 known post-v0.54.0 commits (`66c69f86`, `803c6947`, `fc965ccc`, `089cf6a0`) are NOT dispositioned in the Phase 42 ledger.
- Cross-target clippy gate trivially N/A: Phase 42 touches zero `.rs` files; cross-target-verify-checklist (`.planning/templates/cross-target-verify-checklist.md`) is referenced as inheritance-only for Phase 43.
</success_criteria>

<output>
After completion, the Phase 42 directory contains:
- `42-CONTEXT.md` (UNCHANGED, locked at 2026-05-17)
- `42-DISCUSSION-LOG.md` (UNCHANGED)
- `42-01-PLAN.md` (this plan, UNCHANGED post-write)
- `DIVERGENCE-LEDGER.md` (NEW, ~130-180 lines) — canonical audit artifact for v0.53.0..v0.54.0; binding input for Phase 43
- `42-01-SUMMARY.md` (NEW) — plan-close summary mirroring Phase 33 + 39 Plan-01-SUMMARY shape

Project-level edits:
- `.planning/ROADMAP.md` — Phase 42 entry flipped to [x] complete; Phase Details Plans counter flipped to 1/1; UPST6 stub appended at D-42-B4-chosen location (recommended: v2.5 § Future Cycles holding section)
- `.planning/STATE.md` — completed_phases counter bumped; Current Position flipped to Phase 42 ready for verification; Plan 42-01 close entry inserted under Key Decisions (v2.5)

Next step: `/gsd-verify-work 42` (gsd-verifier sub-agent will produce `42-VERIFICATION.md` with PASS/FAIL evidence against the must_haves block above; expected ≥17/17 PASS given the D-42-B2 close-gate already enforces the same checks). Then `/gsd-plan-phase 43` to begin UPST5 sync execution.
</output>
