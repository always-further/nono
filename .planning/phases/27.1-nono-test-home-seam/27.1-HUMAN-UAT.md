---
status: partial
phase: 27.1-nono-test-home-seam
source: [27.1-VERIFICATION.md, 27.1-REVIEW.md]
started: 2026-05-05T00:00:00Z
updated: 2026-05-08T15:30:00Z
completed_session: 2026-05-08T15:30:00Z
session_outcome: "1 pass (Test 2 WR-01 disposition accepted as intentional design); 1 blocked (Test 1 audit-attestation tests gated on v2.4-FU-1/FU-2/FU-3 production-code follow-ups not yet landed). No gaps surfaced. Phase 27.1 partial-closure outcome stands per D-27.1-14 contingency."
---

## Current Test

[testing complete]

## Tests

### 1. Windows-host audit-attestation tests with v2.4-FU-1 + v2.4-FU-2 production fixes applied

expected: `cargo test -p nono-cli --test audit_attestation` returns `2 passed; 0 failed; 0 ignored` once v2.4-FU-1 (audit-loader swap in `crates/nono-cli/src/audit_commands.rs:12` from `rollback_session::load_session` to `audit_session::load_session`) AND v2.4-FU-2 (bundle-target architecture decision: mirror to audit_dir, dual-root verify, or test rewrite) AND v2.4-FU-3 (remove both `#[ignore]` attributes) all land. Currently `2 ignored` is the explicitly-allowed D-27.1-14 contingency outcome and is acceptable for v2.3 closure of REQ-NTH-03.
result: blocked
blocked_by: future-work
reason: "User confirmed 2026-05-08 during /gsd-verify-work 27.1: v2.4-FU-1 + v2.4-FU-2 + v2.4-FU-3 production-code follow-ups have NOT yet landed. Tests remain #[ignore]'d per D-27.1-14 contingency — explicitly-documented partial-closure outcome for REQ-NTH-03 acceptance #2. This is NOT a Phase 27.1 acceptance gap; the seam itself reaches the audit-attestation flow end-to-end on Windows host (verified by `nono run --audit-integrity` writing to <NONO_TEST_HOME>/.nono/audit/<id>/). Test 1 will flip to PASS when the v2.4 follow-ups land."
rationale: Requires v2.4 production code that is explicitly out of Phase 27.1 scope per D-27.1-14 ("Do NOT block the seam landing on Blocker 3"). Tracked in `.planning/phases/27.1-nono-test-home-seam/deferred-items.md`. Not a Phase 27.1 acceptance gap.

### 2. WR-01 split-brain home resolution disposition

expected: Either (a) WR-01 accepted as documented intentional design (NONO_TEST_HOME and `validated_home()` resolve to different sources by design — production users do not set NONO_TEST_HOME; the seam is a test-only de-facto contract per Plan 01 SUMMARY T-27.1-05), or (b) a follow-up phase opened to unify the two resolvers (e.g., make `validated_home()` honor NONO_TEST_HOME, or have `nono_home_dir()` cross-check NONO_TEST_HOME against the validated HOME chain).
result: pass
disposition: option (a) — intentional design accepted
confirmed: 2026-05-08T15:30:00Z
confirmed_by: oscarmackjr-twg via /gsd-verify-work 27.1 AskUserQuestion
reason: "Plan 01 SUMMARY T-27.1-05 disposition stands. NONO_TEST_HOME is a test-only de-facto contract; production users do not set it. The split-brain HOME resolution is a documented edge case that surfaces only under intentional misconfiguration (user sets both NONO_TEST_HOME and a divergent HOME). WR-01 closes as 'by design.' D-27.1-08 always-on-in-release-binaries lock stays in effect; the threat model for production users (who don't set NONO_TEST_HOME) is unchanged from pre-Phase-27.1."
rationale: WR-01 from `27.1-REVIEW.md` identifies a confused-deputy class hazard: `policy::sensitive_paths` expanding `~/.ssh` against the HOME chain while `audit_root()`/`rollback_root()`/`protected_paths` write to `<NONO_TEST_HOME>/.nono/`. Plan 01 SUMMARY T-27.1-05 disposition documents this as "intentional"; D-27.1-08 locks the seam as "always-on in release binaries" — making this a production-relevant design call, not a test-only concern. Disposition was explicit at planning time; human review confirms the disposition stands.

## Summary

total: 2
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 1

## Gaps
