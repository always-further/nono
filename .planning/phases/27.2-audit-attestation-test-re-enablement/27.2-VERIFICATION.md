---
phase: 27.2-audit-attestation-test-re-enablement
verified: 2026-05-09T19:00:00Z
status: passed
score: 16/16 must-haves verified
overrides_applied: 0
---

# Phase 27.2: Audit-Attestation Test Re-Enablement Verification Report

**Phase Goal:** Close v2.4-FU-1 (audit-loader swap from `rollback_session::load_session` -> `audit_session::load_session` for audit-only sessions) and v2.4-FU-2 (bundle-target architecture decision: mirror to audit_dir vs sign-to-session_dir vs dual-root verify), then remove the two `#[ignore]` attributes on `crates/nono-cli/tests/audit_attestation.rs` (FU-3) so REQ-AAH-01 (Phase 27) and REQ-NTH-03 (Phase 27.1) close fully on Windows host.

**Verified:** 2026-05-09T19:00:00Z
**Status:** passed
**Re-verification:** No (initial verification)

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                              | Status     | Evidence                                                                                                                                                          |
| -- | ------------------------------------------------------------------------------------------------------------------ | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1  | `nono audit verify <id>` resolves audit-only sessions via `audit_session::load_session`                            | VERIFIED   | `audit_commands.rs:12` imports `use crate::audit_session::load_session`; `cmd_verify:625` calls it; integration tests pass on Windows host (2/0/0)                |
| 2  | `nono audit verify <id>` resolves rollback-only and dual-target sessions correctly (no regression)                 | VERIFIED   | `audit_session::load_session` lines 184-188 iterate audit_root then rollback_root; combo session.json mirrored to audit dir per BL-01 fix in `rollback_runtime.rs:664-671`; tests pass |
| 3  | Fallback hits at legacy `<rollback>/<id>/` succeed and emit one `tracing::warn!` per process                       | VERIFIED   | `warn_once_legacy_bundle_path` defined `audit_session.rs:164-178` with `OnceLock<()>` guard; called at line 215; warning text contains legacy path, canonical path, "v2.5" |
| 4  | Full miss surfaces canonical `NonoError::SessionNotFound(session_id)` verbatim (no silent degradation)             | VERIFIED   | `audit_session.rs:221` returns `Err(NonoError::SessionNotFound(session_id.to_string()))` after exhausting both roots                                              |
| 5  | `audit_session::load_session` is no longer `#[allow(dead_code)]`                                                   | VERIFIED   | `audit_session.rs:181` has no attribute; line directly above is doc comment (line 180)                                                                            |
| 6  | `nono run --audit-integrity --audit-sign-key <key>` writes bundle to `<audit_root>/<id>/audit-attestation.bundle`  | VERIFIED   | `rollback_runtime.rs:217` unconditional `let session_dir = ensure_audit_session_dir(&session_id)?`; bundle write at `audit_attestation.rs:155` uses session_dir   |
| 7  | `nono run --audit-integrity --audit-sign-key <key> --rollback` writes bundle to audit_root, NOT rollback_root      | VERIFIED   | Same `session_dir` is now derived from audit_root unconditionally; integration test `rollback_signed_session_verifies_from_audit_dir_bundle` passes asserting bundle at audit_dir  |
| 8  | When `--rollback` is set, snapshot files (`changes/`, `manifest.json`) continue under `<rollback_root>/<id>/`      | VERIFIED   | `rollback_runtime.rs:244-252` creates rollback_dir via `ensure_rollback_session_dir` (BL-02 fix uses canonical helper); SnapshotManager routed to rollback_dir   |
| 9  | Audit attestation bundles outlive `rm -rf <rollback_root>/<id>/`                                                   | VERIFIED   | Bundle at `<audit_root>/<id>/` is structurally separate from rollback_root after `create_audit_state` change                                                      |
| 10 | ADR exists at `docs/architecture/audit-bundle-target.md` with full structure                                       | VERIFIED   | File exists; 94 lines; H2/H3 hierarchy matches `aipc-unix-futures.md`: Status frontmatter (5 fields incl. Supersedes), Context+Goals+Non-goals, Decision Table, Decision, Consequences (Positive/Negative/Backward-compat shim contract), References (Internal/Source code/Related ADRs) |
| 11 | ADR contains option matrix A/B/C with trade-offs and explicit Rejected verdicts on B and C                         | VERIFIED   | Decision Table at line 39-43 has Option A (Accepted), Option B (Rejected), Option C (Rejected) with rationale                                                      |
| 12 | ADR explicitly supersedes Plan 22-05a Decision 5 and documents v2.5 shim-removal milestone                         | VERIFIED   | Line 7: `**Supersedes:** Plan 22-05a Decision 5...`; line 73: removal milestone v2.5-FU-1 with cross-link to deferred-items.md                                  |
| 13 | `deferred-items.md` has v2.5-FU-1 + v2.5-FU-2 entries; existing v2.4 entries preserved                             | VERIFIED   | Line 101 H2 "Phase 27.2 v2.5 production follow-ups"; line 106 v2.5-FU-1; line 114 v2.5-FU-2; v2.4-FU-3 still at line 92                                          |
| 14 | Both `#[ignore]` attributes removed from `audit_attestation.rs`                                                    | VERIFIED   | `grep -c "#\[ignore"` returns 0; integration test runner reports neither test as ignored                                                                          |
| 15 | Test 2 JSON-shape assertions match `cmd_verify` flat output; nested keys removed                                    | VERIFIED   | Lines 507-508 assert `json["attestation_present"]` and `json["attestation_valid"]`; nested `json["attestation"]["present"]` removed; TODO(v2.5-FU-2) at line 509  |
| 16 | Bare `std::env::set_var/remove_var` in Test 1 wrapped by ScopedEnvVar RAII guard                                   | VERIFIED   | `struct ScopedEnvVar` at line 261; `impl Drop for ScopedEnvVar` at line 275; usage `let _env_guard = ScopedEnvVar::set(...)` at line 318                          |

**Score:** 16/16 truths verified

### Required Artifacts

| Artifact                                                              | Expected                                                              | Status      | Details                                                                                                                       |
| --------------------------------------------------------------------- | --------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `crates/nono-cli/src/audit_session.rs`                                | load_session live consumer; warn_once_legacy_bundle_path helper       | VERIFIED    | Lines 164-178 helper; line 215 call; line 181 load_session no longer dead_code; tracks audit-first → rollback-fallback         |
| `crates/nono-cli/src/audit_commands.rs`                               | cmd_verify uses audit_session::load_session                           | VERIFIED    | Line 12: `use crate::audit_session::load_session`; line 13: rollback_session import retained for other helpers; line 625 call  |
| `crates/nono-cli/src/rollback_runtime.rs`                             | create_audit_state collapsed to always-audit-dir for session_dir      | VERIFIED    | Line 217 unconditional `ensure_audit_session_dir`; lines 244-252 rollback_dir creation via canonical helper (BL-02 closure)   |
| `docs/architecture/audit-bundle-target.md`                            | Audit bundle target ADR (>= 90 lines)                                 | VERIFIED    | 94 lines; complete heading hierarchy; option matrix; supersession; v2.5 milestone; backward-compat shim contract               |
| `.planning/phases/27.1-nono-test-home-seam/deferred-items.md`         | v2.5 follow-up entries appended                                       | VERIFIED    | New H2 at line 101; v2.5-FU-1 + v2.5-FU-2 H3 entries; existing v2.4 sections untouched                                        |
| `crates/nono-cli/tests/audit_attestation.rs`                          | Both tests re-enabled; ScopedEnvVar guard; flat JSON assertions      | VERIFIED    | 0 `#[ignore]` attributes; ScopedEnvVar at lines 261-283; flat JSON at lines 507-508; TODO(v2.5-FU-2) at line 509              |

### Key Link Verification

| From                                                                | To                                                            | Via                                                                | Status   | Details                                                                                                              |
| ------------------------------------------------------------------- | ------------------------------------------------------------- | ------------------------------------------------------------------ | -------- | -------------------------------------------------------------------------------------------------------------------- |
| audit_commands.rs::cmd_verify                                       | audit_session.rs::load_session                                | `use crate::audit_session::load_session`                           | WIRED    | Line 12 import; line 625 call; integration tests exercise the path                                                    |
| audit_session.rs::load_session                                      | warn_once_legacy_bundle_path                                  | call on fallback-root hit (`if !is_primary` branch)                | WIRED    | Line 215 call inside fallback branch; helper defined at lines 164-178                                                |
| warn_once_legacy_bundle_path                                        | tracing::warn!                                                | OnceLock<()>-guarded one-shot emission                             | WIRED    | Line 165 `static WARNED: std::sync::OnceLock<()>`; tracing::warn at line 167 with v2.5 milestone substring          |
| rollback_runtime.rs::create_audit_state                             | crate::audit_session::ensure_audit_session_dir                | unconditional call when audit_integrity_requested                  | WIRED    | Line 217 unconditional binding; consumed at sign_session_attestation call site                                        |
| AuditState.session_dir                                              | sign_session_attestation bundle write                         | `session_dir.join(ATTESTATION_BUNDLE_FILENAME)`                    | WIRED    | session_dir flows from line 217 to bundle write at audit_attestation.rs:155                                          |
| Test 2 (rollback_signed_session_verifies_from_audit_dir_bundle)     | cmd_verify flat JSON output                                   | `json["attestation_present"] / json["attestation_valid"]`           | WIRED    | Lines 507-508 match cmd_verify output at `audit_commands.rs:651-659`; tests pass                                     |
| Test 1                                                              | ScopedEnvVar guard                                            | `let _env_guard = ScopedEnvVar::set(env_var.clone(), &secret);`    | WIRED    | Line 318; lifetime scoped to function body covering subprocess invocation                                             |
| docs/architecture/audit-bundle-target.md                            | Plan 22-05a Decision 5 supersession                           | Status frontmatter Supersedes field                                | WIRED    | Line 7: `**Supersedes:** Plan 22-05a Decision 5 ("for backward compatibility" dual-location rationale)`              |
| docs/architecture/audit-bundle-target.md                            | v2.5-FU-1 milestone                                           | Backward-compat shim contract -> Removal milestone                 | WIRED    | Line 73 references v2.5-FU-1 with cross-link to deferred-items.md                                                     |
| deferred-items.md (v2.5-FU-1 entry)                                 | ADR cross-link                                                | v2.5-FU-1 body links to docs/architecture/audit-bundle-target.md   | WIRED    | Line 110 includes "see `docs/architecture/audit-bundle-target.md` § 'Backward-compat shim contract'"                  |

### Data-Flow Trace (Level 4)

| Artifact                            | Data Variable        | Source                                                                            | Produces Real Data | Status   |
| ----------------------------------- | -------------------- | --------------------------------------------------------------------------------- | ------------------ | -------- |
| audit_commands::cmd_verify          | session              | audit_session::load_session() (real disk lookup; canonicalize+starts_with)        | Yes                | FLOWING  |
| rollback_runtime::create_audit_state| session_dir          | ensure_audit_session_dir(session_id) (real `fs::create_dir_all` + permissions)    | Yes                | FLOWING  |
| audit_session::load_session         | metadata             | SnapshotManager::load_session_metadata(&dir) (reads session.json)                 | Yes                | FLOWING  |
| audit_attestation::sign_session_... | bundle_path          | session_dir.join(ATTESTATION_BUNDLE_FILENAME) -> std::fs::write                   | Yes                | FLOWING  |
| Test 2 stdout JSON                  | json                 | serde_json::from_slice(&verify_output.stdout) (real subprocess output)            | Yes                | FLOWING  |

### Behavioral Spot-Checks

| Behavior                                                              | Command                                                                | Result                                            | Status |
| --------------------------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------------------------------- | ------ |
| nono-cli compiles cleanly                                             | `cargo build -p nono-cli`                                              | `Finished dev profile`                            | PASS   |
| Audit-attestation integration tests pass on Windows host              | `cargo test -p nono-cli --test audit_attestation`                       | `2 passed; 0 failed; 0 ignored`                    | PASS   |
| Both tests visible without `(ignored)` markers in test list           | `cargo test -p nono-cli --test audit_attestation -- --list`             | Both names listed without ignored markers         | PASS   |
| `crates/nono/` byte-identical across phase 27.2                       | `git diff --stat 9d3e5930~1..HEAD -- crates/nono/`                       | empty                                             | PASS   |
| audit_commands.rs has expected loader-swap                            | `grep "use crate::audit_session::load_session"` and `grep "rollback_session::load_session"` | 1 hit, 0 hits                          | PASS   |
| audit_session.rs has warn helper with v2.5 + OnceLock                 | grep `warn_once_legacy_bundle_path`, `OnceLock`, `v2.5`                  | All present                                       | PASS   |
| ADR file >= 90 lines with required sections                           | `wc -l docs/architecture/audit-bundle-target.md`                        | 94 lines                                          | PASS   |
| 0 `#[ignore]` attributes in audit_attestation.rs                       | `grep -c "#\[ignore"`                                                   | 0                                                 | PASS   |

### Requirements Coverage

| Requirement     | Source Plan(s)                  | Description                                                              | Status     | Evidence                                                                                                                       |
| --------------- | ------------------------------- | ------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------ |
| REQ-AAHX-01     | 27.2-01-PLAN.md                 | cmd_verify audit-loader correctness for audit-only sessions              | SATISFIED  | Loader swap landed (`audit_commands.rs:12`); audit_session::load_session live consumer; tests pass for audit-only AND combo (BL-01 fix mirrors session.json) |
| REQ-AAHX-02     | 27.2-02-PLAN.md, 27.2-03-PLAN.md | Bundle-target architecture decision (ADR + implementation)               | SATISFIED  | ADR at `docs/architecture/audit-bundle-target.md` (94 lines, full structure); implementation in `rollback_runtime.rs:217` always-audit-dir; both tests pass |
| REQ-AAHX-03     | 27.2-04-PLAN.md                 | Audit-attestation tests re-enabled and passing on Windows host            | SATISFIED  | Both `#[ignore]`s removed; ScopedEnvVar RAII guard added; Test 2 flat JSON shape; `2 passed; 0 failed; 0 ignored` on Windows host |

**Transitive closure:** REQ-AAH-01 (Phase 27) and REQ-NTH-03 (Phase 27.1) close transitively per the AAHX requirement coverage map.

### Anti-Patterns Found

| File                                            | Line  | Pattern                          | Severity | Impact                                                                                                                |
| ----------------------------------------------- | ----- | -------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------- |
| crates/nono-cli/tests/audit_attestation.rs      | 509   | `TODO(v2.5-FU-2)`                | Info     | Documented forward-pointer to v2.5 schema bump; tracked in deferred-items.md v2.5-FU-2; not a stub                  |
| crates/nono-cli/src/exec_strategy_windows/supervisor.rs | 788, 800 | `collapsible_match` clippy warnings | Info | Pre-existing (Phase 27.1 deferred-items); explicitly out-of-scope for Phase 27.2; not from this phase's modifications |

No blocker or warning anti-patterns introduced by Phase 27.2. The two clippy errors in supervisor.rs are pre-existing and explicitly listed as out-of-scope per Phase 27.1 deferred-items.md.

### Scope Deviations Identified (Resolved)

Two scope-deviation commits documented in 27.2-04-SUMMARY.md and 27.2-REVIEW.md:

1. **`2b7425e7` — `fix(27.2-04): route tracing subscriber to stderr to unblock --json stdout purity`**
   - Touched `crates/nono-cli/src/cli_bootstrap.rs::init_tracing()` (D-27.2-12 said byte-identical).
   - Necessary to unblock human-verify failure surfaced by Test 4 (tracing-WARN line on stdout broke `serde_json::from_slice`).
   - 2-line addition: `.with_writer(std::io::stderr)` on no-log-file and stderr-fallback branches.
   - Verified at lines 107 and 115 of `cli_bootstrap.rs`. Already advertised in `--log-file` help text.

2. **`6e3a158f` — `fix(27.2-CR): close BL-01 + BL-02 from code review`**
   - BL-01: Mirror `session.json` to audit_state.session_dir for combo sessions (`rollback_runtime.rs:664-671`).
   - BL-02: Replace inlined `nono_home_dir().join(".nono").join("rollbacks")` with canonical `audit_session::ensure_rollback_session_dir(...)` helper to avoid Windows split-brain (USERPROFILE vs LOCALAPPDATA).
   - Removed `#[allow(dead_code)]` from `ensure_rollback_session_dir` since it has live caller.

Both deviations are documented in commit messages, 27.2-04-SUMMARY.md ("Post-execution closure"), and 27.2-REVIEW.md ("Post-review BLOCKER closure"). The orchestrator/reviewer authorized them inline per the GSD workflow's escalation gate.

### Cross-Cutting Invariants

| Invariant                                                                | Status   | Evidence                                                                                  |
| ------------------------------------------------------------------------ | -------- | ----------------------------------------------------------------------------------------- |
| D-27.2-11: `crates/nono/` byte-identical across phase 27.2 work          | VERIFIED | `git diff --stat 9d3e5930~1..HEAD -- crates/nono/` returns empty                          |
| D-27.2-12: `crates/nono-cli/src/` byte-identical (modulo authorized scope deviation) | DEVIATED-AUTHORIZED | `cli_bootstrap.rs` 2-line change (`2b7425e7`) and `rollback_runtime.rs` mirror addition (`6e3a158f`) both documented |
| D-27.2-13: locked test helpers (`setup_isolated_home`, `run_nono`, `only_audit_session_id`, `run_command_args`) unchanged | VERIFIED | Verified by reading test file lines 12-128 and 230-246; helper bodies match Phase 27.1 baseline |

### Human Verification Required

None. The plan included a `checkpoint:human-verify` (Task 4 of plan 27.2-04) which surfaced a tracing-stdout-pollution failure that was resolved inline by `2b7425e7`. Post-fix run confirms `2 passed; 0 failed; 0 ignored` (verified by re-running `cargo test -p nono-cli --test audit_attestation` during this verification — output captured above).

Phase 27.1 HUMAN-UAT Test 1 (REQ-AAHX-03 acceptance #4) re-verification on `/gsd-verify-work 27.1` is the orchestrator's separate ledger task and does not block phase 27.2 closure.

### Gaps Summary

No gaps identified. All 16 must-haves verified. Both ROADMAP success criteria satisfied:

- SC #1: `cargo test -p nono-cli --test audit_attestation` returns `2 passed; 0 failed; 0 ignored` on Windows host with `NONO_TEST_HOME` set — VERIFIED by direct test execution.
- SC #2: `cmd_verify` correctly resolves audit-only sessions via `audit_session::load_session`; rollback-only and dual-target sessions retain correctness — VERIFIED by code path inspection (loader swap at audit_commands.rs:12; BL-01 fix mirrors session.json for combo sessions).
- SC #3: Bundle-target architecture decision recorded as ADR (`docs/architecture/audit-bundle-target.md`); chosen path implemented; both audit-integrity flows produce verifiable bundles at canonical path — VERIFIED.

All three requirements (REQ-AAHX-01, REQ-AAHX-02, REQ-AAHX-03) close. Transitive closure of REQ-AAH-01 (Phase 27) and REQ-NTH-03 (Phase 27.1) follows.

---

_Verified: 2026-05-09T19:00:00Z_
_Verifier: Claude (gsd-verifier)_
