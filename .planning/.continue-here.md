---
context: default
milestone: v2.4
milestone_name: Complete the Partial Ports + UPST4
phase: (none yet — Phase 35 plan-phase blocked on malformed_roadmap)
status: paused
last_updated: 2026-05-12T16:41:18.278Z
supersedes: previous .continue-here.md (Phase 30 / v2.3 gaps_found era — that state was resolved by /gsd-complete-milestone v2.3 on 2026-05-12)
---

<current_state>
**Between phases, at v2.4 milestone start.** Two milestone-boundary commits pushed to `origin/main` today:

- `3981782f` `chore: archive v2.3 milestone` — v2.3 closed with documented carry-forwards; tag `v2.3` pushed.
- `24b0f0a3` `chore: open v2.4 milestone — Complete the Partial Ports + UPST4` — STATE.md / PROJECT.md / ROADMAP.md / REQUIREMENTS.md / MILESTONE-CONTEXT.md all locked.

User invoked `/gsd-plan-phase 35`. SDK init failed:

```
gsd-sdk query roadmap.get-phase 35
→ { "found": false, "error": "malformed_roadmap",
    "message": "Phase 35 exists in summary list but missing \"### Phase 35:\" detail section." }
```

The v2.4 milestone block in ROADMAP.md has summary-list bullets but no per-phase `### Phase N:` detail sections. Attempted to append via bash heredoc — failed with `unexpected EOF while looking for matching quote` at line 109. **No actual ROADMAP modification happened.**

Working tree at pause: one staged delete (`.planning/v2.3-MILESTONE-AUDIT.md`) leftover from v2.3 archive close where the unstaged-delete-side wasn't paired with the git mv at commit time. Audit is correctly archived at `.planning/milestones/v2.3-MILESTONE-AUDIT.md` per commit `3981782f`; just needs the delete staged + committed on resume.
</current_state>

<completed_work>

- ✓ `/gsd-complete-milestone` for v2.3 — manual archive (SDK auto-extract was producing garbage)
- ✓ Archive files: `v2.3-ROADMAP.md`, `v2.3-REQUIREMENTS.md` (moved), `v2.3-MILESTONE-AUDIT.md` (moved)
- ✓ ROADMAP collapsed v2.3 to `<details>` block (463→97 lines); MILESTONES.md v2.3 entry; PROJECT.md updated
- ✓ Commit `3981782f` + tag `v2.3` + push to origin
- ✓ `/gsd-new-milestone` for v2.4: name="Complete the Partial Ports + UPST4", scope=all 3 themes (~14-18 weeks)
- ✓ `gsd-sdk state.milestone-switch` updated STATE.md frontmatter; body title + Current Focus edited
- ✓ PROJECT.md Current Milestone locked with v2.4 goal + per-phase target features + out-of-scope deferrals
- ✓ REQUIREMENTS.md fresh: 14 REQ-IDs across PORT-CLOSURE-01..07, RESL-NIX-01..03, PKGS-01+04, UPST4-01..02, AAHX-HOST-01
- ✓ ROADMAP.md v2.4 block + Phase 35-40 summary list entries appended (123 lines total)
- ✓ MILESTONE-CONTEXT.md → milestones/v2.4-MILESTONE-CONTEXT.md (git mv)
- ✓ Commit `24b0f0a3` + push to origin
- ✗ `/gsd-plan-phase 35` blocked on malformed_roadmap; bash heredoc append failed mid-write

</completed_work>

<remaining_work>

**Step 1 (mandatory):** Append `## Phase Details (v2.4)` to `.planning/ROADMAP.md` via the **Write tool** (NOT bash heredoc — that already failed). Detail blocks for Phase 35, 36, 36.5, 37, 38, 39, 40 (7 total).

Format per block (SDK validates this shape):

```markdown
### Phase N: <Name>

**Goal:** <one sentence>

**Trigger:** <why now>

**Requirements:** REQ-XXX-01, REQ-XXX-02 (N reqs).

**Depends on:** <structural> | **Operational dependency:** <e.g., Linux/macOS host>

**Plans:** 0 plans (run `/gsd-plan-phase N`)

Plans:
- [ ] TBD (run `/gsd-plan-phase N`)

**Estimated effort:** <range>.

**Reference:** <pointers>
```

Pattern reference: `.planning/milestones/v2.3-ROADMAP.md` § Phase 25-34 detail sections (clean examples for design-only / cherry-pick / broker / D-20 manual-replay / audit shapes).

**Phase 35 scope correction (read carefully):** The current ROADMAP.md v2.4 milestone summary lists Phase 35 as including "half of REQ-PORT-CLOSURE-05 (escape-quote pipeline; P34-DEFER-08b-2 depends on 08b-1 ordering)." **This is wrong** — P34-DEFER-08b-2 depends on P34-DEFER-08b-1 (ExecConfig refactor) which is in Phase 36. Phase 35 detail block scope should be ONLY:
- REQ-PORT-CLOSURE-01 (Windows env-filter wiring; P34-DEFER-08a-1; ~3-5 days)
- REQ-PORT-CLOSURE-06 (Linux Landlock profiles-dir; P34-DEFER-09-1; ~2-3 days)
- REQ-PORT-CLOSURE-07 (Windows test-harness hygiene; P34-DEFER-01-1 + 10-1; ~3-5 days)

Total Phase 35 effort: ~2 weeks. Note in the Phase 35 detail block that P34-DEFER-08b-2 is folded into Phase 36 as part of REQ-PORT-CLOSURE-05.

**Step 2:** Stage the leftover `.planning/v2.3-MILESTONE-AUDIT.md` delete (`git add -u`).

**Step 3:** Commit as `docs(roadmap): add Phase 35-40 detail sections for v2.4` (covers both the detail-section append AND the leftover audit-delete cleanup).

**Step 4:** Push to origin.

**Step 5:** Re-run `gsd-sdk query roadmap.get-phase 35` to confirm `found: true`.

**Step 6:** Invoke `/gsd-plan-phase 35` and follow normal workflow.

</remaining_work>

<decisions_made>

- v2.4 name: "Complete the Partial Ports + UPST4" (user-selected, 4 options)
- v2.4 scope: all 3 themes ~14-18 weeks (user-selected, 4 options)
- v2.4 phase structure: 35 (quick wins ~2w) → 36 (deep closure ~4-6w) → 36.5 optional (profile drafts ~1w) → 37 (v2.3 carry-forward Linux/macOS host-required ~2w) → 38 optional (Phase 27 reopen ~2-3d) → 39 (UPST4 audit ~1w) → 40 (UPST4 execution ~2-3w)
- Phase 35 scope correction: drop P34-DEFER-08b-2 escape-quote pipeline (depends on 08b-1 in Phase 36). Phase 35 = PORT-CLOSURE-01 + 06 + 07 only.
- Milestone-close manual mode: `gsd-sdk milestone.complete` auto-extracted garbage "accomplishments"; switched to manual MILESTONES.md authoring.

</decisions_made>

<blockers>
- **ROADMAP.md missing `### Phase N:` detail sections (35-40)** — blocks `/gsd-plan-phase 35`. Resolution: Write tool append (not heredoc).
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured state + next_action breadcrumb
2. `.planning/REQUIREMENTS.md` § Traceability + § REQ-PORT-CLOSURE-01/06/07 — Phase 35 scope source
3. `.planning/ROADMAP.md` § v2.4 milestone block — current shape (summary-only; needs detail-section append)
4. `.planning/milestones/v2.3-ROADMAP.md` — pattern reference for `### Phase N:` detail-section format
5. `.planning/milestones/v2.4-MILESTONE-CONTEXT.md` — v2.4 scope-themes provenance
6. `.planning/phases/34-upst3-upstream-v0-41-v0-52-sync-execution/deferred-items.md` § P34-DEFER-08a-1, 09-1, 01-1, 10-1 — Phase 35 effort + scope provenance

## Critical Anti-Patterns (do NOT repeat)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| `cat << 'EOF' >> file` with long markdown content | Bash heredoc failed at line 109 with `unexpected EOF while looking for matching quote`; no actual write. | blocking | Use Write tool for any append > ~20 lines OR containing nested code blocks / backticks / special quotes. |
| `gsd-sdk milestone.complete` auto-extraction | Pulled garbage from heterogeneous SUMMARY.md `one_liner` fields ("RED commit", "1. [Rule 1 — Bug]...", "One-liner:"). | advisory | Write milestone-close artifacts manually; don't trust SDK auto-extract. |
| Passing positional args to SDK `milestone.complete` | CLI interpreted `--help` and `v2.3` as positional version+name and wrote bogus MILESTONES.md/STATE.md/milestones/ entries. | advisory | `--dry-run` first OR manual write. |

## Infrastructure State

- Git: `main` at `24b0f0a3` (in sync with origin); tag `v2.3` pushed
- Working tree: 1 staged delete (`.planning/v2.3-MILESTONE-AUDIT.md` cleanup pending; file correctly archived at milestones/)
- No running services / background processes
- `gh` CLI available; upstream remote configured at `https://github.com/always-further/nono`

<context>
The user completed Phase 34 + v2.3 close + v2.4 open in a long single session today. Then invoked `/gsd-plan-phase 35` intending to start the UPST3-closure quick-wins work. The plan-phase init hit a structural mismatch — ROADMAP.md has v2.4 summary list but no per-phase detail sections — and the bash heredoc attempt to add them failed mid-write.

Resume is mechanical: one Write operation (~120-150 lines of markdown) + one commit + one push + plan-phase invocation. The v2.4 scope is well-locked across PROJECT.md / REQUIREMENTS.md / ROADMAP.md / STATE.md — resume just needs the ROADMAP detail-section append to unblock the SDK.

Context budget at pause: ~66% used / 34% remaining. Fresh session has full budget for Write + commit + plan-phase chain.
</context>

<next_action>
1. Read `.planning/HANDOFF.json` for the next_action breadcrumb.
2. Read `.planning/REQUIREMENTS.md` § Traceability + REQ-PORT-CLOSURE-01/06/07 to confirm Phase 35 scope.
3. Read `.planning/milestones/v2.3-ROADMAP.md` § Phase 25-34 detail sections for the `### Phase N:` format pattern.
4. Use **Write tool** (NOT bash heredoc) to append `## Phase Details (v2.4)` section to `.planning/ROADMAP.md` with detail blocks for Phases 35, 36, 36.5, 37, 38, 39, 40. Apply the Phase 35 scope correction (drop P34-DEFER-08b-2; Phase 35 = PORT-CLOSURE-01 + 06 + 07 only; estimated ~2 weeks).
5. `git add -u .planning/v2.3-MILESTONE-AUDIT.md` to stage the leftover delete; `git add .planning/ROADMAP.md`; commit `docs(roadmap): add Phase 35-40 detail sections for v2.4`.
6. Push to origin.
7. Verify: `gsd-sdk query roadmap.get-phase 35` should return `found: true`.
8. Invoke `/gsd-plan-phase 35`.
</next_action>
