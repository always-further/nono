---
milestone: v2.4
milestone_name: Complete the Partial Ports + UPST4
audited: 2026-05-15T12:00:00Z
status: gaps_found
status_reason: |
  5 of 15 v2.4 requirements are unsatisfied (REQ-RESL-NIX-01..03 + REQ-PKGS-01 + REQ-PKGS-04) because Phase 37 has 0 plans / 0 summaries and is host-blocked on Linux/macOS availability. 1 optional requirement (REQ-AAHX-HOST-01) is similarly unsatisfied in host-blocked Phase 38. 6 requirements (REQ-PORT-CLOSURE-01,02,04,05,06,07) are STRUCTURALLY satisfied in code but flagged `partial` because their owning phase VERIFICATION.md is `human_needed` — code is shipped, human-verify on other-OS host is pending.

  Status `gaps_found` is automatic per workflow rules. The gaps are environmental (host availability + human-verify pacing), not functional regressions. Cross-phase integration is CLEAN — gsd-integration-checker verified 7/7 wiring claims PASS, 0 BLOCKERS, 0 silent regressions, 0 orphaned exports across in-scope phases (35, 36, 36.5, 39, 40).
scores:
  requirements_satisfied: 3
  requirements_partial: 6
  requirements_unsatisfied: 5
  requirements_optional_unsatisfied: 1
  requirements_total: 15
  phases_completed: 5
  phases_host_blocked: 2
  phases_total: 7
gaps:
  requirements:
    - id: REQ-RESL-NIX-01
      status: unsatisfied
      phase: "37"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: missing
      evidence: "Phase 37 has disk_status: no_directory; 0 plans, 0 summaries. Plan 25-01 + CONTEXT artifacts committed in v2.3 (`3ed80d38`) but never lifted into Phase 37 directory because Linux host has not become available."
      blocker_class: environmental
      reason: Linux host required (cgroup v2 backend implementation + integration tests).
    - id: REQ-RESL-NIX-02
      status: unsatisfied
      phase: "37"
      verification_status: missing
      evidence: same as REQ-RESL-NIX-01 (Phase 37 host-blocked)
      blocker_class: environmental
      reason: Linux host required (cgroup.kill wall-clock timeout implementation).
    - id: REQ-RESL-NIX-03
      status: unsatisfied
      phase: "37"
      verification_status: missing
      evidence: same as REQ-RESL-NIX-01 (Phase 37 host-blocked)
      blocker_class: environmental
      reason: macOS host required (setrlimit equivalents).
    - id: REQ-PKGS-01
      status: unsatisfied
      phase: "37"
      verification_status: missing
      evidence: |
        Production code for streaming refactor is LANDED in v2.3 Plan 26-02 (commits `9cb7770f..18eb3913`); REQ-PKGS-01 was marked complete in v2.3 milestone close. The v2.4 traceability table lists it under Phase 37 as "Not started (v2.3 carry-forward)" — this appears to reflect the v2.4 follow-up scope (auto-pull e2e tests) rather than the streaming refactor itself.
      blocker_class: paperwork_drift
      reason: |
        Likely a traceability-table cataloging error. The streaming refactor production code is in the codebase; only the e2e test pass on Linux/macOS host is pending (which is REQ-PKGS-04 scope, not REQ-PKGS-01).
        Recommend either (a) re-mark REQ-PKGS-01 as Complete and re-anchor to Phase 26 (v2.3), or (b) accept the carry-forward framing and treat REQ-PKGS-01 as host-blocked alongside REQ-PKGS-04.
    - id: REQ-PKGS-04
      status: unsatisfied
      phase: "37"
      verification_status: missing
      evidence: |
        Production code (`load_registry_profile` + `is_registry_ref`) landed in v2.3 Plan 26-02. The 3 auto-pull e2e tests (acceptance #1-#3) require Linux/macOS host because Windows Plan 26-02 SUMMARY documented "e2e tests deferred to Linux/macOS host pass."
      blocker_class: environmental
      reason: Linux/macOS host required for auto-pull e2e test pass.
  optional_unsatisfied:
    - id: REQ-AAHX-HOST-01
      status: unsatisfied
      phase: "38"
      verification_status: missing
      evidence: "Phase 38 has disk_status: no_directory; 0 plans, 0 summaries. REQ-AAH-01 closed transitively via Phase 27.2 (FU-1+FU-2+FU-3) on Windows host. Native re-validation on Linux/macOS host is the optional confirmation that the closure has no host-native gap."
      blocker_class: environmental
      reason: "Optional + Linux/macOS host required. Marked 'Not started (optional)' in REQUIREMENTS.md; can be skipped if Phase 37 field-validation surfaces no Phase-27-related gap."
  partial_human_needed:
    - id: REQ-PORT-CLOSURE-01
      status: partial
      phase: "35"
      verification_status: human_needed
      structural_evidence: |
        Code shipped per 35-01-WIN-ENV-FILTER-SUMMARY.md. Wired env-filter fields (allowed_env_vars, denied_env_vars) into Windows ExecConfig + build_child_env; 4 Windows-gated regression tests added (`test_windows_empty_allow_denies_all_env_vars`, etc.).
      human_verify_blocker: "test_windows_*_env_vars are #[cfg(all(test, target_os = \"windows\"))] gated. SUMMARY records 4 passed on Windows dev host; independent re-confirm on this Windows host CAN run — recommend executing `cargo test -p nono-cli --lib exec_strategy_windows::launch::env_filter_tests` to flip status from human_needed → passed."
    - id: REQ-PORT-CLOSURE-02
      status: partial
      phase: "36"
      verification_status: human_needed
      structural_evidence: |
        Code shipped across 36-01a (deprecated_schema module port), 36-01b (canonical profile sections), 36-01c (override_deny → bypass_protection rename), 36-01d (data/docs/tooling migration). All 4 sub-plans frontmatter `requirements-completed: REQ-PORT-CLOSURE-02`.
      human_verify_blocker: |
        Verify items #2 (docs MDX render) is host-agnostic and could be confirmed on Windows. Items #1, #3, #4 are Linux/macOS or interactive-Windows gated. Partial human-verify is feasible on this host.
    - id: REQ-PORT-CLOSURE-04
      status: partial
      phase: "36"
      verification_status: human_needed
      structural_evidence: |
        Code shipped per 36-02-WIRING-YAML-MERGE-SUMMARY.md frontmatter (yaml_merge wiring trio + wiring.rs base abstraction).
      human_verify_blocker: same as REQ-PORT-CLOSURE-02 (phase-level human-verify items)
    - id: REQ-PORT-CLOSURE-05
      status: partial
      phase: "36"
      verification_status: human_needed
      structural_evidence: |
        Code shipped per 36-03-EXECCFG-SURGICAL-PORT-SUMMARY.md (b5f0a3ab deep ExecConfig refactor + escape-quote pipeline rider).
      human_verify_blocker: same as REQ-PORT-CLOSURE-02
    - id: REQ-PORT-CLOSURE-06
      status: partial
      phase: "35"
      verification_status: human_needed
      structural_evidence: |
        Code shipped per 35-02-LINUX-LANDLOCK-PROFILES-SUMMARY.md frontmatter (Linux Landlock profiles-dir pre-creation hunk + idempotency integration test).
      human_verify_blocker: "Phase 35 human-verify item #2 (Linux first-run UX confirmation) requires Linux host with kernel 5.13+. Cannot move on this Windows host."
    - id: REQ-PORT-CLOSURE-07
      status: partial
      phase: "35"
      verification_status: human_needed
      structural_evidence: |
        Code shipped per 35-03-WIN-TEST-HYGIENE-SUMMARY.md (Windows test-harness hygiene).
      human_verify_blocker: |
        Phase 35 human-verify item #3 (profile_cli debug-syntax tests) is host-agnostic and could run on Windows. Recommend `cargo test -p nono-cli --test profile_cli test_policy_show_json_no_rust_debug_syntax test_policy_diff_json_no_rust_debug_syntax` to flip status.
tech_debt:
  - phase: "35"
    items:
      - "Phase 35 verification status remains human_needed; 2 of 3 human-verify items are runnable on this Windows host (env_filter_tests + profile_cli debug-syntax tests). Running them would close REQ-PORT-CLOSURE-01 and REQ-PORT-CLOSURE-07."
  - phase: "36"
    items:
      - "Phase 36 verification status remains human_needed; 1 of 4 human-verify items is runnable on this Windows host (docs MDX render is host-agnostic). The other 3 need Linux/macOS or interactive Windows console."
  - phase: "39"
    items:
      - "Override applied for D-39-A1 range-strict: 2 commits (5d821c12 + 0748cced) deferred to UPST5 per `git describe --tags --contains` evidence. Captured in DIVERGENCE-LEDGER.md ADR review and ROADMAP UPST5 stub."
  - phase: "40"
    items:
      - "Override applied for D-40-E1 cumulative invariant: commit 96886ae9 added +4 lines to exec_strategy_windows/mod.rs via ScrubPolicy::secure_default() factory; codified D-40-E1 addendum in 40-CONTEXT.md. Sentinel SHA unchanged across 40-04..40-06."
      - "PR #922 contribution sections for 40-05 + 40-06 — addressed 2026-05-14 (post-verification orchestrator-merge step); now 6/6 plan sections in PR body."
      - "Phase 41 backlog (Linux/macOS clippy + test pre-existing failures, Windows build/integration/packaging/regression/security) carries forward unchanged. Baseline-aware CI gate documented; out of v2.4 scope."
  - phase: "27.2" (carry-forward from v2.3)
    items:
      - "Phase 27.2-04 tracing-stdout regression — RESOLVED 2026-05-09 in commit 2b7425e7. cargo test -p nono-cli --test audit_attestation returns 4 passed; 0 failed; 0 ignored. STATE.md blocker cleared 2026-05-14 (commit d8a1edb5)."
  - phase: "open audit items"
    items:
      - "20 quick-task entries in .planning/quick/ with status: missing — historical artifacts from earlier milestones (260405..260419). Most likely closed-but-not-marked; needs cleanup pass."
      - "4 v24 code-review todos in .planning/todos/pending/v24-cr-0[1-4]-*.md — broker FFI not-found mapping, broker null-handle validation, broker empty-handle-list path, job-object test skip policy. Small Windows-host tasks; could fit in v2.4 or carry to v2.5."
      - "1 debug session (40-rollback-status-cfg-gate, status: fixing) — appears stale; the Phase 40 Wave 0 fix-chain landed in commits a72736bb..92b71c8f. Recommend manual cleanup."
      - "REQUIREMENTS.md miscount: 'Total: 14 requirements' but the traceability table lists 15 rows (7 + 3 + 2 + 2 + 1 = 15). Off-by-one in the summary line."
deferred:
  - "Phase 37 (3 RESL-NIX + 2 PKGS reqs) — host-blocked; recommend re-anchoring to v2.5 in ROADMAP.md before v2.4 close, OR closing v2.4 with explicit `### Known Gaps` section documenting the 5 deferred reqs."
  - "Phase 38 (AAHX-HOST-01, optional) — host-blocked; recommend re-anchoring to v2.5 alongside Phase 37."
nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases: ["35", "36", "36.5", "39", "40"]
  overall: nyquist_validation_not_run_for_v2.4
---

# Milestone v2.4 — Audit Report

**Milestone:** v2.4 Complete the Partial Ports + UPST4
**Audited:** 2026-05-15
**Status:** gaps_found (environmental + paperwork; no functional regressions)
**Score:** 3 satisfied / 6 partial / 5 unsatisfied / 1 optional unsatisfied (out of 15)

---

## Executive Summary

v2.4 has shipped 5 of 7 phases on Windows host (35, 36, 36.5, 39, 40), absorbing 14 D-19 cherry-picks + 3 D-20 manual replays from upstream v0.52..v0.53, plus heavy P34 deferral closures (deprecated_schema, yaml_merge, ExecConfig refactor, profile-drafts, scrub module).

Two phases remain host-blocked:
- **Phase 37** (REQ-RESL-NIX-01..03, REQ-PKGS-01, REQ-PKGS-04) — Linux/macOS host required
- **Phase 38** (REQ-AAHX-HOST-01, optional) — Linux/macOS host required

Six requirements are STRUCTURALLY satisfied in code but their owning phase's VERIFICATION.md is `human_needed` because select human-verify items require live execution on other-OS hosts. Three of those human-verify items are actually runnable on this Windows host and would close those requirements if exercised today.

**The `gaps_found` status is mechanical (workflow rule) rather than indicative of broken code.** No cross-phase regression has been detected. Phase 40 verification passed 8/8 with one codified addendum override; Phases 36.5 and 39 passed clean; Phases 35 and 36 are blocked on human-verify pacing rather than code defects.

---

## Phase Status Summary

| Phase | Title | Status | Plans | Summaries | Verification |
|---|---|---|---|---|---|
| 35 | UPST3-closure quick wins | complete (code) / human_needed | 3 | 3 | 8/9 (1 human-verify pending — runnable on Windows) |
| 36 | UPST3 deep closure | complete (code) / human_needed | 6 | 6 | 17/17 (4 human-verify pending — 1 runnable on Windows) |
| 36.5 | Profile drafts (optional) | passed | 1 | 1 | 8/8 |
| 37 | v2.3 carry-forward Linux/macOS | host-blocked | 0 | 0 | missing (no_directory) |
| 38 | REQ-AAH-01 native re-validation (optional) | host-blocked | 0 | 0 | missing (no_directory) |
| 39 | UPST4 audit | passed (1 override) | 1 | 1 | 17/17 |
| 40 | UPST4 sync execution | passed (1 override + 1 deferred-now-resolved) | 6 | 6 | 8/8 |

---

## Requirements Coverage (3-Source Cross-Reference)

### Satisfied (3)

| REQ-ID | Phase | Evidence |
|---|---|---|
| REQ-PORT-CLOSURE-03 | 36.5 | Phase 36.5 passed (8/8); 36.5-01-DRAFTS-ABSORPTION-SUMMARY.md frontmatter `requirements-completed: [REQ-PORT-CLOSURE-03]` |
| REQ-UPST4-01 | 39 | Phase 39 passed (17/17 with override); 39-01-SUMMARY.md lists REQ-UPST4-01 |
| REQ-UPST4-02 | 40 | Phase 40 passed (8/8 with override); 40-01..40-06 SUMMARYs each list REQ-UPST4-02; baseline-aware CI gate clean on both Wave 2 head commits |

### Partial — structurally complete, human-verify pending (6)

| REQ-ID | Phase | Code shipped | Verify blocker |
|---|---|---|---|
| REQ-PORT-CLOSURE-01 | 35 | 35-01 env-filter wiring + 4 Windows-gated regression tests | Windows host re-confirm: runnable here today |
| REQ-PORT-CLOSURE-02 | 36 | 36-01a deprecated_schema port + 36-01b canonical sections + 36-01c bypass_protection rename + 36-01d docs/tooling | 1 of 4 verify items host-agnostic (docs MDX); 3 need Linux/macOS or interactive Windows console |
| REQ-PORT-CLOSURE-04 | 36 | 36-02 yaml_merge wiring trio + wiring.rs base abstraction | Same phase-level human-verify gate as 02 |
| REQ-PORT-CLOSURE-05 | 36 | 36-03 b5f0a3ab ExecConfig refactor + escape-quote pipeline rider | Same phase-level human-verify gate as 02 |
| REQ-PORT-CLOSURE-06 | 35 | 35-02 Linux Landlock profiles-dir pre-creation + idempotency integration test | Linux host required |
| REQ-PORT-CLOSURE-07 | 35 | 35-03 Windows test-harness hygiene | profile_cli debug-syntax tests host-agnostic: runnable here today |

### Unsatisfied (5, all host-blocked)

| REQ-ID | Phase | Reason | Recommendation |
|---|---|---|---|
| REQ-RESL-NIX-01 | 37 | Linux host required (cgroup v2 backend) | Re-anchor to v2.5 |
| REQ-RESL-NIX-02 | 37 | Linux host required (cgroup.kill timeout) | Re-anchor to v2.5 |
| REQ-RESL-NIX-03 | 37 | macOS host required (setrlimit) | Re-anchor to v2.5 |
| REQ-PKGS-01 | 37 | **Likely paperwork drift** — production code landed in v2.3 Plan 26-02; only e2e tests pending | Re-mark Complete or re-anchor explanation |
| REQ-PKGS-04 | 37 | Linux/macOS host required (auto-pull e2e tests) | Re-anchor to v2.5 |

### Optional Unsatisfied (1)

| REQ-ID | Phase | Reason |
|---|---|---|
| REQ-AAHX-HOST-01 | 38 | Optional + Linux/macOS host required; can skip if Phase 37 surfaces no Phase-27 gap |

---

## Cross-Phase Integration Check (gsd-integration-checker — clean)

7 of 7 cross-phase wiring claims verified. 2 WARNING-level documentation notes; **0 BLOCKERS**; 0 silent regressions; 0 orphaned exports inside v2.4 scope.

| # | Claim | Status | Evidence |
|---|---|---|---|
| 1 | Phase 36 deprecated_schema NOT overwritten by Phase 36.5 profile-drafts | PASS | Both surfaces coexist in `profile_cmd.rs:13` imports; `GLOBAL_DEPRECATION_COUNTER` alive at `deprecated_schema.rs:209`; `cmd_promote` alive at `profile_cmd.rs:456`; `package_status::enforce_for_active_profile` consumed at `profile_runtime.rs:171` |
| 2 | Phase 36 serde alias discipline (bypass_protection / override_deny) consistent with Phase 40-05 suppress_save_prompt | PASS | `profile/mod.rs:435,452,679` — three fields with `#[serde(default, alias = "...")]`. Test `filesystem_config_accepts_suppress_save_prompt_and_ignore_alias` at `profile_save_runtime.rs:1053` exercises round-trip |
| 3a | Phase 40-01 server.rs NODE_USE_ENV_PROXY block preserved through Phase 40-06 | PASS | `server.rs:101-119` (push block) + `server.rs:609-617` (test assertion) intact; Phase 40-06 commit `cfab2e8b` body explicitly preserves Wave 1 review fixes byte-identically |
| 3b | Phase 40-03 scrub module independent of Phase 40-04 Landlock ABI cache | PASS | Zero references to `scrub` anywhere in `crates/nono/src/sandbox/`; surfaces operate in separate subsystems |
| 3c | Sentinel SHA `96886ae9` unchanged across plans 40-04..40-06 | PASS | `git log 96886ae9..HEAD -- crates/nono-cli/src/exec_strategy_windows/mod.rs` returns empty. D-40-E1 codified-addendum exception holds |
| 4 | Phase 35 Windows env-filter — all 4 tests present | PASS | `exec_strategy_windows/launch.rs:2716` cfg-gated module + 4 test fns at lines 2761/2799/2831/2877; backing `build_child_env` at line 551 with deny/allow checks at 651-656 |
| 5 | `nono::scrub` re-export consumed by downstream | PARTIAL | Re-export at `crates/nono/src/lib.rs:97-100` consumed by 5 `nono-cli` call sites (`audit_integrity.rs:196`, `exec_strategy.rs:1588`, `output.rs:484`, `rollback_runtime.rs:632`, `supervised_runtime.rs:147`). **`nono-proxy` has zero consumers** — by design per Phase 40-03 SUMMARY (redaction is CLI/audit concern, not proxy). Memory wording "may consume it" matches |
| 6 | f77e0e3 policy structurally enforced in `credential.rs` | PASS | `credential.rs:112` uses `HashMap<String, LoadedCredential>` — single-credential-per-prefix by construction. `get()` at line 226 returns `Option<&LoadedCredential>` — 2-match-deny case is **type-system impossible**, not just doc-only |
| 7 | Phase 27.2-04 tracing → stderr fix preserved | PASS | `cli_bootstrap.rs:107,115` both use `.with_writer(std::io::stderr)`. Commit `2b7425e7` (2026-05-09) still in place |

### Integration WARNINGs (documentation-only, no fix needed in code)

1. **Phase-context path drift:** the milestone scope description placed `deprecated_schema.rs` at `crates/nono-cli/src/profile/deprecated_schema.rs`, but the actual location is `crates/nono-cli/src/deprecated_schema.rs` (top-level CLI source). Phase 36-01a SUMMARY's `key-files.created` is correct; wiring works via `crate::deprecated_schema::*` imports. No code change needed; phase-context prompt drift only.
2. **`nono::scrub` not consumed by `nono-proxy`:** by design (proxy module does not perform audit redaction). Wave-0 foundation IS reached by `nono-cli`. If a future phase needs proxy-side audit redaction, the surface is already exported.

### Requirements integration map (all v2.4 in-scope REQ-IDs WIRED end-to-end)

| REQ-ID | Integration path | Status |
|---|---|---|
| REQ-PORT-CLOSURE-01 | `launch.rs::build_child_env` + 4 env_filter_tests + ExecConfig Windows variant | WIRED |
| REQ-PORT-CLOSURE-02 | `deprecated_schema.rs::GLOBAL_DEPRECATION_COUNTER` ← `profile/mod.rs::detect_legacy_override_deny_key` ← profile_cmd dispatch | WIRED |
| REQ-PORT-CLOSURE-03 | `cmd_promote` ← `ProfileCommands::Promote` ← `cli.rs` `--draft` flag plumbing | WIRED |
| REQ-PORT-CLOSURE-04 | `wiring::apply_yaml_merge` ← `profile_cmd.rs:1327 cmd_patch` | WIRED |
| REQ-PORT-CLOSURE-05 | ExecConfig 17-field shape at `exec_strategy.rs:293-343`; bbdf7b85 escape-aware parser in `nono/src/diagnostic.rs` | WIRED |
| REQ-PORT-CLOSURE-06 | `pre_create_landlock_profiles_dir` at `profile_runtime.rs:137`, called from line 159; Linux-gated | WIRED |
| REQ-PORT-CLOSURE-07 | `strip_verbatim_prefix` + `suggested_flag_parts` shared between production code and tests in `query_ext.rs` / `profile_save_runtime.rs` | WIRED |
| REQ-UPST4-01 | Phase 39 DIVERGENCE-LEDGER.md (22 commits, 7 clusters) + ADR review | WIRED |
| REQ-UPST4-02 | 14 D-19 cherry-picks + 3 D-20 replays absorbed into `nono::scrub` re-export, SandboxState.allowed_domains, ScrubPolicy audit pipeline, `route::build_base_root_store`, `suppress_save_prompt`, CredentialStore structural enforcement | WIRED |

**Cross-phase wiring summary:** Connected 7/7 expected connections; orphaned 0 exports; missing 0 connections.

---

## Pre-Close Artifact Audit (44 open items)

| Category | Count | Disposition |
|---|---|---|
| Debug sessions | 1 | Stale (`40-rollback-status-cfg-gate` fixing — Wave 0 fix-chain landed); recommend close-out |
| Quick tasks | 20 | Mostly status=missing historical entries; cleanup pass needed |
| Threads | 0 | — |
| Todos | 4 | v24 CR items in pending/; small Windows-host work; route to v2.4 or v2.5 |
| Seeds | 0 | — |
| UAT gaps | 11 | Phases 35 + 36 human_uat (some runnable on Windows; see partial section above) |
| Verification gaps | 7 | Same as UAT gaps (human_needed verifications) |
| Context questions | 1 | Single unresolved item |
| **Total** | **44** | |

Per workflow step 0: if user chooses **[A] Acknowledge** on close, these items get documented in STATE.md `## Deferred Items`. Recommendation is to **resolve** the stale debug session + the 20 missing-status quick tasks before close (paperwork only), and accept the rest as deferred to v2.5.

---

## Recommended Routing

Two viable paths to v2.4 close, ordered by preference:

### Path A — Tighten v2.4 scope, then close (recommended)

1. Move Phases 37 + 38 out of v2.4 ROADMAP.md scope (re-anchor to v2.5 backlog with `host-blocked` annotation).
2. Run the 3 human-verify items that are runnable on Windows host:
   - `cargo test -p nono-cli --lib exec_strategy_windows::launch::env_filter_tests` (closes REQ-PORT-CLOSURE-01)
   - `cargo test -p nono-cli --test profile_cli test_policy_show_json_no_rust_debug_syntax test_policy_diff_json_no_rust_debug_syntax` (closes REQ-PORT-CLOSURE-07)
   - Render `docs/profile-authoring-guide.md` and verify bypass_protection blocks render (partial close on REQ-PORT-CLOSURE-02)
3. Clean up the 20 stale quick-task entries + the 1 stale debug session.
4. Re-mark REQ-PKGS-01 as Complete (v2.3 Phase 26-02 already shipped the production code).
5. Re-run `/gsd-audit-milestone v2.4` to re-score.
6. `/gsd-complete-milestone v2.4` — score should be 6 satisfied / 3 partial (Linux/macOS host items) / 0 unsatisfied in v2.4 scope; close with known gaps in v2.5 carry-forward.

### Path B — Close v2.4 as-is with explicit Known Gaps

1. `/gsd-complete-milestone v2.4` → choose [A] Acknowledge for the 44 open items + 5 unsatisfied requirements.
2. Document deferred items in STATE.md `## Deferred Items` and MILESTONES.md `### Known Gaps`.
3. v2.5 inherits the 5 host-blocked reqs + 4 v24 CR todos + human-verify backlog.

---

## Anti-pattern Watchlist (for v2.5)

These were the load-bearing lessons from Phase 40 — should be remembered for the next UPST sync:

1. **Cross-target clippy is load-bearing on Windows host.** `aws-lc-sys`/`ring` cross-compilers absent → categorize gates 3+4 as `skipped_gates_load_bearing` (NOT `skipped_gates_environmental`) in SUMMARY frontmatter. CI substitute is mandatory.
2. **PLAN COMPLETE not declarable until CI baseline-aware gate passes** (zero `success → failure` transitions vs documented baseline). Task 5 wait-for-CI gate baked into Wave 1+ plan templates from commit `be46f483`.
3. **SUMMARY frontmatter must distinguish** `skipped_gates_load_bearing: [3, 4]` (CR-A-class — CI must verify) from `skipped_gates_environmental: [6, 7, 8]` (runtime unavailable everywhere). Source: 40-`.continue-here.md` from Wave 0 incident.
4. **Fork uses ONE umbrella PR to upstream** (PR #922 pattern). Per-plan upstream PRs would require per-plan feature branches due to GitHub's one-PR-per-branch-pair rule.

---

## Audit Verdict

**`gaps_found` — environmental + paperwork drift, not functional.**

Cross-phase integration **clean** — gsd-integration-checker confirmed 7/7 wiring claims PASS, 0 BLOCKERS, 0 silent regressions across in-scope phases (35, 36, 36.5, 39, 40). All v2.4 in-scope REQ-IDs trace to multi-phase consumer paths with no orphaned surfaces.

All in-scope phase verifications pass or are `human_needed` on items that pre-date the close decision. The 5 unsatisfied requirements are categorically host-blocked (Linux/macOS), not implementation gaps. The integration dimension returns **clean**; the requirements dimension is the only reason status is not `passed`.

**Path A is the cleaner close** — invest 30-60 minutes in running 3 Windows-host human-verify items + cleanup pass + ROADMAP scope adjustment, then close v2.4 with a clean Known Gaps section. Path B is faster but carries more debt forward.
